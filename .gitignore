# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# prisma
prisma/dev.db
prisma/dev.db-journal

# vercel
.vercel

# backup files (created by patch scripts — never commit)
backup/
backups/
*.bak
*.bak*
*.backup
*.bak-narrative


# Turbo build cache
.turbo/

# production logs (may contain sensitive info)
*.log
logs/

# temp media uploads
.tempmediaStorage/
tmp/

.vercel
.env*.local
dev.db
scratch/

.vercel

# ไฟล์ทำงาน/สำรองในเครื่อง — ห้ามเข้า git (12 มิ.ย. 69)
backup-db/
audit-*.json
audit-scan.txt
quality-report.json
rerun-plan.json
rerun-results.json
deleted-cases-backup-*.json

# Local analysis artifacts (12 มิ.ย. screening)
postrollback-cases.json
prompt-classification.json
prompt-final-verdict.json
prompt-manipulate-screen.json
prompt-toxic-screen.json
viral-library-screen.json

# MEGA — ไฟล์ปกที่สายพานประกอบ (generated)
/public/mega-covers/

# ไฟล์ทดสอบ/สแครชที่ root ขึ้นต้นด้วย _ (payload เทส, run logs, report) — ห้ามเข้า git (7 ก.ค. 69)
# หมายเหตุ: ล็อกเฉพาะ root (/_*) จึงไม่กระทบไฟล์ใน src/ data/ ที่อาจขึ้นต้น _
# ไฟล์ยังอยู่บนดิสก์ครบ ระบบข่าว/ปกไม่ได้อ่านไฟล์พวกนี้ตอนรัน
/_*
