แก้: newsdesk-bridge.js — validate minScore เป็นเลข 0-100 (point 4ก). แก้บรรทัด 41: แทน `const MINSCORE = (A && A.minScore) || 0` ด้วย Number()+clamp — แปลงเป็นเลข, เช็ค Number.isFinite, ถ้าไม่ใช่เลขหรือ NaN ใช้ 0, แล้ว clamp ช่วง 0-100 (Math.min(Math.max(n,0),100)). กันค่า string เช่น '0";echo INJECTED;#' หลุดเข้า curl URL บรรทัด 85. ไม่แตะ validateBase/SEND_IDS/phase. (ถ้าสะดวก แก้ catch{} บรรทัด 20 เป็น catch (_e){} พ่วงด้วยเพื่อ strict-ES2018 — ตามคำสั่ง ES2018 เท่านั้น). node --check ผ่าน.
เฉพาะไฟล์: .claude/workflows/newsdesk-bridge.js
โซน: ระบบบริษัท AI เอง. แก้ได้: public/company/** และ .claude/workflows/newsdesk-*.js, company-*.js, eng-fix.js. 🔴ห้ามแตะระบบข่าวจริง/ท่อผลิตในโซนนี้. 🔴ห้ามแตะไฟล์หัวใจ: src/lib/ai/openai.js, src/lib/ai/aiRouter.js, src/lib/ai/claudeClient.js, prisma/schema.prisma, scripts/validate-workflow.mjs. แก้เฉพาะไฟล์ที่ระบุ · incremental (Edit) ห้าม rewrite ทั้งไฟล์ · รักษา backward-compat.