# 📋 รายงานตรวจระบบท่อข่าวอัตโนมัติ + วิจัย prompt-library + คดีย่อหน้า 3
**วันที่ตรวจ:** 1 ส.ค. 2569 · **ผู้ตรวจ:** Fable 5 (ultracode — ทีมตรวจ 14 ชุด + ฝ่ายค้านตรวจซ้ำ)
**สถานะรายงาน:** ✅ เขียนครบทุกส่วนแล้ว — ค้างอย่างเดียวคือเฟส "ฝ่ายค้านตรวจซ้ำ" บั๊ก 61 ข้อ (หยุดไว้เพื่อประหยัดโควตา · หลักฐานดิบพร้อมรันต่อ ดู memory `news-audit-handoff-aug1`)

---

## 1. สรุปผู้บริหาร (TLDR)
**ตอบ 3 คำถามหลักก่อน:**

1. **ย่อหน้า 3 พังเพราะอะไร** → **คำสั่งของระบบเราเองสั่งซ้อนกัน 3 ชั้นแล้วขัดกันเอง** ไม่ใช่โมเดลไม่เก่ง ไม่ใช่จับคู่ล้มเหลว — การ์ด 59/73 ใบสั่ง "ย่อหน้า 3 ปิดด้วยบทเรียน" + กฎเหล็กสั่ง "ทุกข่าวต้องมีบทเรียน" + STYLE PACK สั่ง "จบด้วยสัจธรรม" **แต่กฎเขียนอีกข้อสั่งห้าม "ไม่สรุปข้อคิดชีวิต" ที่ย่อหน้าเดียวกัน** → โมเดลประนีประนอมด้วยการแต่งสำนวนกึ่งวิเคราะห์ = "มุมที่น่าจำอยู่ที่..." (รายละเอียด+ทางแก้: ข้อ 6)

2. **prompt-library หยิบใช้จริงเสมอไหม** → **ใช้จริง แต่ไม่การันตี 100%** — งานจริงทุกชิ้นที่ตรวจได้ (รวมเช้านี้ 06:33-06:42) ใช้การ์ดจริง `matchType=EXACT` ครบ 55/55 รายการ ไม่มีเคสหลุดเลย **แต่โค้ดมีทางหลุด 4 ทาง และทางที่ 3 (เส้นคิวหาการ์ดไม่ได้) จะบันทึกป้ายหลอกว่า `library` ทั้งที่ใช้ built-in** → วันที่หลุดจริงคุณจะดู log ไม่ออก (ข้อ 5)

3. **ระบบมีบั๊กตรงไหน** → พบ 61 จุด · ร้ายแรง 9 ข้อ · **ที่สำคัญที่สุด: งานแก้บั๊ก 15 ข้อเมื่อ 25 ก.ค. ถูกย้อนกลับทั้งชุด ทำให้ความเสี่ยงเก่ากลับมาอยู่บนเว็บจริง 20 จาก 21 ข้อ** (ข้อ 4 และ 7.1)

**เรื่องด่วนที่ต้องเคาะ:**
- 🔴 **ด่านกันลิงก์ (TEXT_ONLY) ถูกลบจากไฟล์ในเครื่อง 4 ไฟล์** — เว็บจริงยังปลอดภัย แต่ต้องกู้ก่อนที่จะเผลอ commit (ข้อ 3)
- 🔴 **`/api/prompt-library` ลบหอสมุดทั้งคลัง 73 ใบได้ในคำสั่งเดียว ไม่มีรหัสผ่าน**
- 🔴 **ไม้สอง `gpt-4o` ตายทุกครั้งที่ถูกเรียก** (ส่งค่าเกินเพดานรุ่น) = เวลาโมเดลหลักล้ม ไม่มีตัวสำรองจริง

---

## 2. ขั้นตอนการทำงานของท่อข่าวตอนนี้

### 2.1 เส้นทางจริง (ทุกงานเข้าประตูเดียว = คิว)
```
[1] คนกดส่งที่หน้า /content/new
        │  (ทั้งข้อความล้วน / ลิงก์ / รูป → เข้าประตูเดียวกันหมด)
        ▼
[2] POST /api/queue/add ── ด่านกรอง 5 ชั้น
        │   • เช็คกุญแจ (เฉพาะคนนอกที่ส่ง header มา — เว็บตัวเองผ่านฟรี)
        │   • กันข้อความขยะ (เครื่องหมาย ? เกิน 30%)
        │   • [ที่นี่คือด่าน TEXT_ONLY กันลิงก์ — ตัวที่ถูกลบในเครื่อง ดูข้อ 3]
        │   • กันบอทยิงซ้ำ (msg-claim) + กันข่าวซ้ำ (คล้ายเกิน 62% ใน 45 นาที)
        ▼
[3] เข้าคิว (enqueueJob) — เลขงาน = ลายนิ้วมือของเนื้อหา
        │   เนื้อเดียวกัน = เลขเดิม = ไม่เจนซ้ำ  ·  แล้วเตะปลุกคนคุมคิว
        ▼
[4] /api/queue/worker ── คนคุมคิว (ถูกปลุก 4 ทาง: ตอน add, cron ทุก 1 นาที,
        │   หน้า status ปลุกเอง, UI ปลุกเมื่อรอนาน)
        │   • แบ่งงานตามเครื่อง: ★ งานข่าว = Vercel เท่านั้น ★ งานปก/คลิป = เครื่องทีม
        │   • คว้างานทีละ 1 ชิ้นแบบ atomic (กัน 2 เครื่องแย่งงานเดียวกัน)
        ▼
[5] POST /api/auto/process ── ตัวแยกทาง
        │        ├─ ข้อความล้วน ─→ processAutoFlowText  ← ★ เส้นที่วิ่งจริงทุกวันนี้
        │        └─ มีลิงก์ ─────→ processAutoFlow      ← ปิดอยู่ (ด่าน TEXT_ONLY)
        ▼
[6] สมองเจนข่าว (ทำตามลำดับ)
        ①  extract     ดึง/สกัดเนื้อข่าวออกมาเป็นข้อมูลดิบ
        ②  breakdown   วิเคราะห์ประเด็น แตกมุมของเรื่อง
        ③  blueprint (โครงอารมณ์)  ⟍ ทำ 2 อย่างนี้ขนานกัน
            smartResearch (ค้น Serper) ⟋
        ④  เลือกการ์ดพร้อมท์จากหอสมุด (promptMatcher) — ทีละมุข กันซ้ำ
            มุมที่ 2 เป็นต้นไป คะแนนจับคู่ต่ำกว่า 45 = ตัดทิ้ง
        ⑤  เขียนจริง (analyze) — ทำขนานทุกมุม แต่ละมุมมีเวลา 420 วินาที
        ⑥  ขัดเกลา (correction pipeline)
        ▼
[7] เก็บผลลง job + เซฟเข้าคลังข่าว → UI ถาม /api/queue/status ทุก 3 วินาที
```

### 2.2 ตารางแต่ละขั้น ใช้โมเดลอะไร เวลาเท่าไหร่ (สาย TEXT = เส้นจริง)
| ขั้น | โมเดลหลัก | ตัวสำรอง | เพดานเวลา | เพดานคำตอบ |
|---|---|---|---|---|
| ① extract | gemini-3.5-flash | gpt-5.5 | 15 วิ (ใน gemini) | 4,000 |
| ② breakdown | **gpt-5.6-terra** | gpt-4o (60 วิ) | 200 วิ / นอก 300 วิ | 24,000 |
| ③ blueprint | gpt-5.4-mini | ❌ ไม่มี | 120 วิ (ล้มได้ ไม่ตายทั้งงาน) | 1,200 |
| ③ research | Serper (ไม่ใช่ AI) | Tavily | 60 วิ | — |
| ④ เลือกการ์ด | **ไม่ใช้ AI** (คิดคะแนนเอง) | — | — | — |
| ⑤ เขียนจริง | gpt-5.5 สาย write | gpt-5.5 (90 วิ) | 180 วิ / นอก 420 วิ ต่อมุม | 10,000 |
| ⑥ ขัดเกลา | gpt-5.4-mini | — | — | 500–1,000 |

**หมายเหตุที่ควรรู้:**
- **AGENTS.md ตกรุ่น 1 จุด** — เขียนว่า breakdown ใช้ gpt-5.5 แต่ของจริงสาย TEXT ย้ายไป **gpt-5.6-terra** ตั้งแต่ 16 ก.ค. (สาย URL ยังเป็น gpt-5.5)
- **ป้ายชื่อโมเดลในโค้ดหลอกตา** — ในตัวจัดคิวโมเดล (`aiRouter.js`) เขียนว่า `'gpt4o'` แต่ของจริงเรียก **gpt-5.5** (ป้ายเก่าไม่ได้เปลี่ยนตาม)
- **แถบความคืบหน้าที่เห็นตอนรอ เป็นภาพเคลื่อนไหวตั้งเวลา ไม่ใช่สถานะจริง** — ผลจริงมาทีเดียวตอนงานเสร็จ
- สวิตช์คุมงาน: `GEN_ANGLES` (จำนวนมุมข่าว, ปกติ 2) · `GEN_PER_ANGLE` (เวอร์ชันต่อมุม, ปกติ 1) · `ANGLE_MIN_MATCH_SCORE` (ปกติ 45)

---

## 3. 🔴 เหตุการณ์ด่วน: ด่านกันลิงก์ (TEXT_ONLY) ถูกลบออกจากไฟล์ในเครื่อง

**เจออะไร:** โค้ดด่าน `TEXT_ONLY_MODE` (ตัวที่บล็อกไม่ให้เจนข่าวจากลิงก์/รูป — เจ้าของสั่งติดตั้งไว้ 16 ก.ค. และ memory ระบุ "ห้ามลบ") **ถูกลบออกจากไฟล์ในเครื่องนี้ 4 ไฟล์** โดยยังไม่ได้ commit:
1. `src/app/api/auto/route.js` (ลบ 11 บรรทัด)
2. `src/app/api/auto/process/route.js` (ลบ 12 บรรทัด)
3. `src/app/api/auto/stream/route.js` (ลบ 9 บรรทัด)
4. `src/app/api/queue/add/route.js` (ลบ 18 บรรทัด — ด่านหลักของระบบคิว)

**กระทบแค่ไหน:**
- ✅ **เว็บจริง (Vercel) ยังปลอดภัย** — deploy จาก branch main ซึ่งด่านยังอยู่ครบ
- ⚠️ **เครื่องนี้เสี่ยง** — ถ้ารันเซิร์ฟเวอร์จากโค้ดชุดนี้ (เช่น worker :3000 / เครื่องเทส :3900) งานลิงก์/รูปจะทะลุเข้าท่อได้ ทั้งที่สาย URL ถูกสั่งปิดเพราะคุณภาพไม่ถึง
- ใครลบ/ลบเมื่อไหร่: ไม่มีหลักฐานใน git (การแก้ยังไม่ commit) — สันนิษฐานว่าหลุดจากงานพัฒนาสายอื่นบน branch นี้

**วิธีกู้ (1 คำสั่ง — รอเจ้าของเคาะ ผมไม่ได้กู้ให้เพราะงานนี้เป็นการตรวจอย่างเดียว):**
```
git checkout HEAD -- src/app/api/auto/route.js src/app/api/auto/process/route.js src/app/api/auto/stream/route.js src/app/api/queue/add/route.js
```

---

## 4. บั๊ก/จุดเสี่ยงที่พบ (61 ข้อ — คัดตัวสำคัญมาเล่า)

> **หมายเหตุความน่าเชื่อถือ:** ข้อในหมวด 🔴 และ 🟠 มาจากทีมสแกนที่อ่านโค้ดจริงและอ้างไฟล์:บรรทัดครบ · เฟส "ฝ่ายค้านตรวจซ้ำ" ยังไม่ได้รัน (หยุดเพื่อประหยัดโควตา) ยกเว้นข้อที่ระบุว่า ✅ตรวจเองแล้ว

### 🔴 ร้ายแรง — ควรแก้ก่อน
**1. `/api/queue/clear` ใครยิงก็ได้ ไม่มีรหัสผ่าน** (`queue/clear/route.js:18`)
โหมดปกติลบผลงานที่เพิ่งเจนเสร็จ · โหมด `all` ล้างคิวทั้งระบบรวมงานที่กำลังวิ่ง — คนนอกที่รู้ URL ยิงได้เลย

**2. `/api/prompt-library` ลบหอสมุดทั้งคลังได้ในคำสั่งเดียว** (`prompt-library/route.js:153`)
`DELETE ?id=all` ไม่มีด่านตรวจสิทธิ์ ลบพร้อมล้างไฟล์สำรองด้วย = **หอสมุดการ์ด 73 ใบหายเกลี้ยงได้ในคลิกเดียว**

**3. ด่านกุญแจของคิวเป็นแบบ "ส่งมาถึงจะตรวจ"** (`queue/add/route.js:22`)
ไม่ส่ง header มาเลย = ผ่านฟรี และกุญแจสำรองที่ตั้งไว้คือคำว่า `test-key` ที่เขียนอยู่ในซอร์สโค้ด

**4. ไม้สองพัง 100% — `gpt-4o` ถูกส่งค่าเกินเพดานทุกครั้ง** (`ai/openai.js:189`)
เวลาโมเดลหลักล้ม ระบบส่งต่อให้ gpt-4o พร้อมเพดานคำตอบ 24,000 แต่ของจริง gpt-4o รับได้แค่ 16,384 → **ตัวสำรองตายทุกครั้งที่ถูกเรียก**

**5. งบเวลารวมเกินเพดานที่ Vercel ให้** (`queue/worker/route.js:78`)
เวลาชั้นในรวมกันสูงสุด ~960 วินาที แต่เพดาน route คือ 800 วินาที · แถม worker ตั้งเวลารอ 900 วินาที ซึ่ง**นานกว่าอายุตัวเอง** = งานหนักถูกตัดกลางคันโดยไม่มีใครรู้สาเหตุ

**6. worker คือคนเดียวที่บันทึกว่า "งานเสร็จ"** (`queue/worker/route.js:78`)
ถ้า worker ตาย/หลุดตอนงานใกล้เสร็จ → **ผลงานที่เจนเสร็จแล้วหายทั้งชิ้น** แล้วระบบเจนใหม่เต็มรอบ (จ่ายเงินซ้ำ)

**7. งานแก้บั๊ก 15 ข้อเมื่อ 25 ก.ค. ถูกย้อนกลับทั้งชุด** (`0397c09`)
ความเสี่ยงเก่าจึงกลับมามีผลบนเว็บจริง — **11 จาก 12 ข้อของรอบตรวจ 25 ก.ค. ยังอยู่ครบ แก้ไปได้ข้อเดียว**

**8. ตัวกรองคำ (safetyFilter) แทนคำแบบตัดคำกลางคัน ปิดไม่ได้** (`ai/safetyFilter.js:14`)
เสี่ยงบิดข้อเท็จจริงในข่าวแบบเงียบๆ ไม่มีสวิตช์ปิด

**9. สาย URL: ดึงเนื้อข่าวล้มแล้วกลืนเงียบ** (`summarizeService.js:585`)
คืนค่าว่า "สำเร็จ" พร้อมส่งข้อความดิบไปเป็นเนื้อข่าว — แพตช์กันเรื่องนี้ลงเฉพาะสาย TEXT (ยังไม่กระทบตอนนี้เพราะสาย URL ปิด **แต่จะระเบิดทันทีถ้าเปิดสาย URL**)

### 🟠 ปานกลาง — ควรตามเก็บ
| เรื่อง | ที่ไหน | อาการ |
|---|---|---|
| DNA วิเคราะห์ล้ม → เสียบ DNA ปลอม "ดราม่าสังคม" เดินต่อเงียบๆ | `summarizeService.js:2071` | ได้การ์ดผิดแนวทั้งใบ ไม่มีป้ายเตือน |
| จ่ายเงินเกลาคำแล้วทิ้งผล 100% | `safeCorrectionService.js:141` | เช็คชนิดข้อมูลผิด ผลที่เกลาถูกโยนทิ้งทุกครั้ง |
| ผลลัพธ์ว่าง/สั้นผิดปกติ ยังนับว่า "สำเร็จ" | `summarizeServiceText.js:1540` | ข่าวเปล่าๆ ผ่านออกไปได้ |
| Gemini ตั้งเวลา timeout ผิดชั้น | `ai/geminiClient.js:91` | 15 วินาทีไม่เคยทำงานจริง สลับไป GPT ไม่ทัน |
| Tavily (ตัวค้นสำรอง) ไม่มี timeout + ตัวสำรองไปไม่ถึงเมื่อ Serper "มี key แต่ล้ม" | `tavilyService.js:37`, `research-search/route.js:25` | ค้างยาว / ไม่มีตัวสำรองจริง |
| บันทึกคลังข่าวแบบยิงแล้วไม่รอผล แต่ตอบว่า `archiveSaved:true` | `auto/process/route.js:219` | บน Vercel อาจไม่ได้บันทึกจริง |
| `persistStore.update()` ไม่ sync ไฟล์สำรอง (ผิดกฎ AGENTS.md ข้อ 10) | `persistStore.js:251` | ไฟล์สำรองให้ข้อมูลเก่า |
| เขียนไฟล์ `data/*.json` ทับตรงๆ ไม่ atomic + อ่านพังแล้วคืนค่าว่างเงียบ | `persistStore.js:96` | ไฟล์พังตอน crash แล้วเงียบ |
| Auto-Pilot โต๊ะข่าว default = **เปิด** เมื่อไม่มีแถว config | `newsDesk/harvester.js:985` | ขัดกับนโยบาย "Auto-Pilot ปิด ห้ามเปิดคืน" |
| ป้ายพร้อมท์โกหก — โชว์ "📦 Library" ทั้งที่ใช้ built-in | `summarizeServiceText.js:1560` | ดู log ไม่ออกว่าหลุดไปใช้ fallback |
| คลังข่าวโตไม่มีเพดาน + โหลดทั้งคลังเข้าหน่วยความจำทุกครั้งที่เซฟข่าว 1 ชิ้น | `auto/process/route.js:53` | ช้าลงเรื่อยๆ (ตอนนี้ 1,403 รายการ) |
| ทางแยกโมเดลไม่เท่ากันตามประตูเข้า | `auto/route.js:13` | เข้าทางคิวได้ gpt-5.6-terra · เข้าทาง /api/auto ได้ของเก่า |
| SSRF — `/api/auto` รับ URL อะไรก็ได้แล้วเซิร์ฟเวอร์ยิงเอง | `scraper/urlExtractor.js:110` | ไม่มี allow-list |

### 🟡 เล็กน้อย / ข่าวดี
- ✅ **บั๊ก 45 วินาทีของ `/api/analyze` แก้แล้ว** (28 ก.ค. ยกเพดานเป็น 90 วินาที ปรับได้ผ่าน env) — เป็นข้อเดียวใน 12 ข้อเดิมที่ปิดได้
- ✅ **ระบบกู้งานค้างครบ 4 ชั้น** ไม่มีงานค้างสถานะ "กำลังทำ" ตลอดกาล
- ✅ กันงานซ้ำมี 3 ชั้น (ลายนิ้วมือเนื้อหา / กันบอทเบิ้ล / กันข่าวคล้ายใน 45 นาที)
- ⚠️ `/api/auto/stream` เลิกใช้แล้วแต่ยังเปิดรับ ไม่มีรหัสผ่าน และ client หลุดแล้วงานยังวิ่งต่อจนจบ (จ่ายเงินฟรี)
- ⚠️ แถบความคืบหน้าบนหน้าเว็บเป็นภาพเคลื่อนไหวตั้งเวลา ไม่ใช่สถานะจริง
- ⚠️ คอมเมนต์ในโค้ดไม่ตรงของจริงหลายจุด (clear บอก 10 นาที จริง 8 · worker บอก 6 นาที จริง 15)

---

## 5. prompt-library: จับคู่ยังไง เสถียรไหม ใช้จริงเสมอไหม

### 5.1 หลักการจับคู่ (อธิบายเป็นภาษาคน)
**การ์ดมาจากไหน — มี 4 ชั้น ไล่ลงมาเรื่อยๆ ไม่มีทางมือเปล่า:**
1. Supabase (คลังจริง ตาราง `store_items`)
2. ไฟล์ในเครื่อง `data/prompt-library.json` (**73 ใบ** ทุกใบมีเนื้อพร้อมท์ครบ)
3. เซอร์วิสอ่านไฟล์เดิมซ้ำอีกรอบ (เข็มขัด 2 ชั้น)
4. การ์ด "Built-in Fallback V12" ที่ฝังในโค้ด

**เลือกยังไง — ไม่ได้ใช้ AI เลือก:**
- ก่อนอื่นระบบถอด "DNA ของข่าว" ออกมาก่อน (แนวไหน อารมณ์อะไร ความขัดแย้งแบบไหน)
- แล้วเอา DNA ไปให้คะแนนการ์ดทุกใบด้วย **สูตรคะแนน 6 มิติ** ใน `promptMatcher.js` — เป็นการคำนวณตรงๆ ผลเหมือนเดิมทุกครั้ง ไม่ใช่ AI เดา
- ตัดเกรดผลจับคู่: **EXACT** (≥60 คะแนน และตรงอย่างน้อย 2 มิติ) · **CLOSE** (≥40) · **BORROWED** (<40 = ยืมการ์ดผิดแนวมาใช้)
- ถ้าผลออกมาเป็น BORROWED เท่านั้น ถึงจะเรียก Gemini มาช่วยเลือก 1 ใบจาก 5 ใบคะแนนสูงสุด
- `PROMPT_VARIETY_BAND` (บนเว็บจริงตั้งไว้ 5) = ถ้าการ์ดหลายใบคะแนนใกล้แชมป์ ให้สุ่มหยิบเพื่อไม่ให้ข่าวซ้ำสำนวน — มีพื้นกันตกที่ 45 คะแนน

**ได้การ์ดแล้วทำอะไรต่อ — ใช้เกือบทุกช่องของการ์ด** ระบบเอาไปประกอบเป็นหัวคำสั่งเขียนข่าว: หมวด/คะแนนไวรัล → สูตรโครงเรื่อง (dnaTemplate) → เส้นอารมณ์เปิด-กลาง-ปิด → ตัวอย่างประโยคเปิด → สไตล์ปิดท้าย → **เนื้อพร้อมท์ (promptText) เป็นแกนกฎการเขียน** → ข้อห้าม (doNot)

### 5.2 เสถียรแค่ไหน — **กลาง**
| ด้าน | ระดับ | เหตุผล |
|---|---|---|
| "ไม่ล้ม มีการ์ดใช้เสมอ" | 🟢 **สูง** | fallback 4 ชั้น ทุกชั้นมี catch ไม่มีทางที่ระบบจะหยุดเพราะหาการ์ดไม่ได้ |
| "ได้การ์ด**ถูกตัว** และตรวจสอบได้" | 🟠 **กลางค่อนต่ำ** | มีหลายทางที่คุณภาพเสื่อมแบบ**เงียบ** ไม่มีป้ายเตือน |

**จุดเปราะเรียงตามความร้ายแรง:**
1. 🔴 **DNA วิเคราะห์ล้ม → ระบบเสียบ DNA ปลอมว่า "ดราม่าสังคม" แล้วเดินต่อเนียนๆ** (`summarizeService.js:836`) — ผลคือให้คะแนนการ์ดทั้งคลังโดยเทียบกับข่าวปลอม ได้การ์ดผิดแนวทั้งใบ **และไม่มีป้ายบอกในผลลัพธ์เลย** (ท่อปกติรอดเพราะใช้ DNA จากขั้น breakdown แทน — เสี่ยงเฉพาะงานที่ breakdown ไม่ได้ให้ DNA มา)
2. 🔴 **`.catch(()=>null)` ครอบขั้นเลือกการ์ดทั้งก้อน** (`autoFlowService.js:324`) — พังเพราะอะไรก็ตาม (คลังโหลดไม่ได้ / สูตรคะแนนพัง / AI ล้ม) log จะบอกเหมือนกันหมดว่า "ไม่มี match ใน library — ใช้ Built-in V12 แทน" → **วันที่ระบบพังจริง จะดูเหมือนแค่คลังการ์ดไม่มีของ**
3. 🟠 ถ้าไฟล์ในเครื่องพัง + Supabase ล่มพร้อมกัน → ข่าวทุกชิ้นเจนด้วยการ์ด V12 ใบเดียว (สำนวนเหมือนกันหมด)
4. 🟠 ไฟล์ `data/prompt-library.json` **หยุดอัปเดตตั้งแต่ 1 ก.ค.** เพราะ `persistStore.update()` เขียนแค่ Supabase ไม่ sync ลงไฟล์ (ผิดกฎ AGENTS.md ข้อ 10) — ตัวเลขในไฟล์จึงไม่ใช่หลักฐานว่าเลิกใช้การ์ด

### 5.3 หยิบใช้จริงเสมอไหม — **"เกือบเสมอ" ไม่ใช่ 100%**

✅ **หลักฐานฝั่งดี (แข็งแรงมาก):**
- `data/job_queue.json` (แก้ไขล่าสุดเช้าวันนี้ 06:45) — **3 งานล่าสุดเมื่อเช้านี้ 06:33–06:42 ใช้การ์ดจริงทุกงาน** บันทึก promptId คนละใบต่อเวอร์ชัน, `promptSource=library`, `matchType=EXACT` **ครบทั้ง 55 รายการในไฟล์**
- ค้นคำว่า `fallback_builtin` ในไฟล์ผลงานทุกไฟล์ (job_queue, generation-logs, latest-versions, ready-content) = **เจอ 0 ครั้ง**
- สถิติสะสม: การ์ดถูกใช้ไปแล้ว **59 จาก 73 ใบ รวม 1,727 ครั้ง** (แชมป์ใบเดียวถูกใช้ 210 ครั้ง)
- การ์ดถูก**ฝังเข้าคำสั่งเขียนจริง** ไม่ใช่แค่แปะป้ายไว้เฉยๆ

⚠️ **แต่โค้ดมีทางหลุดไปใช้พร้อมท์ built-in ได้ 4 ทาง:**
| ทาง | เกิดเมื่อไหร่ | มีป้ายบอกไหม |
|---|---|---|
| 1. คลังการ์ดว่างเปล่า | Supabase ว่าง/ล่ม + ไฟล์อ่านไม่ได้ | ✅ ป้าย `fallback` ตรงไปตรงมา |
| 2. ขั้นเลือกการ์ดพัง | STAGE 2 โยน error | ✅ ป้าย `fallback` |
| 3. **เส้นคิว (เส้นจริง!) ไม่ได้การ์ด** | `getTopPrompts` คืนค่าว่าง | 🔴 **ป้ายหลอก — บันทึกว่า `library` ทั้งที่ใช้ built-in** |
| 4. มุมแรกจับคู่หลวม | ต้องเปิด `REF_WEIGHT_BY_MATCH=1` | (ปิดอยู่ในเครื่องนี้) |

🔴 **ทางที่ 3 คือปัญหาที่ต้องรู้:** ถ้าเส้นคิวหาการ์ดไม่ได้ ระบบจะใช้ "Built-in Fallback V12" แต่บันทึกลง log ว่า `promptSource='library'` ชื่อ "Built-in Fallback V12" `matchType='MATCHED'` (`summarizeServiceText.js:737`) — **แปลว่าถ้าวันไหนระบบหลุดไปใช้ built-in คุณจะดู log ไม่ออก** เพราะมันแปะป้ายว่าใช้หอสมุด (ตรงกับความเสี่ยงข้อ 20 ในรอบตรวจ 25 ก.ค. ที่ยังไม่ได้แก้)

**สรุปคำตอบเจ้าของ:** ตอนนี้**หยิบใช้จริง** — หลักฐานงานจริงทุกชิ้นเท่าที่ตรวจได้ใช้การ์ดจากหอสมุด 100% ไม่มีเคสหลุดเลยแม้แต่ครั้งเดียว แต่ระบบ**ไม่การันตี**ว่าจะเป็นแบบนี้เสมอ และถ้าหลุดขึ้นมาจริง เส้นคิวจะไม่บอกคุณ

### 5.4 ของที่ยืนยันไม่ได้ในรอบนี้ (เพราะกติกาห้ามยิง DB/เน็ต)
- จำนวนการ์ดจริงบน Supabase ตอนนี้ (ไฟล์ในเครื่องบอก 73 ใบ / memory 30 มิ.ย. บอก 69 / คอมเมนต์ในโค้ดอ้าง 201 ใบ — **ตัวเลขไม่ตรงกัน 3 แหล่ง ควรเช็ค**)
- ค่า env จริงบน Vercel (`PROMPT_VARIETY_BAND`, `REF_WEIGHT_BY_MATCH`)
- มีการ์ด 1 ใบ (`prompt_1ebbcf93`) มีข้อมูลชั่วคราวตอนรันรั่วติดลงคลังถาวร (`_matchScore`, `_isBorrowed`) — ความสะอาดข้อมูลเสีย ควรล้าง

---

## 6. 🔍 คดีย่อหน้า 3: ทำไมข่าวชอบจบด้วย "ข้อคิด/สรุปคำแปลกๆ"

### 6.1 คำตัดสิน
> ## **ไม่ใช่โมเดลเขียนไม่เก่ง และไม่ใช่การจับคู่ล้มเหลว**
> ## **ตัวการคือ "คำสั่งของระบบเราเอง" ที่สั่งซ้อนกัน 3 ชั้น แล้วดันขัดกันเองที่ย่อหน้าเดียวกัน**

**ความมั่นใจ: สูง** — เพราะเจอเคสจริง 2 ใน 4 ตัวอย่างที่คุณยกมา บันทึกอยู่ในไฟล์งานพร้อมชื่อการ์ดที่ใช้:
| ข่าว | การ์ดที่ใช้ | ตำแหน่งวลี | สาย |
|---|---|---|---|
| "มุมที่น่าจำอยู่ที่เหตุผลของนายพิชัยพร" | `prompt_1aecfb11` | ย่อหน้า 3 จาก 3 | Enhanced |
| "เรื่องที่น่าจำของแม่ทัพกุ้งไม่ใช่แค่เส้นทางก่อนก้าว" | `prompt_1e6a0b63` | ย่อหน้า 3 จาก 3 | Enhanced |

(ทั้งคู่จับคู่การ์ดสำเร็จปกติ ไม่ใช่ fallback — **พิสูจน์ว่าไม่ใช่ปัญหาการจับคู่**)

### 6.2 ห่วงโซ่หลักฐาน — คำสั่งที่ผลิตประโยคพวกนี้
**ชั้นที่ 1 — การ์ดในคลังสั่งตรงตัว (ตัวหลัก)**
- **59 จาก 73 ใบ** (81%) สั่งว่า *"ย่อหน้า 3 ปิดสั้น 1-2 ประโยค ด้วยบทเรียนหรือคำอวยพรที่มีน้ำหนัก"* — นับจากไฟล์จริง
- **14 ใบ** สอนสำนวน *"ไม่ใช่แค่... แต่ยัง..."* ซ้ำถึง 3 จุดในใบเดียว (ทั้งในเนื้อพร้อมท์ + สูตรภาษา + ตัวอย่างประโยคเปิด)
- **7 ใบ** สอนกรอบ *"น่าจดจำ / ให้จำได้ว่า"* — เช่น `prompt_5f6ef1e8` เขียนไว้ว่า *"จุดน่าจดจำอยู่ที่เจตนาดีของคนให้"* ซึ่งเป็นโครงประโยคเดียวกับ "มุมที่น่าจำอยู่ที่เหตุผลของนายพิชัยพร" เป๊ะ

**ชั้นที่ 2 — กฎระบบบังคับทุกข่าว ปิดไม่ได้** (`summarizeServiceText.js:1123-1124`)
> ★ กฎเหล็ก: ทุกข่าวต้องมีมุมที่ดีอย่างน้อย 1 จุด — **บทเรียน** / ความหวัง / คนที่ทำดี
> ★ เป้าหมาย: อ่านจบแล้ว "อิ่มใจ / ซาบซึ้ง / **ได้แง่คิด**"

**ชั้นที่ 3 — VIRAL STYLE PACK แนบทุกงาน ไม่มีสวิตช์ปิด** (`viralFewshot.js:16,20`)
> 5. **จบด้วยประโยคสัจธรรมสั้นๆ** ที่คนอยากก๊อปไปโพสต์ต่อ
> 1. ...ความย้อนแย้ง (**"ไม่ใช่แค่สวย แต่ยังขยัน..."**)

### 6.3 กลไกที่ทำให้ไปโผล่ "ย่อหน้า 3" โดยเฉพาะ
นี่คือหัวใจของคดี — **ระบบสั่งขัดกันเองที่ย่อหน้าเดียวกัน:**
```
ข่าวสั้น = โครง 3 ย่อหน้า  →  "ย่อหน้า 3" ก็คือ "ย่อหน้าสุดท้าย" ตัวเดียวกัน
        │
        ├── การ์ด 59/73 ใบสั่ง : "ย่อหน้า 3 ปิดด้วยบทเรียน/คำอวยพร"     ← สั่งให้มี
        ├── กฎเหล็กสั่ง        : "ทุกข่าวต้องมีบทเรียน อ่านจบต้องได้แง่คิด"  ← สั่งให้มี
        ├── STYLE PACK สั่ง    : "จบด้วยประโยคสัจธรรม"                  ← สั่งให้มี
        └── กฎเขียนสั่ง (1333,1340): "ย่อหน้าสุดท้ายบรรยายเรียบๆ ไม่ตีความ
                                       ไม่สรุปข้อคิดชีวิต"                ← สั่งห้าม!
```
โมเดลได้รับคำสั่ง **"ต้องมีข้อคิด" กับ "ห้ามมีข้อคิด" พร้อมกันในพร้อมท์เดียว** วิธีที่มันประนีประนอมคือ **แต่งเป็นสำนวนกึ่งวิเคราะห์** ให้ดูไม่เหมือนคำสอนโต้งๆ — ผลออกมาเป็น "มุมที่น่าจำอยู่ที่..." / "สิ่งที่ทำให้คนหยุดดูไม่ใช่แค่..." / "ทิ้งท้ายด้วยหลักคิด..." ซึ่งคือ**คำแปลกๆ ที่คุณเห็น**

**หลักฐานปิดคดี:** ค้นคำเป๊ะๆ ("มุมที่น่าจำ", "สิ่งที่ทำให้คนหยุดดู", "ทิ้งท้ายด้วยหลักคิด") ทั้งโปรเจกต์ = **เจอ 0 ครั้ง** แปลว่าไม่มีใครเขียนวลีพวกนี้ไว้ให้ลอก — โมเดล**แต่งเองเพื่อสนองคำสั่งที่ขัดกัน** ⇒ **การไล่แบนคำจะแก้ไม่ตรงจุด ต้องแก้ที่คำสั่งขัดกัน**

### 6.4 ทำไมแก้แล้วไม่หาย / ทำไมมันมาจากทุกทาง
- **ต้นทางการ์ดเป็นแบบนี้มาแต่เกิด** — การ์ดทั้ง 73 ใบสกัดจากโพสต์ไวรัลจริง ซึ่ง 7-10 โพสต์ในคลังจบด้วยข้อคิดอยู่แล้ว (เช่น *"ไม่ใช่แค่สวย แต่หาเงินเก่ง ดูแลครอบครัวได้"*) โรงงานสกัดการ์ดดูดแพทเทิร์นนี้เข้าคลัง **โดยไม่มีตัวกรอง**
- **มีคนเคยพยายามแก้แล้วแต่แก้ผิดสาย** — `promptStore.js` (สาย URL) บรรทัด 186 ถูกเติมคำว่า *"ไม่สรุปข้อคิดชีวิต"* แล้ว แต่ `promptStoreText.js` (**สายที่ใช้จริง**) บรรทัดเดียวกัน **ไม่ได้รับแพตช์**
- **ขั้นขัดเกลา (correction) ไม่จับสำนวนนี้** — เคสแม่ทัพกุ้งผ่าน correction แล้ว (`_correctionApplied=true`) วลียังอยู่ เพราะตัวตรวจคุณภาพจับแค่ข้อความเพี้ยน ไม่จับสำนวนซ้ำซาก
- **Classic กับ Enhanced ใช้คำสั่งชุดเดียวกัน** ต่างแค่มีข้อมูลค้นเพิ่ม — จึงมีอาการเหมือนกันทั้งคู่
- **สำนวนติดมาตั้งแต่ขั้น extract ด้วย** — พบวลี "น่าจดจำ + ไม่ได้...แค่...แต่" ในคลังข่าวต้นน้ำ 3 รายการ (ก่อนถึงขั้นเขียนด้วยซ้ำ)

### 6.5 ทางแก้ (เรียงจากเบา→แรง — เสนอเฉยๆ ยังไม่แตะโค้ดตามกฎล็อกท่อข่าว)
| # | ทำอะไร | แรงแค่ไหน | ผลที่คาด |
|---|---|---|---|
| 1 | ใส่สวิตช์ปิดข้อ 5 ของ VIRAL_STYLE_PACK ("จบด้วยสัจธรรม") — `viralFewshot.js:20` | เบา แก้ 1 บรรทัด | ตัดแรงกดดันชั้นที่ 3 ออก ทดสอบผลได้ทันที |
| 2 | แก้กฎเหล็ก `:1123` ให้ "บทเรียน" **ห้ามอยู่ย่อหน้าสุดท้าย** (ให้อยู่ในเนื้อเรื่องแทน) | เบา | เลิกดันข้อคิดไปกองที่ย่อหน้าปิด |
| 3 | เติมท่อน "ไม่สรุปข้อคิดชีวิต" ให้ `promptStoreText.js:186` ให้ตรงกับสาย URL | เบา | ปิดช่องที่แพตช์เก่าตกสาย |
| 4 | เพิ่มกฎใน correction ให้จับสำนวน "มุม/เรื่องที่น่าจำ...", "ไม่ใช่แค่...อย่างเดียว", "ทิ้งท้ายด้วย..." ที่**ย่อหน้าปิด** | กลาง | เป็นตาข่ายดักท้ายทาง (จับเป็นแพทเทิร์นประโยค อย่าจับคำเดี่ยวเพราะ "หยุดดู" มีใช้ปกติ) |
| 5 | ล้างการ์ด 59 ใบที่สั่ง "ย่อหน้า 3 = บทเรียน" ให้เหลือ "ปิดด้วยข้อเท็จจริงที่มีน้ำหนัก" | **แรง** | แก้ต้นตอชั้นที่ 1 แต่กระทบคลังทั้งระบบ ควรทำหลังทดสอบ 1-3 แล้ว |
| 6 | ใส่ตัวกรองในโรงงานสกัดการ์ด ไม่ให้ดูดแพทเทิร์น "จบด้วยข้อคิด" เข้าคลังอีก | กลาง | กันปัญหาย้อนกลับมาในอนาคต |

**แนะนำ:** ทำข้อ 1-3 ก่อน (แก้รวมไม่เกิน 5 บรรทัด) แล้วเจนข่าวทดสอบ 3-5 ชิ้นดูว่าย่อหน้าปิดเปลี่ยนไหม — ถ้ายังเหลืออาการค่อยขยับข้อ 4-5

---

## 7. ภาคผนวก
### 7.1 สถานะความเสี่ยงรอบตรวจ 25 ก.ค. — **แก้ได้ 1 จาก 21 ข้อ**
สาเหตุ: **งานแก้ 15 บั๊กชุดนั้น (commit `0397c09`) ถูกย้อนกลับทั้งชุด** ความเสี่ยงจึงกลับมาอยู่บนเว็บจริง

🔴 ยังอยู่: ตัวกรองคำแทนคำ (1) · Gemini timeout ผิดชั้น (2) · งบเวลาเกินเพดาน (3) · queue/clear ไม่มีรหัสผ่าน (4) · สองสมองขัดกัน (5) · error ไม่มี errorType (6) · ไม้สอง gpt-4o ตาย (10) · ด่านสิทธิ์คิวผ่านฟรี (15)
🟠 ยังอยู่: Auto-Pilot default เปิด (8) · ผลว่างนับสำเร็จ (11) · Tavily ไม่มี timeout (12) · เซฟคลังยิงแล้วไม่รอ (13) · extract ไม่มีด่าน TEXT_ONLY (16) · จ่ายเงินเกลาคำแล้วทิ้งผล (17) · ป้ายพร้อมท์โกหก (20)
🟡 ยังอยู่: ท่อสำรองโค้ดตาย (9) · ไม่มี cron (14) · แถบสถานะหลอก (18) · prompt-library.json ไม่ trace (21)
✅ **แก้แล้วข้อเดียว: `/api/analyze` ตัน 45 วินาที** (28 ก.ค. → 90 วินาที)

### 7.2 ระดับความเชื่อถือของรายงาน
| ส่วน | ตรวจแค่ไหน |
|---|---|
| แผนที่ระบบ (ข้อ 2) | ✅ อ่านโค้ดจริง 3 มุมอิสระ ผลตรงกัน |
| คดีย่อหน้า 3 (ข้อ 6) | ✅ **ตรวจซ้ำเองแล้ว** — เปิดโค้ด 4 จุดชี้ขาด + นับการ์ดจากไฟล์เอง |
| prompt-library (ข้อ 5) | ✅ หลักฐานข้อมูลจริง (job_queue + ไฟล์คลัง) |
| บั๊ก 61 ข้อ (ข้อ 4) | ⚠️ **ยังไม่ผ่านฝ่ายค้านตรวจซ้ำ** — ควรตรวจก่อนลงมือแก้ |

### 7.3 ยืนยันไม่ได้ในรอบนี้ (กติกาห้ามยิง API)
- จำนวนการ์ดจริงบน Supabase — **3 แหล่งบอกไม่ตรงกัน: ไฟล์ 73 / memory 69 / คอมเมนต์โค้ด 201**
- ค่า env จริงบน Vercel (`PROMPT_VARIETY_BAND`, `REF_WEIGHT_BY_MATCH`)
- ตัวอย่างข่าวพังอีก 2 อัน (ชุดไทย, ป๋อ) — หลุดจากคิวแล้ว เหลือบน Supabase `generation_logs`

### 7.4 ช่องโหว่การเก็บหลักฐาน (ทำให้สืบสาเหตุยาก)
1. **ไม่มีการบันทึกว่าใช้โมเดลอะไรเขียน** เลยสักชั้น → ข่าวพังแล้วชี้ตัวโมเดลไม่ได้
2. **Generation Log ตัด `promptId` ต่อเวอร์ชันทิ้ง** (`generationLogger.js:101`) → พังที่มุมรองจะชี้การ์ดผิดใบ
3. **`job_queue` เก็บแค่ ~12 งานล่าสุด** → หลักฐานระดับเวอร์ชันอายุสั้นมาก

---
*หลักฐานดิบ: journal 2 ไฟล์ (ดูพาธใน memory `news-audit-handoff-aug1.md`) — ทุกข้อในรายงานนี้อ้างไฟล์:บรรทัดจริงในโค้ด*
