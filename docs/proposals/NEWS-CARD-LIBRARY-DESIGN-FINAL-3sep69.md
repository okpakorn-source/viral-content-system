# แบบสุดท้าย: จัดระเบียบคลังการ์ดให้ข่าวสวยสุด (3 ก.ย. 69) — ฉบับลงมือจริง

> ผู้สังเคราะห์: สถาปนิก (เฟเบิ้ล-สุด) · สถานะ: แบบสุดท้ายรอเจ้าของเคาะ — ยังไม่แตะโค้ด/ข้อมูล/DB
> ฐาน: **quality-first** (`card-design-quality-first-3sep69.md`) — มติกรรมการ 3/3 (42 · 43 · 41) + ยืมกระดูกจาก safe-measurable + อวัยวะจาก coverage-first (ระบุที่มารายข้อใน §2)
> หลักยึด: ข่าวสวย = เนื้อการ์ดที่ถูกหยิบบ่อยสอนไม่ขัดกฎกลาง + ผู้เลือกจริง (luna 93% อ่านชื่อ+600 ตัวแรก) เห็นของดี + หมวดที่เพจแพ้มีทางเข้าครบ 3 ชั้น · ทุกก้อนมีสวิตช์ปิดคืน + A/B กรรมการปิดชื่อก่อนแตะ production

## 0) ข้อเท็จจริงใหม่จากการตรวจรอบตัดสิน (ผมรัน D0 ของแบบ 3 แล้ว — เปลี่ยนดีไซน์ 5 จุด)

| # | พบอะไร (ตรวจเอง 3 ก.ย.) | ผลต่อแบบ |
|---|---|---|
| 1 | สนามเทส :3991 **ชี้ Supabase จริง** — `.env.local` มี `SUPABASE_SERVICE_KEY`/`DATABASE_URL` และ `set-arm.mjs`/`run-arms.ps1` รันเซิร์ฟจาก worktree | สมมติฐาน "file-store สลับไฟล์ต่อแขน" ของแบบ 1 ใช้ไม่ได้ → ต้องสร้าง **ห้องแล็บ overlay** (F2) จึงจะ "DB จริงไม่ถูกแตะจนกว่า A/B ผ่าน" ได้จริง |
| 2 | `SUPABASE_DISABLED=1` มีอยู่แล้ว (supabase.js:115) แต่ครูตัวอย่างอ่านจาก**ตาราง `viral_examples` ตรง** (vf:1052 `sb.from('viral_examples')`) ไม่ผ่าน store | ห้ามใช้โหมดปิด Supabase ทำ A/B — จะตัดครูทุกแขน แขนควบคุมไม่เหมือน prod · ครูแล็บของหมวดใหม่ต้องมาทาง `VIRAL_EXAMPLES_LAB_FILE` ใน vf (เฟส 2) |
| 3 | ctaStyle เรนเดอร์แบบ `if (smartPrompt.ctaStyle)` เท่านั้น (Svc:1235-1237 · Text:1618-1620) — ค่าว่าง = บรรทัด "เป้าหมายตอนจบ:" หายทั้งบรรทัด | แก้จุดอ่อนที่กรรมการ 3 ชี้: กวาด ctaStyle 201 ใบเป็น **ค่าว่าง** ไม่ใช่ข้อความ callback — การ์ดเลิกพูดเรื่องปิดเรื่องจริงๆ เหลือ VR-006 เสียงเดียว (การ์ดผอมคงเส้นคงวา) |
| 4 | `getAll` ฝั่ง Supabase **sync ทับ `data/prompt-library.json` ทุกครั้งที่อ่านสำเร็จ** (persistStore:230) | overlay แล็บต้อง short-circuit ที่หัว createStore ก่อนถึง Supabase — ห้าม hook หลัง fetch ไม่งั้น mirror ถูกทับด้วยคลังแขนทดลอง |
| 5 | ฐานจริงล่าสุด (ab-aggregate-r3 แขน P2len ที่เปิดเป็น default): รวม **36.90/50** · opening 7.70 · fidelity **6.73** · readability 7.80 · viral 7.17 · tightness **7.50** | เกณฑ์สัมบูรณ์ของแบบ 1 (38.4 / tightness≥7.2) ตายแล้ว — tightness ผ่านฟรีตั้งแต่ยังไม่ทำ · **ทุกเกณฑ์เปลี่ยนเป็น paired delta เทียบแขน A ที่รันรอบเดียวกัน** · คอขวดจริงคือ fidelity |

## 1) คำตอบต่อจุดอ่อนที่กรรมการชี้ (ครบทุกข้อ)

| กรรมการชี้ | แบบสุดท้ายตอบ |
|---|---|
| ก1/ก3: แขน B มัดรวม ~8 การเปลี่ยน — ชนะชี้ไม่ได้ว่าชิ้นไหนพา แพ้ถอยทั้งก้อน | ยอมรับการมัดเมื่อ**ชนะ** (ทุกชิ้นถอยแยกได้ด้วยสวิตช์/restore รายชุด — ไม่ต้องรู้ว่าชิ้นไหนพาก็เปิดได้ทั้งชุด) · เมื่อ**แพ้**มีบันไดแตกก้อน B1→B2→B3 ลงทะเบียนล่วงหน้า (§6.4 — วิธีของแบบ 3) ไม่เหมาว่าแนวคิดผิด |
| ก2/ก3: เกณฑ์สัมบูรณ์ 38.4/7.2 เทียบฐาน stale (จริง 6.73/7.50) | เกณฑ์ทั้งหมดเป็น paired delta ภายในข่าวเดียวกัน เทียบแขน A รอบเดียวกัน ตั้งก่อนรัน ก้ำกึ่ง=ไม่ผ่าน (§6.3) |
| ก3: ctaStyle ใหม่แบบ callback = ยัดคำสั่งปิดเรื่องกลับเข้าการ์ด ขัดหลักการ์ดผอมเอง | ctaStyle 201 ใบ → **ค่าว่าง** (บรรทัดหายจากใบสั่ง — ตรวจ renderer แล้ว §0.3) · ความรู้ callback อยู่ที่ VR-006 ที่เดียว |
| ก1/ก3: ไม่มี hard-gate คดีความ ทั้งที่เปิดหมวดเดียวกัน | ยกของ coverage มาทั้งชุด: doNot 4 เรื่อง + เกณฑ์ A/B "ฟันธงแทนศาล = 0 ฉบับ" (คนอ่านเอง) + คนตรวจ 10 โพสต์จริงแรก + alias ปิดคืนได้ด้วยสวิตช์เดียว (§2 F10, §6.3) |
| ก1/ก2: หมวดใหม่ไร้ครู — A/B หมวดใหม่จะแพ้ทั้งที่การ์ดดี | Gate 2 รอครูพร้อม: ครูตรงหมวด ~34 ใบคัดเข้ม (เกณฑ์ coverage) ป้อนแล็บผ่าน `VIRAL_EXAMPLES_LAB_FILE` โดยไม่ import ตารางจริงก่อนผล (§2 F12) — ตาราง `viral_examples` ห้าม import ก่อนผ่าน เพราะโหมด wide ดึงทุกแถวเข้าโผทันที (vf:1056-1058) |
| ก2: อย่าเขียนชื่อ 25 ใบลง store ก่อนผล A/B (บทเรียนจาก coverage) | ทุกการแก้ data อยู่ในไฟล์คลังแขนแล็บจนกว่า Gate 1 ผ่าน + เจ้าของเคาะ — production ไม่ถูกแตะแม้ไบต์เดียวก่อน go (§5) |
| ก2 nit: 4,672 = 64.6% ของ 7,237 (60% คู่ 7,752) | ใช้เลขตรวจแล้ว: 8d557a0a ไลก์กลาง 4,672 = 60% ของฐานเพจ 7,752 |

## 2) รายการเปลี่ยนทีละข้อ (F1–F14 · ที่มาในวงเล็บ)

| # | ทำอะไร | ไฟล์/ข้อมูล | สวิตช์ · ย้อนกลับ |
|---|---|---|---|
| F1 (แบบ1 C1) | แขนทดลอง `ENDING_MODE=plain` ตัดเสียงปิดเรื่องที่ 3 (Style Pack ข้อ 5) — กลไกมีแล้ว vf:53-75/promptModes ไม่แก้โค้ด · เปิด prod เฉพาะแขน C ชนะ B ≥ +0.5 | `.env.local` เท่านั้น | ลบ env → `truth` (default) |
| F2 (ใหม่ — จาก D0 §0.1) | **ห้องแล็บ overlay**: ใน `createStore(name)` ถ้า `CARD_LIBRARY_LAB='1'` และ name='prompt-library' → คืน store อ่านจากไฟล์ `CARD_LIBRARY_OVERLAY_FILE` (คลังทั้งชุดของแขน) แบบ short-circuit ก่อนถึง Supabase + **เขียนทุก op เป็น no-op พร้อม warn** (กัน usage/track แล็บปนเปื้อน store จริง — ดีกว่าที่ผ่านมา) · การ์ดเห็นตามไฟล์แขน = พัก/ชื่อใหม่/ใบใหม่มีผลในแล็บโดย DB ไม่เปลี่ยน · กันพลาด: ถ้า `VERCEL_ENV`/`VERCEL` ตั้งอยู่ → ไม่ทำงาน + console.error | `src/lib/persistStore.js` (ไม่ล็อก ไม่มี diff ค้าง) + เทส snapshot "env ว่าง = พฤติกรรมเดิม" | `CARD_LIBRARY_LAB` ไม่ตั้ง = โค้ดเดิมทุกเส้นทาง · revert คอมมิตเดียว |
| F3 (แบบ1 C2 + แก้ตามกรรมการ 3) | กวาดทั้งคลัง: ctaStyle 201 ใบ (ยืนยัน distinct=1 ไบต์เดียวกัน) → **''** + ลบวลี "จบที่ใจความหรืออวยพร(สั้นๆ)" ใน promptText 172 ใบ / structure / emotionalArc.close / dnaTemplate + remap viralScore 19 ใบที่มีโพสต์จริง ≥3 (แบบ1 C11 — mix เลือกด้วย viralScore ล้วน Svc:1897-1902, Text:2489-2494) | store `prompt-library` ผ่านสคริปต์ F13 (PUT whitelist ไม่มี status แต่มี ctaStyle — ก็ไม่ใช้ PUT ใช้ createStore ตรง) | restore จาก backup รายใบ/ทั้งชุด |
| F4 (แบบ1 C3) | ผ่าตัดหนัก 11 ใบหมุนเวียนจริง/พิสูจน์แย่: `da627c89 8d557a0a af1dd10c d1f2c079 e1843d2c df970616 9034e322 30782875 2565b093` + `1e6a0b63 98f3b48d` — ลบแพตเทิร์นชน VR-003/VR-006 + ลบวลีสำเร็จรูป + เขียน 600 ตัวแรกใหม่เป็น scenario+เดิมพัน (เกณฑ์ตรวจร่าง: ผู้ตรวจไขว้อ่าน "ชื่อ+600 ตัวแรก" รายใบ) | data (ร่าง `card-surgery-v2.json` ก่อน) | restore รายใบ |
| F5 (แบบ1 C4) | เปลี่ยนชื่อ+ท่อนเปิด 5 ใบธีมชนะที่ไม่ถูกหยิบ: `8cac45b3 36bfb0aa 7d4f380f f7475b96 50e06bf4` | data | restore |
| F6 (แบบ1 C5 คงชุด 27) | พัก 27 ใบ `status:'archived'` (ไม่ลบ — เคสเก่าอ้าง promptId ได้เสมอ): `42cf8151 b04c0723 272ea6bf 563ef970 88048dbb 710e44d5 13de11d1 3b133ecf 7e67eb4e 90399d4a a6ad79fa eff0c8d1 f269357c f8dc8e01 79b874a9 7f83a5f1 81bfe6e5 da42230d 07e6b339 1b322526 1788bbc6 5cfac41f e48e897f 5474588f 81a09e5b` + `3af7e755` (กรรมการ 31.78 ต่ำสุดเสถียร) + `9ef84649` (fidelity 5.0) · คงเป็น active ตามคำเตือนผู้ตรวจ: `6ad1ced5 5cb20ebd 8937b84e` · ก่อนพักกู้ doNot/hooks ไม่ซ้ำเข้าใบคง (dup-groups-t0.75.json) | data (ผ่าน F13) | ลบ field status รายใบ = เดิม 100% · `CARD_LIBRARY_V2=0` ปลดทั้งชุดทันใจ |
| F7 (แบบ1 C6 = 8 บรรทัดของแบบ 2/3) | ตัวกรองสถานะ 6 ทางเข้า: helper ใหม่ `src/lib/ai/libraryStatus.js` (`isCardSelectable`) + เติมเงื่อนไขที่บรรทัด filter เดิม Text:1103, 2795, 2816 (+mix 2489-2494) · Svc:786, 2198, 2220 (+mix 1897-1902) — **ไฟล์ล็อก 2 ตัว: จำเป็นจริง** เพราะทุกทางเข้า fallback อ่าน store ตรง ไม่มีชั้นกลางอื่น (persistStore ห้ามกรอง — สถิติ/UI/track ตาบอด) · GET ของ route ไม่กรอง | `summarizeServiceText.js` + `summarizeService.js` (ล็อก — ขอ NEWS-LOCK-APPROVED + **รอ workflow คู่ขนาน merge ก่อน**) + `libraryStatus.js` ใหม่ | `CARD_LIBRARY_V2` (default '1' — no-op จนกว่า data มี flag) · `=0` เห็นทุกใบไบต์เดิม |
| F8 (แบบ1 C7 + ร่างปรับของแบบ 2/3 §3.1) | การ์ดใหม่ 3 ใบแบบผอม (คดีความ · ศาสนา-งานบุญ · กีฬาแข่งขัน): บรรทัดแรก "ใช้เมื่อ:" ให้ luna · ไม่มีคำสั่งปิดเรื่องในการ์ด (ctaStyle '' ตามคลัง) · กติกาเรียกคน VR-002 · ตัวเลขนำผูกราคาที่จ่าย VR-003 · จุดหักที่ 2 VR-004 · ประโยคปลอบผู้รับ VR-008 (คดี/ศาสนา) · doNot คดีความครบ 4 เรื่อง · viralScore 83 · **ตัดผู้สูงอายุเรียน** (0 ข่าวแท้/1,403 — raw-samples ยืนยันซ้ำ ทั้ง 3 ตัวอย่างไม่ใช่ผู้สูงอายุเรียน) เงื่อนไขฟื้น ≥5 ข่าวแท้/30 วัน | ไฟล์ร่าง `card-library-new-cards-v2-3sep69.json` → แล็บก่อน (อยู่ในไฟล์แขน) → import จริงเป็น `proposed` หลัง Gate 2 ผ่าน แล้ว flip active รายหมวด | ตั้ง archived รายใบ (id จดตอน import) |
| F9 (แบบ1 C8+C9 ใต้สวิตช์ของแบบ 3) | semanticClusters: CATEGORY_MAP เพิ่ม ศาสนา/งานบุญ/ทำบุญ→ศาสนา-งานบุญ · คดีความ/คดี/กฎหมาย/ฟ้องร้อง→คดีความ · ย้าย 'อาชญากรรม'+'ข่าวอาชญากรรม'→คดีความ · 'กีฬา'→กีฬาแข่งขัน · 'ฮีโร่ชาวบ้าน'/'จิตอาสา'→ช่วยเหลือกัน (ยุบหมวด — โอน `bdcc74bd` `eaf9fa45` เป็นช่วยเหลือกันใน data) · **CONFLICT_CLUSTERS ยังไม่แตะ** — เพิ่มเฉพาะเมื่อพรีเช็ค F11 ไม่ถึง 80% (เข็มของแบบ 2/3) | `src/lib/ai/semanticClusters.js` (สะอาด ไม่ล็อก) + snapshot test | `NEW_CARD_CATS_V1` (default '0' จน Gate 2 ผ่าน) · `=0`/unset = แมปเดิมไบต์ต่อไบต์ |
| F10 (แบบ1 C10 + แผน machine-readable ของแบบ 2) | แก้ชื่อ 25 ใบ prefix ไม่ตรง category (+2 ฮีโร่หลังโอน = 27): `5474588f 23e9e480 cd1e25df 1e6a0b63 4109f5fc 552caf5d b971bf32 030d3aec e01ebf47 86d2b87c 2d357456 64923962 8778eb2a c1dbe2a9 e55016a8 823c17be 7d37537d 81a09e5b a9602fda 4bf20cf8 9ef84649 7f83a5f1 6ffec55f 5cfac41f 27502b2d` + `bdcc74bd eaf9fa45` — กติกา: prefix := `[<category จริง>-<อารมณ์เดิม>]` ไม่แตะส่วน scenario | data (รายการอยู่ `pause-rename-plan-3sep69.json` — ปรับ pause 29→27 ตาม F6 แล้ว regen เป็น `-final.json`) | คืนชื่อเดิมจาก backup |
| F11 (แบบ2 ขั้น 4) | พรีเช็คฟรีก่อนจ่ายเงิน Gate 2: รัน mapCategory+scoreLibraryPrompts ออฟไลน์กับข่าวหมวดใหม่ 337 ข่าวใน archive — การ์ดหมวดตรงติด top-8 ≥ 80% ไม่ผ่าน = จูน alias/ชื่อการ์ด (หรือค่อยพิจารณา CONFLICT_CLUSTERS) ก่อนเริ่ม Gate 2 | สคริปต์ใน run-dir (อ่านอย่างเดียว) | — |
| F12 (แบบ2 C6 + เฟสของแบบ 1/3) | ครูตรงหมวด ~34 ใบจากโพสต์เพจจริง (คดี 130 · ศาสนา 194 · กีฬา 71 โพสต์ · เกณฑ์: ไลก์ ≥ ฐาน 7,752 + ตะแกรง VR + คนกวาดโทน โดยเฉพาะคดีความ) — **เฟสแล็บ**: ป้อนผ่าน `VIRAL_EXAMPLES_LAB_FILE` (vf อ่านไฟล์แทนตารางเมื่อ `CARD_LIBRARY_LAB=1`) · **เฟสจริง (หลัง Gate 2 ผ่าน)**: import ตาราง `viral_examples` พร้อม tag `source:'cardlib-v2-3sep69'` ถอนได้เป็นชุด + ชั้นใหม่ 2 ชั้น + เติมชั้นกีฬา (มีครู 1 ใบ) + ย้ายคำ ศาสนา/พระสงฆ์/ศรัทธา/ทำบุญ ออกจากชั้นช่วยเหลือกัน (vf:271) | `viralFewshot.js` (**dirty ของ workflow คู่ขนาน — ทำหลัง merge เท่านั้น** · ไม่อยู่ในลิสต์ล็อก) + data ครู | `TEACHER_SHELVES_V2` (default '0') · `=0` = ชั้น/คำเดิมทุกไบต์ · ครูลบตาม tag |
| F13 (แบบ3 C3) | ช่องเขียน+ย้อน: `scripts/card-status/{backup,build-arms,migrate,restore}.mjs` — dry-run เป็น default, `--apply` จึงเขียน, backup อัตโนมัติ (getAll authoritative → ไฟล์ + นับแถว), gen reverse-script, idempotent · เขียนผ่าน `createStore('prompt-library')` เท่านั้น | scripts ใหม่ (โฟลเดอร์ไม่ล็อก) · backup ที่ `C:\tmp\news-r233-run\card-design\backup\` | `restore.mjs <backup>` รายใบ/ทั้งชุด — ซ้อมกับสำเนาก่อนใช้จริง |
| F14 (แบบ1 C12 / แบบ3 C10) | UI คลัง: ป้าย+ตัวกรอง active/proposed/archived · PUT เพิ่ม action `archive`/`restore` (แบบ action use/success — ไม่เปิด whitelist ให้เขียน status ตรง) · เปลี่ยนปุ่มลบรายใบ (page.js:53 → route.js:168 `store.remove` ไม่มีด่าน) เป็นปุ่มพัก + ลบจริงเหลือเฉพาะใบ proposed freq 0 หลังพิมพ์ id ยืนยัน | `src/app/prompt-library/page.js` + `src/app/api/prompt-library/route.js` (ไม่ล็อก) | git revert คอมมิตเดียว (โค้ดล้วน) |

## 3) สวิตช์ทั้งหมด

| สวิตช์ | ที่อยู่ | default | ย้อนกลับ |
|---|---|---|---|
| `CARD_LIBRARY_LAB` (+`CARD_LIBRARY_OVERLAY_FILE`) | persistStore.js (F2) | ไม่ตั้ง = ปิด · แล็บเท่านั้น · เพิกเฉย+เตือนเมื่ออยู่บน Vercel | unset = โค้ดเดิมทุกเส้นทาง |
| `ENDING_MODE` | promptModes/viralFewshot (มีอยู่แล้ว) | `truth` | unset/`truth` |
| `CARD_LIBRARY_V2` | libraryStatus.js + 6 ทางเข้า (F7) | `'1'` ตอน deploy (no-op จน data มี flag — เทสยืนยันไบต์เดิม) | `=0` เห็นทุกใบเหมือนเดิมไม่ต้อง revert |
| `CARD_LAB_INCLUDE_PROPOSED` | libraryStatus.js | ไม่ตั้ง = ปิด · ห้ามเปิดใน prod | unset |
| `NEW_CARD_CATS_V1` | semanticClusters.js (F9) | `'0'` จนกว่า Gate 2 ผ่านรายหมวด | `=0` = แมปเดิมไบต์ต่อไบต์ |
| `TEACHER_SHELVES_V2` (+`VIRAL_EXAMPLES_LAB_FILE`) | viralFewshot.js (F12 — หลัง merge) | `'0'` | `=0` = ชั้น/คำเดิม · ครูถอนตาม tag |

ทุกสวิตช์ใหม่: อ่านค่าแบบรับเฉพาะ `'1'`/`'0'` ตามแบบแผน `cardAuthority` + **snapshot test "ปิด = ไบต์เดิม" ทุกตัว** (กระดูกของแบบ 3) · หมายเหตุ: `SUPABASE_DISABLED` มีอยู่แล้วแต่**ห้ามใช้ทำ A/B** (§0.2)

## 4) สายงานขนาน (1 ไฟล์ = 1 คนแก้ · ทุกสายไม่ชนกันและไม่ชน workflow คู่ขนาน)

| สาย | ไฟล์ที่เป็นเจ้าของ | งาน |
|---|---|---|
| A: ร่าง data | run-dir + `docs/proposals/` เท่านั้น (`card-surgery-v2.json` · `card-library-new-cards-v2-3sep69.json` · `pause-rename-plan-final.json` · ไฟล์คลังแขน A/B/C · ครูแล็บ · ข่าว .txt ใหม่ 9 ใบ) | F3–F6, F8, F10 ร่าง + F12 คัดครู — ผู้ตรวจไขว้เช็ก "ชื่อ+600 ตัวแรก" รายใบ (ห้ามตรวจงานตัวเอง) |
| B: แล็บ | `src/lib/persistStore.js` + `tests/persist-store-lab-overlay.test.mjs` | F2 |
| C: matcher | `src/lib/ai/semanticClusters.js` + เทส snapshot | F9 |
| D: UI | `src/app/prompt-library/page.js` + `src/app/api/prompt-library/route.js` + เทส route | F14 |
| E: สคริปต์เขียน | `scripts/card-status/*.mjs` (ใหม่) + เทส dry-run/restore | F13 |
| F: ปฏิบัติการ A/B | run-dir: `set-arm.mjs` (เพิ่ม ARMS/LAB_KEYS ใหม่) · สำเนา `ab-run-cards.mjs` รับโฟลเดอร์ข่าว · build-packet/assemble/aggregate เดิม | Gate 1/2 + พรีเช็ค F11 |
| G (เฟส 2 — เริ่มได้เมื่อ workflow คู่ขนาน merge แล้วเท่านั้น + เจ้าของยืนยันสิทธิ์ไฟล์ล็อกอีกครั้ง) | `summarizeServiceText.js` (ล็อก) + `summarizeService.js` (ล็อก) + `src/lib/ai/libraryStatus.js` (ใหม่) + `viralFewshot.js` | F7 + F12(โค้ด) — ประกาศไฟล์ก่อนเริ่ม · rebase + รันเทสของเขาซ้ำ (lib-classifier-v2, teacher-rank-v2) |

## 5) ลำดับปล่อย: โค้ดก่อน → deploy → ข้อมูล (ทุกขั้นมีด่าน)

| ขั้น | งาน | ด่านก่อนไปต่อ |
|---|---|---|
| S0 | เจ้าของเคาะแบบนี้ + จองคิวไฟล์กับ workflow คู่ขนาน + backup คู่สด (export store `prompt-library` authoritative + สำเนา mirror) + **ซ้อม restore กับสำเนา** | backup นับแถวตรง 201 · restore ซ้อมผ่าน |
| S1 | สาย A ร่างทั้งหมด + สร้างไฟล์คลังแขน: A=export สด (ควบคุม) · B=A ทับด้วย F3+F4+F5+F6(ตัดออกจากไฟล์แขน)+F8(ใบใหม่ active ในแล็บ)+F10+โอนฮีโร่ · C=B | ผู้ตรวจไขว้ผ่านรายใบ · diff ไฟล์แขน B ตรงร่าง 100% |
| S2 | สาย B/C/D/E เขียนโค้ด+เทส → push → deploy (ทุกสวิตช์ inert: LAB ไม่ตั้ง · NEW_CARD_CATS_V1=0 · UI ใช้ได้เลย) | เทสใหม่+เก่า+`validate-workflow.mjs` ผ่าน · snapshot "ปิด=ไบต์เดิม" ผ่านทุกตัว |
| S3 | **Gate 1** ในแล็บ (§6.1) → รายงานเจ้าของพร้อมผลดิบ → **จุด go/no-go แรก** | ผ่านเกณฑ์ §6.3 + เจ้าของเคาะ — ไม่ผ่าน = เข้าบันได §6.4 หรือจบโดย production ไม่ถูกแตะเลย |
| S4 | migration รอบ 1 ลง store จริง (F3+F4+F5+F6+F10+โอนฮีโร่+viralScore — สคริปต์เดียว dry-run diff ให้คนกวาดตา → `--apply` → ตรวจนับ) · ยังไม่ import ใบใหม่ (รอตัวกรอง F7 ตามกฎ "เปิดตัวกรองก่อน import" ของแบบ 3) · flag archived ยังไม่มีผลจน S5 — ยอมรับโดยรู้ตัว (ถ้า workflow คู่ขนาน merge ก่อน S4 ให้สลับ S5 ขึ้นก่อน) | dry-run ตรงจำนวนคาด (201 ctaStyle · 172 promptText · 11 ผ่าตัด · 30 ชื่อ · 27 flag · 19 score · 2 โอน) · smoke 1 ข่าว/ท่อ · เฝ้า lift 7 วัน |
| S5 (หลัง merge) | สาย G: F7 ตัวกรอง + F12 โค้ด vf + เทส → deploy (CARD_LIBRARY_V2=1 no-op→มีผลกับ flag ที่ลงแล้ว · TEACHER_SHELVES_V2=0) + ตั้ง ENDING_MODE ตามผลแขน C | เทส+validate ผ่าน · ยืนยันใบพักหายจาก candidate จริง · promptId ใบพักไม่โผล่ log 48 ชม. |
| S6 | พรีเช็ค F11 (ฟรี) → **Gate 2** ในแล็บ (§6.2) → **จุด go/no-go สอง (รายหมวด)** → migration รอบ 2: import การ์ดใหม่เป็น proposed → ตรวจไม่โผล่ prod → flip active เฉพาะหมวดที่ผ่าน + `NEW_CARD_CATS_V1=1` + `TEACHER_SHELVES_V2=1` + import ครูตาราง (tag) → คนตรวจโพสต์คดีความจริง 10 โพสต์แรก | ผ่านเกณฑ์รายหมวด + hard-gate คดีความ · หมวดไม่ผ่าน = การ์ดคง proposed |
| S7 | เฝ้า 14 วัน: `lift-report.mjs` (มิติการ์ด/หมวดมีแล้ว) + สัดส่วนการหยิบใบผ่าตัด/ใบใหม่ + การกระจายตัวเลือก luna (top-10 ≤ 30%) — เกณฑ์ถอย: ไลก์กลางต่ำกว่าหน้าต่างก่อนเกิน 15% → ปิดสวิตช์/restore รายชุด | นัดอ่านรายงานกับเจ้าของ |

## 6) แผน A/B ห้องแล็บ (production ไม่ถูกเขียนก่อน S4 แม้ไบต์เดียว)

**6.1 Gate 1 — คลัง v2 ทั้งชุด (S3):** ข่าวมาตรฐาน 5 ใบ (`ab-news/` เดิม) × 3 แขน = 15 ฉบับ ≈ **$7.5** · ทุกแขนตรึงกฎนักเขียน default ปัจจุบัน · รันผ่าน run-arms.ps1 เดิม + แขนใหม่ใน set-arm:
- แขน A: LAB ไม่ตั้ง (เห็น store จริง = prod วันนี้ รวมครูจริง)
- แขน B: `CARD_LIBRARY_LAB=1` + ไฟล์แขน B (คลัง v2 ทั้งชุด) · ENDING truth
- แขน C: = B + `ENDING_MODE=plain`

**6.2 Gate 2 — หมวดใหม่+ครู (S6):** ข่าวจริงจาก archive 9 ใบ (raw-samples.json: คดี `rhl3ke` ปู มัณฑนา · `bsolk7` ติ๊ก ชิโร่ · `niwio1` ทรายสก๊อต — ศาสนา `1entfv` หลวงพ่อพบโชค · `ru6dtl` เบลล่าถวายกุฏิ · `c2f3y1` เจ้าอาวาสรับส่งนักเรียน — กีฬา `q77lv4` สมจิตร · `etxpx3` สนุกเกอร์คนพิการ · `dxvco9` หมอนทอง) × 2 แขน = 18 ฉบับ ≈ **$9**:
- แขน A2: store จริงหลัง S4/S5 (ไม่มีหมวดใหม่)
- แขน B2: LAB + ไฟล์แขน (การ์ดใหม่ active) + `NEW_CARD_CATS_V1=1` + `TEACHER_SHELVES_V2=1` + `VIRAL_EXAMPLES_LAB_FILE=ครู 34 ใบ`

กรรมการปิดชื่อ 15 คน 5 มิติเดิม ผ่าน `build-packet-ab.mjs` (สุ่มลำดับ+ปิดป้ายแขน) → `assemble-judging.mjs` → `aggregate-ab.mjs` · ทุกรอบบันทึก: arm env + git rev + `git status` ต้องนิ่ง + promptId ที่ถูกหยิบต่อฉบับ (packet-ab-key มีแล้ว)

**6.3 เกณฑ์ผ่าน (paired delta ภายในข่าวเดียวกัน เทียบแขน A รอบเดียวกัน · ตั้งก่อนรัน · ก้ำกึ่ง = ไม่ผ่าน):**
1. Gate 1: mean(B−A) รวม ≥ **+1.0/50** และ fidelity ≥ **+0.5** (คอขวดจริง 6.73) และ tightness ≥ **0** และ opening ≥ **−0.3** และไม่มีข่าวใดร่วง ≥ **2.0** และ promptId ใบพัก/ใบเดิมของชื่อที่เปลี่ยน โผล่ในแขน B/C = **0 ครั้ง**
2. `ENDING_MODE=plain` เปิด prod เฉพาะ mean(C−B) ≥ **+0.5**
3. Gate 2 ตัดสิน**รายหมวด** (3 ข่าว/หมวด): B2 ชนะ ≥ **2/3** และ mean(B2−A2) หมวด ≥ **+1.0** และ fidelity B2 หมวด ≥ **6.5** และ luna หยิบการ์ดหมวดตรง ≥ **2/3** (อ่านจาก log) — ผ่านหมวดไหนเปิดหมวดนั้น
4. hard-gate คดีความ: ฉบับฟันธงผิด-ถูกแทนศาล/เรียกสถานะคู่ความผิด = **0** (คนอ่านเอง 3 ข่าว × 2 แขน) — ตกข้อนี้ = คดีความไม่เปิด แม้คะแนนชนะ
5. พรีเช็ค F11 ก่อน Gate 2: top-8 ≥ **80%** ของ 337 ข่าว

**6.4 บันไดแตกก้อน (ลงทะเบียนล่วงหน้า — ใช้เมื่อ Gate 1 ไม่ผ่าน):** B1 = F3 เท่านั้น (กวาด+viralScore) · B2 = B1+F4+F5+F10 (ผ่าตัด+ชื่อ) · B3 = B2+F6 (พัก) — วัดทีละขั้นกับข่าว 5 ใบเดิม ขั้นละ ~$5 (แขนเดียวเทียบ A เดิม) หาก้อนที่พัง แก้เฉพาะก้อน แล้วยืนยันซ้ำทั้งชุด ≤ $7.5

**งบรวม:** Gate 1 $7.5 + Gate 2 $9 = **$16.5 ฐาน** · เพดานรวมกรณีเข้าบันได+ยืนยันซ้ำ ≈ **$40** (ฐานราคาเจ้าของ ~$0.5/ข่าว/แขน · ค่ากรรมการแยกไม่มีข้อมูล — ระบุตามจริง)

## 7) เกณฑ์ go/no-go ก่อนแตะ DB จริง (สรุปรวม)

1. Gate 1 ผ่านครบทุกข้อ §6.3(1) + เจ้าของเคาะ → จึงเริ่ม S4 (migration รอบ 1) — ไม่ผ่านและบันได §6.4 ไม่กู้ = จบ production ไม่ถูกแตะ
2. ก่อน apply ทุกครั้ง: backup วันเดียวกัน + dry-run diff คนกวาดตา + reverse-script ซ้อมแล้ว + tree นิ่ง + นอกช่วงข่าวเข้า
3. S5 deploy ตัวกรองต้องพิสูจน์ no-op ก่อน (store ยังไม่มี flag ตอน deploy โค้ดในเคสสลับลำดับ) และ "เปิดตัวกรองใน prod ก่อน import ใบ proposed เสมอ"
4. Gate 2 + hard-gate คดีความ + พรีเช็ค ≥80% → จึง import การ์ดใหม่/ครู + เปิดสวิตช์รายหมวด
5. หลังปล่อย: lift 14 วันต่ำกว่าหน้าต่างก่อน >15% หรือ luna top-10 >30% → ถอยตามสวิตช์/restore รายชุด แล้วรายงานก่อนลองใหม่

## 8) ความเสี่ยงที่ยังเปิดอยู่

| # | เสี่ยง | สถานะ/กัน |
|---|---|---|
| 1 | ห้องแล็บ overlay ต่างจาก prod เล็กน้อย (เขียน store เป็น no-op ในแล็บ — usage counter ไม่ขยับระหว่างรัน) | ยอมรับ: ไม่มีผลต่อการเลือกการ์ดในรอบเดียว · ดีกว่าปนเปื้อน store จริงแบบรอบก่อนๆ |
| 2 | จังหวะ merge ของ workflow คู่ขนานไม่รู้วัน — F7/F12 (ตัวกรอง+ครู) ค้างรอ ทำให้ใบพัก 16/27 ที่ freq ≥3 ยังถูกหยิบได้หลัง S4 จนถึง S5 | มูลค่าหลัก (กวาด+ผ่าตัด+ชื่อ) ส่งที่ S4 โดยไม่ต้องรอ · ถ้า merge มาก่อน สลับ S5 ขึ้นก่อน S4 |
| 3 | ฝีมือการเขียนเนื้อการ์ดใหม่ (F4/F8) คือตัวแปรมนุษย์ วัดด้วยข่าว 5+9 ใบ | ผู้ตรวจไขว้เกณฑ์ "ชื่อ+600 ตัวแรก" รายใบ + วัดสัดส่วนการหยิบจริง 7-14 วัน + งบผ่าตัดรอบสองรอไว้หลังเห็น lift |
| 4 | ครู 34 ใบเป็นงานคัดมือ อาจได้ไม่ครบ/โทนหลุด (โดยเฉพาะคดีความ) | เกณฑ์คัดเข้ม 3 ชั้น + ชั้นไม่ถึง 8 ใบยอมรับ Style Pack (ระบบไม่ล้ม vf:1259) + Gate 2 วัดของจริง + tag ถอนเป็นชุด |
| 5 | คะแนนกรรมการรอบใหม่เทียบรอบเก่าไม่ได้ (แผงคน/ความยากข่าวต่างกัน) | เกณฑ์ทุกข้อ paired ในรอบเดียวกันเท่านั้น — ห้ามอ้าง 36.9/31.78 ข้ามรอบ |
| 6 | การ์ด 163 ใบนอกลิสต์ผ่าตัด ได้แค่การกวาด F3 | เจตนา (แบบ 1): เก็บงบรอบสองไว้หลัง lift จริง · watchlist 4 ใบ (`1e6a0b63 98f3b48d 2565b093 af1dd10c` — 2 ตัวแรกเข้าผ่าตัดแล้ว) ทบทวนพร้อมรอบสอง |
| 7 | สิ่งที่ยังตอบไม่ได้ (ประกาศ ไม่เดา): ไลก์จริงของใบ freq สูงยุคนี้ (โพสต์จริง ≤1/ใบ) · เกณฑ์ขั้นต่ำครูของ vf · ผลครู 1 ใบซ้ำในชั้นกีฬา · ต้นทุนกรรมการแยก | เฝ้าไลก์ 14 วันแทนตัวเลขล่วงหน้า · อ่าน log ตอน Gate 2 |

*ไฟล์ประกอบ: ฐานหลักฐาน `card-design-scout-3sep69.md` + 3 แบบ 3 ก.ย. · แผนรายใบ `C:\tmp\news-r233-run\card-design\pause-rename-plan-3sep69.json` (จะ regen เป็น `-final` ตาม F6/F10) · ร่างการ์ดเดิม `card-library-new-cards-2sep69.json` · ผลดิบ `C:\tmp\news-r233-run\{card-design,card-proposal}\out\` + `ab-aggregate-r3.json`*

— 🏛️ สถาปนิก (เฟเบิ้ล-สุด)
