/**
 * =====================================================
 * Viral Few-shot — เรียนสำนวนจากหอสมุดไวรัลจริง 200+ โพสต์ (8 ส.ค. 69: 202 ใบ/14 หมวด — ทุกใบถูกเรียกได้จริง)
 * =====================================================
 * (11 มิ.ย. — ผู้ใช้เลือก: Few-shot ตามหมวด + สำนวนเพจไวรัลเต็มตัว)
 * ดึงโพสต์ไวรัลจริง "หมวดเดียวกับข่าว" 2 ตัวอย่างใส่พรอมต์ writer
 * + VIRAL STYLE PACK (สูตรที่สกัดจากโพสต์ท็อป: hook/ตัวเลข/วลีลายเซ็น/จังหวะ/จบคม)
 * fail-safe: Supabase ล่ม → ได้ Style Pack อย่างเดียว (ไม่พัง pipeline)
 */

import { getSupabase } from '../supabase.js';
import fs from 'node:fs';
import path from 'node:path';

// ★ 16 ส.ค. 69 — ตัวอ่าน env: ตัดช่องว่าง + ถอดเครื่องหมายคำพูด + ไม่สนตัวพิมพ์
//   🔴 ใช้ได้เฉพาะ "สวิตช์ที่เกิดในแบตช์นี้" (VIRAL_SHORTLIST, VIRAL_SHORTLIST_K) เท่านั้น
//   ⛔ ห้ามเอาไปครอบสวิตช์เก่า — เคยทำแล้วผู้ตรวจ 2 ทีมจับได้ว่าเป็นระเบิด:
//      ตัวอย่างเดิมที่เคยเกิดจริง: HOOK_STYLE_MODE ถูกอ่าน 3 แบบใน 3 ไฟล์ (uppercase / ดิบ / ที่นี่)
//      ล้างรูปแบบแค่ไฟล์เดียวแล้วอีก 2 ไฟล์อ่านไม่ตรงกัน → ส่งกฎขัดกันเองเข้าพรอมต์เดียวกัน
//      🗓️ 18 ส.ค. 69: HOOK_STYLE_MODE ถูกถอดออกจากระบบแล้ว (ไม่มีโค้ดอ่านอีก) — เก็บไว้เป็นตัวอย่างบทเรียนเท่านั้น
//      → จะล้างรูปแบบสวิตช์เก่าจริง ต้องทำเป็นแบตช์ของตัวเองที่แก้ทุกไฟล์พร้อมกัน
const _envTok = (name) => String(process.env[name] ?? '').trim().replace(/^["']|["']$/g, '').toLowerCase();

// สูตรสกัดจากโพสต์ top engagement ของหอสมุด — always-on
// 18 ส.ค. 69 เจ้าของสั่ง "ใช้โค้ดช่วง 12 มิ.ย. – 1 ก.ค." — คืนสูตรนี้เป็นตัวดั้งเดิม e5ba1eb (11 มิ.ย.)
// ถอยที่คืนออกไป: c168841 (14 ส.ค. แก้ข้อ 1) · ee64be8 (1 ส.ค. แก้ข้อ 5) · กู้กลับ: git show e5ba1eb
const VIRAL_STYLE_PACK =
  '=== 🔥 VIRAL STYLE PACK — สูตรเพจไวรัล (บังคับใช้) ===\n' +
  '1. เปิดด้วย HOOK ไวรัล (เลือก 1): คำถามกระแทก ("จะมีสักกี่คนที่..."), ความย้อนแย้ง ("ไม่ใช่แค่สวย แต่ยังขยัน..."), คำพูดตัวละคร ("ตั้งแต่จำความได้ ผมก็ถามหาแต่แม่"), หรือภาพเหตุการณ์พีค — ห้ามเปิดเนิบ\n' +
  '2. ตัวเลขเจาะใจ: ถ้าในข้อมูลมีตัวเลข (อายุ/จำนวนปี/เงิน/ระยะทาง) ต้องชูให้เด่นแบบ "ปั่นจักรยาน 14 กิโลฯ เพื่อเงิน 20 บาท" — ใช้เฉพาะตัวเลขที่มีจริง ห้ามแต่ง\n' +
  '3. วลีลายเซ็นชวนแชร์ 1-2 จุดต่อโพสต์ (เลือกที่เข้ากับเรื่อง): "ไม่แปลกใจเลยที่...", "ขอนับถือใจ...", "ดีใจแทน...", "ใครจะคิดว่า..."\n' +
  '4. จังหวะโพสต์เฟซบุ๊ก: บรรทัดสั้นสลับยาว ขึ้นบรรทัดใหม่บ่อยกว่าบทความ — ประโยคทุบให้อยู่บรรทัดของมันเอง\n' +
  '5. จบด้วยประโยคสัจธรรมสั้นๆ ที่คนอยากก๊อปไปโพสต์ต่อ ("ไม่มีวันไหนยากไปกว่าวันที่...", "คนกตัญญูไม่มีวันล้มจม") — ห้ามจบด้วยคำถาม\n' +
  '=== จบ VIRAL STYLE PACK ===\n\n';

// ★ 14 ส.ค. 69 ค่ำ — "สูตรแสนไลก์" เป็นตัวจริง (เจ้าของสั่ง "จัดการให้เป็นโค้ดใหม่" หลังดูผลเทส 3 ข่าวแล้วชอบ)
//   default เปิด · ถอยกลับสูตรเดิมทั้งชุด: VIRAL_HITS_FORMULA=0 (แพตเทิร์นเดียวกับ VIRAL_ROTATE=0)
//   ที่มา: กรรมการ 4 โมเดล (Fable/Sol/K3/Opus) อ่านโพสต์จริงของเพจ 122 ใบพร้อมไลก์จริงจาก CSV
//   สถิติที่นับได้จริง: เปิดตัวเลข=ท่าแชมป์ (10-13/27 กลุ่มแสน เฉลี่ย ~136k) · เปิดคำพูด=ท่าไลก์ต่ำสุด (0-1/27)
//   · ประโยคบอกคนอ่านว่าควรรู้สึกอะไร = 0/122 · จบด้วยประโยคนิยามภาพสั้นพบบ่อยในใบปัง
const _hitsOn = () => process.env.VIRAL_HITS_FORMULA !== '0';

// ★ 14 ส.ค. 69 (สูตรแสนไลก์ ข้อ 3): ถ่วงการหยิบครูด้วยไลก์จริงจากเพจ — ไฟล์ data/viral-likes-real.json
//   (สร้างโดย scripts/match-real-likes.mjs) · อ่านเฉพาะตอนสวิตช์เปิด · ไม่แตะ Supabase กลางเด็ดขาด
// ★ ผู้ตรวจอิสระจับได้ (S1 — กันซ้ำรอย "ครูหด" ที่เจ้าของสั่งย้อน 14 ส.ค.): ไลก์ในตารางเป็น 0 ทั้ง 202 ใบ
//   การทับดิบๆ 130 ใบที่แมชได้ = อีก 72 ใบน้ำหนักเหลือ ~0 ตายถาวร → แก้เป็น 2 ชั้น:
//   (1) ใบที่ไม่มีไลก์จริงได้ "ค่ากลาง (median)" ของโผ = ทุกใบยังมีสิทธิ์จริง
//   (2) บีบสเกลด้วย sqrt — ใบ 309k เทียบใบค่ากลาง เหลือห่างกัน ~2-3 เท่า ไม่ใช่ 60+ เท่า
let _realLikes = null, _realLikesAt = 0;
const REAL_LIKES_CACHE_MS = 10 * 60 * 1000; // ★ ผู้ตรวจ S6: แยกจาก CACHE_MS ของโผ (กัน TDZ + ปรับตัวหนึ่งไม่กระทบอีกตัว)
function _loadRealLikes() {
  if (!_hitsOn()) return null;
  if (_realLikesAt && Date.now() - _realLikesAt < REAL_LIKES_CACHE_MS) return _realLikes;
  try {
    _realLikes = JSON.parse(fs.readFileSync(path.join(process.cwd(), 'data', 'viral-likes-real.json'), 'utf8'));
  } catch { _realLikes = null; }
  _realLikesAt = Date.now();
  return _realLikes;
}
// ★ export เพื่อให้ข้อสอบยิงตรงได้ (ผู้ตรวจ S3) · mapOverride ใช้เฉพาะในเทส
export function _applyRealLikes(rows, mapOverride = undefined) {
  const m = mapOverride !== undefined ? mapOverride : _loadRealLikes();
  if (!m || !Array.isArray(rows) || !rows.length) return rows;
  // ★ ผู้ตรวจ S5: ใช้ byId อย่างเดียว (ครอบ 130/130 อยู่แล้ว) — byKey เสี่ยงคีย์ชนเงียบเมื่อคลังโต
  const realOf = (r) => {
    const e = m.byId?.[r.id];
    const likes = typeof e === 'number' ? e : Number(e?.likes);
    return likes > 0 ? likes : null; // กันติดลบ/NaN/0/สตริงขยะ (Sol ท้วง)
  };
  const hitVals = rows.map(realOf).filter((x) => x !== null);
  if (!hitVals.length) return rows;
  const med = hitVals.sort((a, b) => a - b)[Math.floor(hitVals.length / 2)];
  let hit = 0;
  const out = rows.map((r) => {
    const real = realOf(r);
    if (real !== null) hit++;
    // sqrt-scale ทั้งโผ (ใบไม่แมช = ค่ากลาง) — น้ำหนักสุดท้ายเป็นจำนวนเต็มบวกเสมอ
    return { ...r, engagement_likes: Math.max(1, Math.round(Math.sqrt(real ?? med))) };
  });
  console.log(`[ViralFewshot] 💗 ไลก์จริง ${hit}/${rows.length} ใบ + ค่ากลางเติม ${rows.length - hit} ใบ (sqrt-scale, hits-formula)`);
  return out;
}

// map หมวดจาก breakdown → หมวดหอสมุด
// ★ 7 ส.ค. 69: เติม 6 หมวดล่างที่มีในคลังจริง (17 ใบ) แต่โค้ดเดิมไม่รู้จัก = ไร้ทางเข้าถาวร
const CATEGORY_HINTS = [
  { lib: 'ดราม่าครอบครัว', keys: ['ครอบครัว', 'แม่', 'พ่อ', 'ลูก', 'พี่น้อง', 'ดราม่า'] },
  { lib: 'ข่าวเศร้า', keys: ['เศร้า', 'สูญเสีย', 'เสียชีวิต', 'อาลัย', 'จากไป'] },
  { lib: 'ข่าวการเมือง', keys: ['การเมือง', 'เลือกตั้ง', 'รัฐบาล', 'นายก', 'พรรค'] },
  { lib: 'ช่วยเหลือกัน', keys: ['ช่วยเหลือ', 'บริจาค', 'น้ำใจ', 'เสียสละ', 'มูลนิธิ', 'จิตอาสา'] },
  { lib: 'สู้ชีวิต', keys: ['สู้ชีวิต', 'ลำบาก', 'ยากจน', 'ฝ่าฟัน', 'โรค', 'ป่วย'] },
  { lib: 'ข่าวบันเทิง', keys: ['ดารา', 'บันเทิง', 'คนดัง', 'ศิลปิน', 'นักแสดง'] },
  { lib: 'พลิกชีวิต', keys: ['พลิกชีวิต', 'สำเร็จ', 'จากศูนย์', 'เปลี่ยนชีวิต', 'รวย'] },
  { lib: 'ข่าวเตือนใจ', keys: ['เตือนใจ', 'เตือนภัย', 'บทเรียน', 'อุทาหรณ์'] },
  // ★ Sol จับได้: ห้ามใช้คีย์ 'หมา' — เป็นท่อนย่อยของ กฎหมาย/หมายเลข/หมายจับ (ข่าวกฎหมายจะได้ตัวอย่างสัตว์)
  { lib: 'ความรักสัตว์', keys: ['สัตว์', 'สุนัข', 'แมว', 'ช้าง', 'สัตว์เลี้ยง', 'ลูกหมา', 'น้องหมา'] },
  { lib: 'ข่าวชาวบ้าน', keys: ['ชาวบ้าน', 'ชุมชน', 'หมู่บ้าน', 'ตลาด', 'ท้องถิ่น', 'วิถีชีวิต'] },
  { lib: 'ข่าวกีฬา', keys: ['กีฬา', 'นักกีฬา', 'ฟุตบอล', 'วอลเลย์บอล', 'แข่งขัน', 'เหรียญ', 'ทีมชาติ'] },
  { lib: 'คนดังตกต่ำ', keys: ['ตกต่ำ', 'ตกอับ', 'ล้มละลาย', 'หมดตัว', 'ชีวิตดิ่ง'] },
  { lib: 'nostalgia', keys: ['ย้อนวัย', 'วันวาน', 'ความทรงจำ', 'คิดถึงอดีต', 'สมัยก่อน', 'ตำนาน'] },
  { lib: 'moral conflict', keys: ['ศีลธรรม', 'ถูกผิด', 'จริยธรรม', 'ประเด็นถกเถียง', 'ดราม่าสังคม'] },
];

// ★ 7 ส.ค. 69: แคชเก็บ "รายการ top-N" ต่อหมวด (ของเดิมเก็บผิดรูป — cached.at ไม่มีจริง แคชเลยไม่เคยติด)
//   การสุ่มเกิดใหม่ทุกครั้งจากรายการแคช = DB โหลดเท่าเดิม แต่ตัวอย่างหมุนทุกข่าว
let _cache = new Map(); // libCat → { rows, at }
const CACHE_MS = 10 * 60 * 1000;

// ★ สวิตช์ถอย: VIRAL_ROTATE=0 = พฤติกรรมเดิมเป๊ะ (หยิบ 2 ใบไลก์สูงสุดตายตัว)
const _rotateOn = () => process.env.VIRAL_ROTATE !== '0';

/**
 * 📒 สมุดประวัติการหยิบ (8 ส.ค. 69 เจ้าของสั่ง "เก็บประวัติแม่นยำ ตัวไหนถูกเรียก")
 * จดลง Supabase store_items/viral_pick_history: ใบไหน (id+ชื่อ) · หมวด · ขนาดโผ · หัวข่าว · เวลา
 * อ่านผ่าน GET /api/viral-library?action=pick-history หรือ ?action=pick-stats
 * fail-safe แท้: จดไม่สำเร็จ = log แล้วเดินต่อ ห้ามล้มท่อข่าวเด็ดขาด
 */
async function _recordPickHistory(libCat, picks, meta = {}) {
  try {
    const sb = getSupabase();
    if (!sb || !picks.length || meta.noHistory) return;
    const nowIso = new Date().toISOString();
    const id = 'vpick_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);
    // ★ ผู้ตรวจจับได้ (บทเรียนซ้ำรอย "ตำราว่างเงียบๆ" 2 ส.ค.): supabase-js ไม่ throw เมื่อ insert ล้ม —
    //   มันคืน { error } เงียบๆ ต้องเช็คเองไม่งั้นสมุดว่างโดยไม่มีใครรู้ (catch ข้างล่างเป็นตาข่ายชั้นสองเท่านั้น)
    // ★ 16 ส.ค. 69 (ผู้ตรวจอิสระ — โจทย์เจ้าของสั่ง "ต้องตรวจย้อนได้"): เหตุผลของชั้นเฉพาะกิจยาวเฉลี่ย ~366 ตัวอักษร
    //   เพดาน 240 เดิมตัดเหลือ ~3 ใบจาก 8 = ย้อนดูไม่ได้ว่าใบไหนแข่งบ้าง → ขยายเป็น 700 เฉพาะโหมดใหม่
    //   🔴 โหมดเก่า (rotate/ai/score/top2) คงเพดาน 240 เป๊ะ — "ปิดสวิตช์ = เดิมเป๊ะ" ต้องจริงถึงสมุดประวัติด้วย
    const isShortlist = String(meta.pickMode || '').startsWith('shortlist');
    const { error: insErr } = await sb.from('store_items').insert({
      id, store_name: 'viral_pick_history',
      data: {
        ts: nowIso, lib: libCat, poolSize: Number(meta.poolSize) || 0,
        // libSize = ขนาดคลังที่ดึงมาทั้งก้อน (poolSize = โผที่ "ตัวสุ่มเห็นจริง") — ใส่เฉพาะโหมดใหม่ ไม่เปลี่ยนรูปแถวเก่า
        ...(isShortlist && meta.libSize != null ? { libSize: Number(meta.libSize) || 0 } : {}),
        newsTitle: String(meta.newsTitle || '').slice(0, 140),
        mode: String(meta.pickMode || ''), reason: String(meta.pickReason || '').slice(0, isShortlist ? 700 : 240),
        picks: picks.map((p) => ({ id: p.id ?? null, title: String(p.title || '').slice(0, 120) })),
      },
      created_at: nowIso, updated_at: nowIso,
    });
    if (insErr) console.log('[ViralFewshot] 📒 จดประวัติไม่สำเร็จ (ไม่กระทบข่าว):', String(insErr.message || insErr.code || '').slice(0, 60));
  } catch (e) {
    console.log('[ViralFewshot] 📒 จดประวัติไม่สำเร็จ (ไม่กระทบข่าว):', e.message?.slice(0, 40));
  }
}

/**
 * สุ่มถ่วงน้ำหนักตามยอดไลก์ ไม่คืนซ้ำ — "คุณภาพนำ ทุกใบมีสิทธิ์"
 * แยก rand ออกเป็นพารามิเตอร์เพื่อให้ข้อสอบคุมผลได้ (ฟังก์ชันล้วน เทสได้ 100%)
 */
export function weightedSample(rows, n = 2, rand = Math.random) {
  const pool = [...(rows || [])];
  const out = [];
  while (out.length < n && pool.length > 0) {
    const total = pool.reduce((s, r) => s + Math.max(1, Number(r.engagement_likes) || 1), 0);
    let roll = rand() * total;
    let idx = pool.length - 1; // กันเศษทศนิยมหลุดปลายลูป
    for (let i = 0; i < pool.length; i++) {
      roll -= Math.max(1, Number(pool[i].engagement_likes) || 1);
      if (roll <= 0) { idx = i; break; }
    }
    out.push(pool.splice(idx, 1)[0]);
  }
  return out;
}

export function pickLibraryCategory({ category = '', emotionalTags = [], archetype = '' }) {
  // 🐛 บั๊กค้างที่รู้ตัวแล้วแต่ "จงใจไม่แก้ในแบตช์นี้" (16 ส.ค. 69 — ผู้ตรวจอิสระจับได้):
  //   ฟังก์ชันนี้ถูกเรียก "นอก try/catch" (บรรทัด ~760) ⇒ ถ้า emotionalTags ไม่ใช่อาเรย์
  //   (AI คืน {tags:[...]} / ตัวเลข / null ที่ส่งมาตรงๆ) `[...emotionalTags]` จะโยน TypeError
  //   หลุดออกนอกฟังก์ชัน = นักเขียนไม่ได้ทั้งตัวอย่าง **และไม่ได้ Style Pack**
  //   ทางแก้คือ `...(Array.isArray(emotionalTags) ? emotionalTags : [])` — 1 บรรทัด
  //   🔴 ทำไมไม่แก้ตรงนี้: มันเปลี่ยนพฤติกรรมตอน "ปิดสวิตช์" (เดิมล้ม → ใหม่ไม่ล้ม)
  //      = ทำให้คำสัญญาของแบตช์นี้ "ปิดสวิตช์แล้วเหมือนเดิมเป๊ะ" ไม่จริงอีกต่อไป
  //      (ขอบเขตคำสัญญาที่วัดจริง: ปิด VIRAL_SHORTLIST ทุกรูปแบบค่า × ข่าวจริง 40 ใบ + เคสขอบ = เหมือน HEAD 100%)
  //      ซึ่งคือหลักประกันเดียวที่ทำให้แบตช์นี้ปลอดภัยพอจะขึ้นระบบจริง → แยกเป็นแบตช์ของตัวเอง
  //   ⚠️ เป็นบั๊กที่มีอยู่แล้วใน HEAD ไม่ใช่ของใหม่ · คอมเมนต์ _tagsText ที่เขียนว่า "ทุกจุดที่อ่านแท็ก
  //      อารมณ์ผ่านตัวนี้" ยังไม่จริงสำหรับจุดนี้ — บันทึกไว้ตรงๆ ดีกว่าปล่อยให้คนอ่านเข้าใจผิด
  const hay = [category, archetype, ...emotionalTags].join(' ').toLowerCase();
  let best = null, bestScore = 0;
  for (const c of CATEGORY_HINTS) {
    // ★ Sol จับได้: นับคะแนนตาม "ความยาวคีย์ที่แมตช์" — คีย์ยาว = เฉพาะเจาะจงกว่า ต้องชนะคีย์สั้นที่ซ้อนกัน
    //   (เดิมนับ 1 เท่ากันหมด → "ดราม่าสังคม" เสมอ "ดราม่า" แล้วหมวดที่มาก่อนในตารางชนะเสมอ)
    const score = c.keys.reduce((s, k) => s + (hay.includes(k.toLowerCase()) ? k.length : 0), 0)
      + (hay.includes(c.lib.toLowerCase()) ? c.lib.length * 2 : 0);
    if (score > bestScore) { bestScore = score; best = c.lib; }
  }
  return best || 'ดราม่าครอบครัว'; // หมวดใหญ่สุดของหอสมุดเป็น default
}

// ═══ 🎯 8 ส.ค. 69 เจ้าของสั่ง "ห้ามสุ่ม — ต้องแมชโครงเรื่อง/อารมณ์/แนวทางจริง มีเหตุผลรองรับ" ═══
// วิธี 1 (ai): บรรณารักษ์ luna อ่านเนื้อดิบ+สารบัญ 202 เลือกพร้อมเหตุผล — กรรมการ 5 คนเคาะชนะ (แมช 9-5-4 ทั้ง 2 ข่าวเทส)
// วิธี 2 (score): โค้ดให้คะแนนแมชจากบัตรลักษณะ — ตาข่ายถอยอัตโนมัติของวิธี 1
// ★ 14 ส.ค. 69 เจ้าของเคาะกลับ "สุ่มทั้งหมวด" (default ปิดบรรณารักษ์) — หลังใช้จริง 4 วันพบสำนวนลู่จืด:
//   ครูสอนเล่าหดจาก 113 → 69 ใบ/วัน (บรรณารักษ์เลือก "ใบแมชสุด" = ใบเดิมซ้ำสำหรับข่าวแนวเดียวกัน)
//   + เทียบข่าวเดียวกัน 3 ใบต่อหน้าเจ้าของ: ฝั่งสุ่มเปิดหัวมีชีวิตกว่า ("ไม่อายที่ทำงาน รปภ. ...")
//   เปิดบรรณารักษ์คืน: VIRAL_MATCH_MODE=ai · โหมดคะแนน: =score — โค้ดจับคู่ทั้งชุดยังอยู่ครบ ไม่ได้ถอด
export function matchModeName(raw) {
  const v = String(raw ?? '').trim().toLowerCase();
  if (v === 'ai') return 'ai';
  if (v === 'score') return 'score';
  return ''; // ว่าง/off/ค่าอื่นทุกแบบ = สุ่มทั้งหมวดแบบเดิม (ค่าเริ่มต้นตามคำสั่งเจ้าของ 14 ส.ค.)
}
const _matchMode = () => matchModeName(process.env.VIRAL_MATCH_MODE);

// แคชผลจับคู่ต่อข่าว 10 นาที — ทุกเวอร์ชันของข่าวเดียวกันได้ "ครูคู่เดียวกัน" (คู่ที่แมชสุด) + จ่าย AI ครั้งเดียว/ข่าว
let _matchCache = new Map(); // newsTitle|libCat → { picks, reason, mode, at }
const MATCH_CACHE_MS = 10 * 60 * 1000;

let _essCache = null, _essFailAt = 0;
const ESS_RETRY_MS = 60 * 1000;
function _loadEssences() {
  // 🛡️ 16 ส.ค. 69 (ผู้ตรวจอิสระ — บทเรียนซ้ำรอย "ตำราว่างเงียบๆ" 2 ส.ค.): ของเดิมแคช {} ถาวรถ้าอ่านไฟล์ล้มครั้งเดียว
  //   ({} เป็น truthy) → ตัวเลือกครูตายทั้งโปรเซสแบบเงียบจนกว่าจะรีสตาร์ต · แก้เป็น "ลองใหม่ทุก 60 วิ"
  //   สำเร็จแล้วแคชถาวรเหมือนเดิมเป๊ะ (ไม่มีการอ่านไฟล์เพิ่มในทางปกติ)
  if (_essCache && Object.keys(_essCache).length) return _essCache;
  if (_essCache && Date.now() - _essFailAt < ESS_RETRY_MS) return _essCache;
  try {
    _essCache = JSON.parse(fs.readFileSync(path.join(process.cwd(), 'data', 'viral-essences.json'), 'utf8'));
    if (!_essCache || typeof _essCache !== 'object') _essCache = {};
  } catch { _essCache = {}; }
  if (!Object.keys(_essCache).length) {
    _essFailAt = Date.now();
    console.log('[ViralFewshot] ⚠️ อ่าน data/viral-essences.json ไม่ได้/ว่าง — โหมดจับคู่+ชั้นเฉพาะกิจจะถอยวิธีเดิม (ลองใหม่ใน 60 วิ)');
  }
  return _essCache;
}

// 🛡️ 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้ — บั๊กแดง): emotionalTags มาจาก newsAnalysis?.emotionalTags ตรงๆ ไม่มีตัวล้างชนิด
//   AI คืน JSON เพี้ยนเป็น "สตริงเดี่ยว" ได้ → `.join(' ')` โยน TypeError แล้วถูกกลืนใน catch ก้อนนอก
//   ขึ้นข้อความหลอกว่า '⚠️ fetch failed' (ชี้ไปที่ Supabase ทั้งที่ Supabase ปกติดี) และนักเขียนไม่ได้ตัวอย่างเลย
//   → ทุกจุดที่อ่านแท็กอารมณ์ต้องผ่านตัวนี้ (รับทั้งอาเรย์ / สตริง / null / อ็อบเจกต์ขยะ)
const _tagsText = (v) => (Array.isArray(v) ? v.filter((x) => typeof x === 'string' || typeof x === 'number').join(' ') : String(v ?? ''));

// 🛡️ 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้ — บทเรียนเดียวกับ _tagsText แต่ลืมฝั่ง "บัตรลักษณะ"):
//   ช่อง themes/emotion ในบัตรถูกอ่านด้วย .map/.filter/.join 7 จุด — ถ้าบรรณารักษ์หรือสคริปต์
//   เติมบัตรใหม่แล้วพิมพ์ "themes": "ครอบครัว" (สตริงเดี่ยว) จะโยน TypeError แล้วถูกกลืนใน catch ก้อนนอก
//   ⇒ นักเขียนไม่ได้ครูสักใบ พร้อม log หลอกว่า '⚠️ fetch failed' (ชี้ไป Supabase ทั้งที่ Supabase ปกติ)
//   วันนี้ 202/202 ใบเป็นอาเรย์หมด — ตัวนี้คือตาข่ายกันวันที่มีคนเติมบัตรผิดรูป
const _arr = (v) => (Array.isArray(v) ? v : (v === null || v === undefined || v === '' ? [] : [v]));

// ตัดเสมอด้วยรหัสตัวอักษรตรงๆ — ให้ผลเดียวกันทุกเครื่อง (ต่างจาก localeCompare ที่ขึ้นกับ locale/ICU)
const _cmpId = (a, b) => { const x = String(a), y = String(b); return x < y ? -1 : x > y ? 1 : 0; };

/**
 * วิธี 2 (โค้ดล้วน ห้ามสุ่ม): ให้คะแนนความแมชข่าว↔ตัวอย่างจากบัตรลักษณะ — ผลนิ่ง 100% พร้อมเหตุผลแจกแจง
 * อารมณ์ตรง ×3 · ธีมตรง ×2 · หมวดตรง +2 · โทนตรง +1 — เสมอกันตัดสินด้วย id (ไม่มีสุ่มเด็ดขาด)
 */
export function scoreMatchExamples(brief = {}, rows = [], essences = {}) {
  const hay = [brief.title, brief.category, (brief.emotionalTags || []).join(' '), brief.archetype, brief.coreStory, brief.excerpt]
    .join(' ').toLowerCase();
  const scored = rows.map((r) => {
    const e = essences[r.id] || {};
    const hitsEmo = (e.emotion || []).filter((w) => w && hay.includes(String(w).toLowerCase()));
    const hitsTheme = (e.themes || []).filter((w) => w && String(w).length >= 2 && hay.includes(String(w).toLowerCase()));
    const toneHit = !!(e.tone && hay.includes(String(e.tone).toLowerCase()));
    const catHit = !!(brief.libCat && r.category === brief.libCat);
    const score = hitsEmo.length * 3 + hitsTheme.length * 2 + (toneHit ? 1 : 0) + (catHit ? 2 : 0);
    return { r, score, hitsEmo, hitsTheme, toneHit, catHit };
  }).sort((a, b) => b.score - a.score || String(a.r.id).localeCompare(String(b.r.id)));
  return scored.slice(0, 2).filter((x) => x.score > 0).map((x) => ({
    row: x.r, score: x.score,
    reason: `คะแนน ${x.score}: ` + [
      x.hitsEmo.length ? `อารมณ์ตรง(${x.hitsEmo.join(',')})` : '',
      x.hitsTheme.length ? `ธีมตรง(${x.hitsTheme.join(',')})` : '',
      x.toneHit ? 'โทนตรง' : '', x.catHit ? 'หมวดตรง' : '',
    ].filter(Boolean).join(' · '),
  }));
}

// ═══════════════════════════════════════════════════════════════════════════
// 🎚️ 16 ส.ค. 69 — "ชั้นวางเฉพาะกิจ" (VIRAL_SHORTLIST=1) · โค้ดล้วน ไม่เรียก AI
// ═══════════════════════════════════════════════════════════════════════════
// ปัญหาที่แก้ (เจ้าของวัดจากข่าวจริง 300 ใบ + หอสมุด 202 ใบ):
//   ระบบเดิมเลือกครูด้วย "ป้ายหมวด → 1 ชั้น → สุ่มในชั้น" แต่ครูที่เข้ากับข่าวที่สุด
//   อยู่นอกชั้นที่ระบบเลือกถึง 73% · ข่าว 77% ตกแค่ 2 ชั้น · ชั้น "ข่าวกีฬา" มีครู 1 ใบ
//   แต่ถูกเลือก 6% (ข่าวกีฬาได้ครูคนเดียวกันเสมอ และได้แค่ 1 ใบ ไม่ใช่ 2)
// วิธีแก้: ให้คะแนนบัตรลักษณะทั้ง 202 ใบด้วยโค้ด → คัดเข้ารอบ 8 ใบ "ข้ามชั้นได้"
//   → ส่งเข้า weightedSample(usable, 2) ตัวเดิมไม่แตะแม้แต่ตัวอักษรเดียว
//   🔴 ความสุ่มอยู่ที่ weightedSample จุดเดียวเท่านั้น — ขั้นให้คะแนน/คัดโผนิ่ง 100%
//   (ห้ามเลือกใบสุดท้ายตายตัวเด็ดขาด = ท่าที่ทำให้ "ครูหด สำนวนจืด" จนเจ้าของสั่งย้อน a10a40f 14 ส.ค.)
//
// ★ ตัวอ่านสวิตช์ = _envTok บนหัวไฟล์ (trim + ถอดเครื่องหมายคำพูด + ไม่สนตัวพิมพ์)
//   (บทเรียน 16 ส.ค.: HOOK_STYLE_MODE ถูกเช็ค 3 แบบใน 3 ไฟล์ พิมพ์ตัวใหญ่แล้วปิดได้แค่ 1 ใน 3)
// 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): ของเดิมค่าที่อ่านไม่ออก (เช่น =2, =enable) "ปิดเงียบ ไม่มี log"
//   คนตั้งเชื่อว่าเปิดแล้ว แต่ระบบวิ่งเส้นเดิม และไม่มีอะไรบอกเลย → ตะโกนบอกครั้งเดียวต่อค่า
// ═══ 📏 16 ส.ค. 69 — เพดานตัวอย่างครูไวรัล (เจ้าของสั่ง "ขยายเพดานเลย ทำสวิตช์ด้วย") ═══
//
// 🔢 ตัวเลขทั้งหมดข้างล่างวัดจาก **คลังครูตัวจริงที่ระบบป้อนให้นักเขียน** = ตาราง `viral_examples` 202 ใบ
//    (ไฟล์ส่งออก: _hits-formula-workspace/viral-examples-export.json · วัด 16 ส.ค. 69)
//    ⚠️ ผู้ตรวจอิสระจับได้ว่ารอบแรกผมวัดผิดคลัง — ไปวัด fb-posts-parsed.json (โพสต์เพจ 1,422 ใบ)
//       ซึ่งเป็นคนละก้อนกับครูที่ใช้จริง ทำให้ตัวเลขเพี้ยน (เคยเขียนว่า 700 ตัดครู 77% ที่จริงคือ 52.5%)
//       ⇒ ใครจะแก้ตัวเลขพวกนี้รอบหน้า ต้องวัดจาก viral_examples เท่านั้น
//
// ปัญหาที่แก้: ของเดิมตัดตัวอย่างครูที่ 700 ตัวอักษรตายตัว แต่ครูจริงยาวมัธยฐาน 713 (ยาวสุด 1,205)
//   ⇒ เพดาน 700 ได้ครูครบทั้งใบแค่ 96/202 = 47.5% · อีกครึ่งถูกตัดกลางเรื่อง
//   🔴 และตรงที่ถูกตัดคือ "ประโยคจบ" ซึ่งเป็นท่าไม้ตายของโพสต์ไวรัล
//      ⇒ นักเขียนเห็นครูเล่าเรื่อง แต่ไม่เคยเห็นครูจบเรื่อง
//   ผลข้างเคียงที่ตามมา: เมื่อครูสอนวิธีจบไม่ได้ ระบบเลยไปลอก "ตัวอย่างรูปประโยค" ในกฎข้อ 6 แทน
//      ซึ่งรูป "จาก...สู่..." นั้น โพสต์แสนไลก์จริง 27 ใบใช้ 0 ใบ แต่ข่าวเราใช้ 16-42%
//
// ทำไมเลือก 1300 (ผู้ตรวจอิสระเสนอ · เดิมผมตั้ง 1100):
//   เพดาน  700 → ครูครบ  47.5%   |   1100 → 99.5%   |   1300 → 100%
//   ต้นทุนเฉลี่ย/ใบ: 700=658 · 1100=737 · **1300=738**
//   ⇒ ขยับจาก 1100 เป็น 1300 ได้ครูครบทุกใบ โดยจ่ายเพิ่มแค่ ~1 ตัวอักษร/ข่าว = ฟรีในทางปฏิบัติ
//   ต้นทุนรวมของการขยาย 700→1300: +158 ตัวอักษร/ข่าว (~53 โทเคน) ≈ 0.5% ของพรอมต์นักเขียน (~30,000 ตัวอักษร)
//
// 🔴 ถอยกลับของเดิม = VIRAL_EXAMPLE_CHARS=700 เท่านั้น — **ห้ามใช้ =0**
//    (สวิตช์พี่น้องในไฟล์นี้ =0 แปลว่าถอย แต่ตัวนี้ =0 จะถูกดันขึ้นพื้น 300 ซึ่งแย่กว่าเดิม · มี log เตือนให้)
const EX_CHARS_DEFAULT = 1300, EX_CHARS_MIN = 300, EX_CHARS_MAX = 3000;
// เตือนครั้งเดียวต่อโปรเซส — ฟังก์ชันนี้ถูกเรียกทุกข่าว ถ้าเตือนทุกครั้ง log จะรก (ผู้ตรวจทัก)
let _exWarned = false;
const _warnOnce = (msg) => { if (!_exWarned) { _exWarned = true; console.log(msg); } };
export function exampleChars() {
  const raw = _envTok('VIRAL_EXAMPLE_CHARS');
  if (!raw) return EX_CHARS_DEFAULT;
  const n = parseInt(raw, 10);
  if (!Number.isFinite(n)) {
    _warnOnce(`[ViralFewshot] 📏 VIRAL_EXAMPLE_CHARS="${raw}" อ่านเป็นตัวเลขไม่ได้ → ใช้ค่าเริ่มต้น ${EX_CHARS_DEFAULT}`);
    return EX_CHARS_DEFAULT;
  }
  if (n < EX_CHARS_MIN) {
    _warnOnce(`[ViralFewshot] 📏 VIRAL_EXAMPLE_CHARS=${n} ต่ำกว่าพื้น ${EX_CHARS_MIN} → ใช้ ${EX_CHARS_MIN} (สั้นกว่านี้ครูสอนอะไรไม่ได้เลย)`);
    return EX_CHARS_MIN;
  }
  if (n > EX_CHARS_MAX) {
    _warnOnce(`[ViralFewshot] 📏 VIRAL_EXAMPLE_CHARS=${n} เกินเพดาน ${EX_CHARS_MAX} → ใช้ ${EX_CHARS_MAX}`);
    return EX_CHARS_MAX;
  }
  return n;
}

const SL_ON_RE = /^(1|true|on|yes)$/, SL_OFF_RE = /^(|0|false|off|no)$/;
let _slWarned = '';
function _shortlistOn() {
  const v = _envTok('VIRAL_SHORTLIST');
  if (SL_ON_RE.test(v)) return true;
  if (!SL_OFF_RE.test(v) && _slWarned !== v) {
    _slWarned = v;
    console.log(`[ViralFewshot] 🎚️ VIRAL_SHORTLIST="${v}" อ่านไม่ออก → ถือว่าปิด (ค่าที่เปิดได้: 1/true/on/yes)`);
  }
  return false;
}
// ขนาดโผปรับได้โดยไม่ต้องแก้โค้ด (เจ้าของลอง 6/8/10 เองได้) — ค่าเริ่มต้น 8 ตามที่วัดมาแล้ว
// 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): พื้นเดิม 2 ทำให้ env ตัวเดียวพาระบบกลับไปเป็นท่าที่เจ้าของสั่งห้าม —
//   K=2 → weightedSample(2 ใบ, 2) คืนทั้งคู่เสมอ = "ครูตายตัว" (K=3 ก็ยังข้ามชั้นแค่ 27%)
//   → ยกพื้นเป็น 6 (= OWN_FLOOR 2 + อีก 4 ใบให้ตัวสุ่มมีของจริงให้เลือก) และตะโกนบอกเมื่อค่าที่ตั้งถูกดันขึ้น
const SL_K_MIN = 6, SL_K_MAX = 40, SL_K_DEFAULT = 8;
function _shortlistK() {
  const raw = _envTok('VIRAL_SHORTLIST_K');
  if (!raw) return SL_K_DEFAULT;
  const n = parseInt(raw, 10);
  if (!Number.isFinite(n)) {
    console.log(`[ViralFewshot] 🎚️ VIRAL_SHORTLIST_K="${raw}" อ่านเป็นตัวเลขไม่ได้ → ใช้ค่าเริ่มต้น ${SL_K_DEFAULT}`);
    return SL_K_DEFAULT;
  }
  if (n < SL_K_MIN) {
    console.log(`[ViralFewshot] 🎚️ VIRAL_SHORTLIST_K=${n} ต่ำกว่าพื้น ${SL_K_MIN} → บังคับใช้ ${SL_K_MIN} (โผเล็กกว่านี้ = ตัวสุ่มไม่มีของให้เลือก ครูจะตายตัว)`);
    return SL_K_MIN;
  }
  if (n > SL_K_MAX) {
    console.log(`[ViralFewshot] 🎚️ VIRAL_SHORTLIST_K=${n} เกินเพดาน ${SL_K_MAX} → บังคับใช้ ${SL_K_MAX}`);
    return SL_K_MAX;
  }
  return n;
}

// ── ค่าคงที่ของสูตร (ทุกตัวมาจากการวัดจริง ไม่ได้เดา — ดูเหตุผลรายข้อใต้ตาราง) ──
const SL = {
  OWN_FLOOR: 2,      // เกราะ 1: ต้องมีครูจากชั้นเดิมอย่างน้อย 2 ใบ
  SIGNAL_FLOOR: 0.8, // เกราะ 2: สัญญาณสูงสุดของทั้งคลังต่ำกว่านี้ = ถอยวิธีเดิมทั้งดุ้น
  REL_FLOOR: 0.45,   // เกราะ 3: ใบที่คะแนนต่ำกว่า 45% ของใบหัวโผ ไม่รับเข้าโผ (โผหดเองได้)
  CAT_BONUS: 0.8,    // โบนัสอยู่ชั้นเดียวกับที่ pickLibraryCategory เลือก
  W_THEME: 3.0, W_STRUCT: 2.2, W_EMO: 1.2, W_TONE: 0.6,
  // ⚠️ 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้ — คอมเมนต์เดิมขายเกินจริง ห้ามเชื่อคำโฆษณาเก่า):
  //   THEME_CAP "ไม่เคยตัดอะไรเลย" ในสูตรวันนี้ — พีชคณิตตายตัว: tNum บวกได้มากสุด wt×STRONG_MUL ต่อคำ
  //   ส่วน tDen บวก wt ต่อคำ ⇒ tNum/tDen ≤ STRONG_MUL = 1.6 = THEME_CAP เสมอ ⇒ Math.min ไม่มีวันทำงาน
  //   เก็บไว้เป็น "ตาข่ายกันอนาคต" เท่านั้น (ถ้าวันไหนใครขยับ STRONG_MUL ขึ้น หรือเพิ่มตัวคูณช่องธีม)
  //   🔴 ของจริงที่กัน "บัตรธีมเดียวตรงพอดีแล้วพุ่งกินโผ" ไม่มีในโค้ดนี้ — บัตรธีมเดียวที่ตรงจะได้ 1.6 เต็ม
  //      (วันนี้ยังไม่เกิดเพราะทุกบัตรมีธีมยาว ≥4 ตัวอักษร 4-5 คำ: 4 คำ 23 ใบ · 5 คำ 179 ใบ)
  //      ถ้าบรรณารักษ์เริ่มทำบัตรธีมเดียวเมื่อไหร่ ต้องกลับมาทำเพดานของจริงก่อน
  THEME_CAP: 1.6,
  STRONG_MUL: 1.6,   // ตรงในหัวข่าว/แก่นเรื่อง/แท็ก = เชื่อได้กว่าตรงในเนื้อข่าวเฉยๆ
  EMO_BODY: 0.35,    // อารมณ์ที่ตรงแค่ในเนื้อข่าว = มักบังเอิญ (เนื้อข่าวเป็นภาษารายงาน)
  MIN_LEN: 4,        // ห้ามรับคำสั้นกว่า 4 ตัวอักษรมาให้คะแนน
  GRAM: 5, GRAM_WINDOW: 2000,
};
// 【ทำไมธีมแรงสุด ×3.0】 ธีมมีคำต่างกัน 429 คำ (อารมณ์ 67 · โทน 25) = ช่องที่แยกบัตรได้ละเอียดที่สุด
// 【ทำไมอารมณ์แค่ ×1.2 ไม่ใช่ ×3 แบบสูตรเดิม】 วัดจริง 300 ข่าว: ×1.6 ได้ครู 137 ใบ · ×1.2 ได้ 142 · ×1.0 ได้ 141
//   เหตุผลเชิงเนื้อหา: อารมณ์ตัวจริงของคลัง (ซาบซึ้ง 68 ใบ, ชื่นชม 64 ใบ) แทบไม่เคยโผล่เป็นตัวอักษร
//   ในข่าวที่เขียนแบบรายงานข้อเท็จจริง · คำที่ยิงบ่อยคือ 'มั่นใจ/เข้าใจ' ซึ่งในข่าวคือคำรายงาน ("ตำรวจมั่นใจว่า")
// 【ทำไมโทนเบาสุด ×0.6】 'อบอุ่น' คำเดียวกินไป 88/202 ใบ (44%) — รู้ว่าโทนตรงแทบไม่ช่วยแยกว่าควรหยิบใบไหน
// 【ทำไมโครงเรื่องต้องอยู่ แม้แรงไม่มาก】 วัดจำนวนบัตรที่คะแนนเสมอกันเป๊ะตรงเส้นตัดอันดับ 8:
//   สูตรเต็ม 1.03 ใบ · ตัดโครงเรื่องออก 5.18 ใบ — เสมอ = ตัดสินด้วยลำดับ id = ใบที่ id มาก่อนชนะตลอดกาล
//   ซึ่งคือรากของอาการ "ครูหด" · โครงเรื่องเป็นช่องเดียวในบัตรที่แทบไม่ซ้ำกันเลย (201/202 ใบต่างกัน)
// 【ทำไมโบนัสชั้น 0.8 พอดี — วัดทั้ง 3 ค่า】 0 → ครู 149 ใบ แต่เกราะ 1 ต้องฝืนดันใบเข้าโผ 299/300 ครั้ง
//   (คะแนนกับเกราะทะเลาะกันเอง) · 1.6 → ครูหดเหลือ 98 ใบ ข้ามชั้นเหลือ 15% = ถอยกลับไปเป็นระบบเดิม
//   · 0.8 → เกราะเป็นตาข่าย ไม่ใช่ตัวสั่งการแทนคะแนน
//   🔢 นิยามคำว่า "ฝืน" (ผู้ตรวจอิสระท้วงว่าเลขเดิมตรวจย้อนไม่ได้ — วัดใหม่แล้วเขียนนิยามกำกับ):
//      ฝืน = ใบที่เกราะ 1 ดันเข้าโผ ทั้งที่ตัวมันเองไม่ติด top-K ด้วยคะแนนรวมของตัวเอง
//      วัดจริง 16 ส.ค. (ข่าวจริง 300 ใบ · เมล็ดสุ่มล็อก · เนื้อเต็ม ไม่ส่งแท็กอารมณ์):
//      เกราะ 1 หว่านลงโผ 572 ใบ จาก 293 ข่าวที่คัดโผสำเร็จ — แต่ "ฝืนจริง" แค่ 44 ใบ (15.0% ของข่าว)
//      ตัวเลขนี้ยิงซ้ำได้จาก log บรรทัด "เกราะ1 พื้นชั้นเดิม N ใบ (ฝืนคะแนนจริง M ใบ)"
// 【ทำไมไม่ใช้สถิติจากคลังข่าว 21MB】 สถิติน้ำหนักคำคิดจาก data/viral-essences.json ที่โหลดอยู่แล้วล้วน
//   → ไม่ต้องเพิ่มไฟล์ data ใหม่ = ไม่ชนกับดัก Vercel outputFileTracingIncludes (พลาดมาแล้ว 3 รอบ)
//   → บรรณารักษ์เติมบัตรใหม่เมื่อไหร่ ตารางน้ำหนักคิดใหม่เองอัตโนมัติ ไม่มีตารางค้างยุคให้ลืมอัปเดต
// 【ที่ทดลองแล้วไม่เอา — บันทึกไว้กันคนหลังเสียเวลาซ้ำ】
//   · เพดาน "ชั้นละไม่เกิน N ใบ": วัด N=4/5/6 เทียบไม่ใส่ → ครู 145/144/144 vs 144 = อยู่ในระดับความคลาดเคลื่อน ไม่ใส่
//   · ลดน้ำหนักธีมที่ตรงแค่ในเนื้อข่าว (×0.45/×0.65): ครูหดเหลือ 140 และถอยพุ่งเป็น 21-27/300 = แย่ลงทุกด้าน
//   · n-gram ตัวอักษรเป็นสัญญาณหลัก: ข่าวการเมืองไปแมตช์โพสต์ส่งเงินให้แม่เพราะรูปแบบตัวเลข 1,237,000 vs 1,400,000
//     → กันด้วยการล้างตัวเลขทิ้งก่อนหั่น 5-gram และให้โครงเรื่องเป็นตัวตัดเสมอ ไม่ใช่ตัวขับ

// 🇹🇭 กับดักภาษาไทย: ไม่เว้นวรรคระหว่างคำ → includes() ตัดกลางคำได้เสมอ
//   เกราะนี้ทำได้อย่างเดียว: ปฏิเสธตำแหน่งที่ "ตัวถัดไปเป็นสระ/วรรณยุกต์ที่ต้องเกาะตัวหน้า"
//   หรือ "ตัวก่อนหน้าเป็นสระหน้า" — สระพวกนี้ขึ้นต้นคำเองไม่ได้ ถ้าเจอแปลว่าเราตัดคำยาวมา
//   วัดผลจริงบนข่าว 300 ใบ: 'เพื่อน' 77→54 (จับ 'เพื่อนำไปใช้') · 'เด็ก' 83→62 · ตัด hit ปลอมรวม 48 ครั้ง
//   และไม่ทำลายของดีในชุดที่วัด: ครอบครัว/ช่วยเหลือ/การศึกษา/เสียสละ/สู้ชีวิต/กตัญญู/ความรัก ตัดทิ้ง 0
//   ⚠️ ขอบเขตของตัวเลขนี้ (เขียนกำกับไว้กันอ่านเกินจริง): วัดเฉพาะ "เกราะสระ เทียบกับ includes ล้วน"
//      บนข่าว 300 ใบชุดนั้น — ไม่ใช่คำรับประกันว่าจะไม่มีวันฆ่าคำดีในข่าวชุดอื่น
//   🔴 16 ส.ค. 69 — แก้คำโฆษณาเกินจริงในคอมเมนต์เดิม (ผู้ตรวจอิสระ 3 มุมยิงตรงกัน ผมรันซ้ำแล้วยืนยัน):
//      คอมเมนต์เดิมยกบทเรียน "คีย์ 'หมา' ไปแมตช์ กฎหมาย/หมายจับ" มาเป็นเหตุผลของเกราะ ทั้งที่เกราะ **ไม่กันเคสนี้**
//      _hasWord('กฎหมายอาญา','หมา') = true อยู่ดี (ตัวถัดไป 'ย' เป็นพยัญชนะ ตัวก่อน 'ฎ' ไม่ใช่สระหน้า)
//      ตัวที่กัน 'หมา' จริงคือ MIN_LEN=4 (คำ 3 ตัวอักษรไม่ถูกนำมาให้คะแนนเลย) — คนละตัวกับเกราะนี้
//      ⛔ ห้ามลดค่า SL.MIN_LEN ต่ำกว่า 4 โดยไม่ทำตัวตัดคำจริงก่อน ไม่งั้นเคส 'หมา'/'กฎหมาย' กลับมาทันที
//   ⚠️ เกราะนี้ลดปัญหา ไม่ได้ล้างปัญหา — ตัวอย่างที่ยังหลุดจริง (ตัวถัดไปเป็นพยัญชนะ):
//      _hasWord('บรรยายสรุป','ยาย') = true (ควรเป็น false เพราะ 'ยาย' อยู่กลางคำ 'บรรยาย')
//   ⚠️ ต้องนับช่วง U+0E30 ให้ครบ (ลืม 'า' = จับ 'พยายาม' ไม่ได้เลย)
//   ⚠️ กับดักที่ข้อสอบจับได้: ห้ามเหมา U+0E46 ('ๆ' ไม้ยมก) เข้ามาด้วย — มันไม่ใช่สระ มันตามหลัง "คำที่จบแล้ว"
//      ถ้าเหมาเข้ามา 'เด็กๆ' จะไม่แมตช์ธีม 'เด็ก' เลย (เขียนตามสเปกดิบครั้งแรกแล้วพลาดตรงนี้จริง)
const _THAI_FOLLOW = /[ะ-ฺๅ็-๎]/; // ะ ั า ำ ิ ี ึ ื ุ ู ฺ ๅ ็ ่ ้ ๊ ๋ ์ ํ ๎ (ไม่รวม ๆ)
const _THAI_LEAD = /[เ-ไ]/;                      // เ แ โ ใ ไ — สระหน้าขึ้นต้นคำเอง ถ้าอยู่ก่อนหน้าแปลว่าเราตัดคำยาวมา

// 🧪 16 ส.ค. 69 — เคยลองเปลี่ยนมาใช้ตัวตัดคำไทยจริง (Intl.Segmenter) แล้ว **ถอยกลับ** เพราะวัดแล้วไม่คุ้ม
//   เหตุผลที่ถอย (ผู้ตรวจอิสระวัดข่าวจริง 200 ใบ × คำในบัตร 492 คำ):
//     hit หายไป 87 · เพิ่ม 0  →  ที่ควรหาย (ตัดกลางคำจริง) ~40 · **ที่ไม่ควรหายแต่ถูกฆ่า ~47**
//     ตัวตัดคำ ICU แยกคำเพี้ยนเองในหลายเคส: 'โอกาสทางการศึกษา' → ทางการ|ศึกษา (หา 'การศึกษา' ไม่เจอ)
//     · 'เด็กชาย' → เด็กชา|ยอ|ยู่ (หา 'เด็ก' ไม่เจอ) · 'สองพี่น้อง' เป็นก้อนเดียว (หา 'พี่น้อง' ไม่เจอ)
//   ⇒ แก้ 40 พัง 47 = ไม่ได้กำไร · แถมผลตัดคำผูกกับเวอร์ชัน ICU ของเครื่อง
//     (Windows กับ Vercel Linux คนละ Node/ICU = โผอาจต่างกัน) → สร้างปัญหาใหม่แทนที่ปัญหาเก่า
//   ⚠️ ถ้าวันหน้าจะทำใหม่ ต้องมีตัวตัดคำที่พิสูจน์ได้ว่า "ฆ่าของดี 0" ก่อน ไม่ใช่แค่ "แม่นขึ้นโดยรวม"
//      และต้องนิ่งข้ามเครื่อง — เก็บบทเรียนนี้ไว้กันคนหลังเสียเวลาซ้ำ
export function _hasWord(hay, needle) {
  if (!hay || !needle) return false;
  let i = hay.indexOf(needle);
  while (i !== -1) {
    const before = i > 0 ? hay[i - 1] : '';
    const after = hay[i + needle.length] || '';
    if (!_THAI_FOLLOW.test(after) && !_THAI_LEAD.test(before)) return true;
    i = hay.indexOf(needle, i + 1);
  }
  return false;
}

// หั่นประโยคโครงเรื่องเป็นชิ้นละ 5 ตัวอักษร — วิธีเทียบโครงที่ใช้ได้กับไทยโดยไม่ต้องมีตัวตัดคำ
// ⚠️ ต้องล้างช่องว่าง/ขีดคั่น/ตัวเลขทิ้งก่อนหั่น: ประโยคโครงในบัตรใช้ '-' คั่น 3 ท่อน (เปิดด้วย…-เล่า…-จบด้วย…)
//   ถ้าไม่ล้าง ชิ้นที่คร่อมขีดจะไม่มีวันตรงกับข่าวเลย · ส่วนตัวเลขทิ้งเพราะรูปแบบตัวเลขทำให้แมตช์มั่ว
const _slClean = (s) => String(s || '').toLowerCase().replace(/[0-9๐-๙]/g, '').replace(/[\s\-—–_.,·"'()[\]]+/g, '');
function _grams(cleaned, n = SL.GRAM) {
  const out = [];
  for (let i = 0; i + n <= cleaned.length; i++) out.push(cleaned.slice(i, i + n));
  return out;
}

/**
 * ตารางน้ำหนักคำ — คิดครั้งเดียวตอนเรียกครั้งแรก (วัดจริง ~6-9 ms) แล้วแคชผูกกับก้อนบัตรที่โหลดมา
 * wgt(คำ) = ln((N+1)/(จำนวนบัตรที่ถือคำนี้+1)) × min(6, max(2, ความยาวคำ−1))
 * ทำไมต้องถ่วง: คำที่บัตรถือเหมือนๆ กันเยอะแทบไม่บอกว่าควรหยิบใบไหน — วัดจากคลังจริง
 *   'ครอบครัว' อยู่ใน 59/202 ใบ · 'ซาบซึ้ง' 68 ใบ · 'อบอุ่น' (โทน) 88 ใบ
 *   ถ้าให้คะแนนเท่ากันหมด บัตรครึ่งคลังจะขึ้นพร้อมกันแล้วตัดสินด้วยลำดับ id = ใบเดิมชนะตลอด
 * ส่วนความยาวคำใช้แทนความเฉพาะเจาะจง แต่เป็นแค่ "ตัวคูณช่วย" (min 2 สูงสุด 6) ไม่ใช่ตัวตัดสิน
 */
let _slW = null, _slWFor = null;
function _slWeights(ess) {
  if (_slW && _slWFor === ess) return _slW;
  const keys = Object.keys(ess || {});
  const N = keys.length || 1;
  const dfT = {}, dfE = {}, dfTone = {}, dfG = {};
  for (const k of keys) {
    const e = ess[k] || {};
    for (const t of new Set(_arr(e.themes).map((x) => String(x).toLowerCase()))) dfT[t] = (dfT[t] || 0) + 1;
    for (const t of new Set(_arr(e.emotion).map((x) => String(x).toLowerCase()))) dfE[t] = (dfE[t] || 0) + 1;
    if (e.tone) { const t = String(e.tone).toLowerCase(); dfTone[t] = (dfTone[t] || 0) + 1; }
    for (const g of new Set(_grams(_slClean(e.structure)))) dfG[g] = (dfG[g] || 0) + 1;
  }
  const idf = (df) => Math.log((N + 1) / ((df || 0) + 1));
  const lenF = (w) => Math.min(6, Math.max(2, w.length - 1));
  const wT = {}, wE = {}, wTone = {}, wG = {};
  for (const t in dfT) wT[t] = idf(dfT[t]) * lenF(t);
  for (const t in dfE) wE[t] = idf(dfE[t]) * lenF(t);
  for (const t in dfTone) wTone[t] = idf(dfTone[t]) * lenF(t);
  // ชิ้น 5 ตัวอักษรที่เป็นคำเชื่อมไทย (การ/ความ/ที่/ด้วย) โผล่ในทุกใบ ต้องถ่วง ไม่งั้นบัตรที่โครงยาว
  // และมีคำเชื่อมเยอะจะได้เปรียบทุกข่าวโดยไม่เกี่ยวกับเนื้อเรื่องเลย
  for (const g in dfG) wG[g] = idf(dfG[g]);
  _slW = { wT, wE, wTone, wG, N }; _slWFor = ess;
  return _slW;
}

/**
 * 🎚️ คัดครูเข้ารอบ K ใบ ข้ามชั้นได้ — โค้ดล้วน นิ่ง 100% (ข่าวเดิมได้โผเดิมเสมอ ไม่ต้องแคช)
 * export เพื่อให้ข้อสอบ/สคริปต์วัดผลยิงตรงได้ (แพตเทิร์นเดียวกับ scoreMatchExamples / weightedSample)
 * @returns {{list: object[], reason: string, fell: boolean, note: string}}
 *   list ว่าง = ให้ผู้เรียกถอยไปใช้วิธีเดิมทั้งดุ้น
 */
export function shortlistExamples(brief = {}, rows = [], essences = {}, K = 8) {
  // 🛡️ 16 ส.ค. 69 (ข้อสอบชุดใหม่จับได้ — ผู้ตรวจอิสระเคลมว่ายิงอินพุตพิการ 20 แบบไม่ล้มเลย แต่เคสนี้ล้มจริง):
  //   ค่าเริ่มต้น `brief = {}` ทำงานเฉพาะตอนส่ง undefined — ส่ง null มาตรงๆ จะล้มที่ `brief.libCat`
  //   ฟังก์ชันนี้ export ให้สคริปต์วัดผล/ข้อสอบยิงตรงได้ ⇒ ต้องทนค่า null เหมือนช่องอื่นทุกช่อง
  //   (ไม่กระทบพาริตี้: ฟังก์ชันนี้ถูกเรียกเฉพาะตอน VIRAL_SHORTLIST เปิดเท่านั้น)
  if (!brief || typeof brief !== 'object') brief = {};
  // 🛡️ 16 ส.ค. 69 (ผู้ตรวจอิสระ): K ผิดรูปต้องไม่ทำให้สัญญาในหัวข้อเพี้ยน — ผู้เรียกในไฟล์นี้กันด้วย _shortlistK แล้ว
  //   แต่ฟังก์ชันนี้ export ให้สคริปต์วัดผลยิงตรงได้ · เดิม K≤1 คืน list ว่างพร้อม fell=false + เหตุผลว่าง
  //   และ K=NaN คืนโผยาวไม่จำกัด (picked.length >= NaN เป็นเท็จเสมอ)
  const k = Number.isFinite(K) ? Math.floor(K) : SL_K_DEFAULT;
  const kUse = Math.max(2, Math.min(SL_K_MAX, k));
  // 🛡️ rows ว่าง/null (Supabase ล่ม) — ห้ามให้ Math.max ของอาเรย์ว่างคืน -Infinity แล้วไหลต่อ
  if (!Array.isArray(rows) || !rows.length) return { list: [], reason: 'ไม่มีแถวจากคลัง', fell: true, note: '' };
  const ess = essences || {};
  // 🛡️ เกราะ 3 (ได้มาฟรีจากโครงสร้าง แต่ต้อง log ให้เห็น): บัตรหาย/พัง/ว่าง → ทุกใบสัญญาณ 0 → ถอยเอง
  if (!Object.keys(ess).length) return { list: [], reason: 'บัตรลักษณะว่าง (data/viral-essences.json หาย/พัง)', fell: true, note: '' };
  const W = _slWeights(ess);
  const libCat = brief.libCat || '';

  // ฝั่งข่าว 2 ชั้น: "ช่องเชื่อถือได้" (หัวข่าว/หมวด/แท็กอารมณ์/แนว/แก่นเรื่อง) กับ "เนื้อข่าวดิบ"
  // ⚠️ ต้องอ่านแบบ optional ทุกช่อง — สายขัดเงา (correction/viralPolishService) เรียกโดยไม่ส่ง newsBrief/newsTitle
  //   🔴 แท็กอารมณ์ต้องผ่าน _tagsText เสมอ — ท่อจริงส่ง newsAnalysis?.emotionalTags ดิบๆ ถ้า AI คืนสตริงเดี่ยว
  //      .join(' ') จะโยน TypeError แล้วนักเขียน "ไม่ได้ตัวอย่างเลย" ทั้งที่ปิดสวิตช์ยังได้ 2 ใบ (ผู้ตรวจจับได้)
  const strong = [brief.title, brief.category, _tagsText(brief.emotionalTags), brief.archetype, brief.coreStory]
    .filter(Boolean).join(' ').toLowerCase();
  const body = String(brief.excerpt || '').toLowerCase();
  // ★ toLowerCase() ไม่มีผลกับอักษรไทย แต่ต้องเรียกอยู่ดี — คลังมีธีมภาษาอังกฤษจริง ('ICU', 'CPR')
  // ⚠️ ต้องหั่นทีละช่อง ห้ามต่อสตริงแล้วหั่นทีเดียว — ไม่งั้นจะเกิดชิ้นปลอมที่คร่อมรอยต่อระหว่างช่อง
  const gramSet = new Set();
  for (const part of [brief.title, brief.coreStory, String(brief.excerpt || '').slice(0, SL.GRAM_WINDOW)]) {
    for (const g of _grams(_slClean(part))) gramSet.add(g);
  }

  let noEss = 0;
  const scored = rows.map((r) => {
    const e = ess[r.id] || {};
    if (!ess[r.id]) noEss++; // บัตรใหม่ที่บรรณารักษ์ยังไม่ได้ทำ essence — ต้อง log ไม่งั้นมันไม่เคยถูกเรียกโดยไม่มีใครรู้

    // ① ธีม ×3.0 — หารด้วยน้ำหนักรวมของบัตรเอง เพราะบัตรใส่ธีมมาไม่เท่ากัน (เฉลี่ย 4.96 คำ)
    //    ถ้าไม่หาร บัตรที่ใส่ธีม 8 คำจะชนะเพราะมีตั๋วลุ้นเยอะกว่า ไม่ใช่เพราะเข้ากับข่าวกว่า
    let tNum = 0, tDen = 0; const tHits = [];
    for (const raw of _arr(e.themes)) {
      const w = String(raw || '').toLowerCase();
      if (w.length < SL.MIN_LEN) continue; // กันคำสั้นที่ "หายากในคลังแต่โหลในข่าว" (แม่/พ่อ/ยาย โผล่ในข่าว 20-40%)
      const wt = W.wT[w] || 0;
      tDen += wt;
      if (_hasWord(strong, w)) { tNum += wt * SL.STRONG_MUL; tHits.push(raw); }
      else if (_hasWord(body, w)) { tNum += wt; tHits.push(raw); }
    }
    const sTheme = tDen > 0 ? Math.min(SL.THEME_CAP, tNum / tDen) : 0;

    // ② อารมณ์ ×1.2 — ตรงในแท็ก/หัว/แก่นเรื่อง = เชื่อเต็ม · ตรงแค่ในเนื้อข่าว = 0.35 เท่า
    let eNum = 0, eDen = 0; const eHits = [];
    for (const raw of _arr(e.emotion)) {
      const w = String(raw || '').toLowerCase();
      if (w.length < SL.MIN_LEN) continue;
      const wt = W.wE[w] || 0;
      eDen += wt;
      if (_hasWord(strong, w)) { eNum += wt; eHits.push(raw); }
      else if (_hasWord(body, w)) { eNum += wt * SL.EMO_BODY; eHits.push(raw); }
    }
    const sEmo = eDen > 0 ? eNum / eDen : 0;

    // ③ โทน ×0.6 — ตัวคูณ min(1, wgt/8) ทำให้โทนที่บัตรถือกันเกลื่อน ('อบอุ่น' 88 ใบ) แทบไม่ได้คะแนน
    //    แต่โทนหายาก (ระทึก/ตัดพ้อ/ละมุน — ใบเดียวในคลัง) ยังได้เต็ม
    let sTone = 0;
    const toneW = e.tone ? String(e.tone).toLowerCase() : '';
    if (toneW && toneW.length >= SL.MIN_LEN) {
      const base = _hasWord(strong, toneW) ? 1 : (_hasWord(body, toneW) ? 0.4 : 0);
      if (base) sTone = base * Math.min(1, (W.wTone[toneW] || 0) / 8);
    }

    // ④ โครงเรื่อง ×2.2 — "ข่าวคลุมโครงของบัตรไปกี่ %" (ไม่ใช่โคไซน์: เนื้อข่าวยาวทำให้ตัวหารบวม
    //    จนค่าออกมา 0.02-0.16 ทุกใบ มีแรงแค่ ~3% ของคะแนนรวม)
    let gNum = 0, gDen = 0;
    const seen = new Set();
    for (const g of _grams(_slClean(e.structure))) {
      if (seen.has(g)) continue; seen.add(g);
      const wt = W.wG[g] || 0;
      gDen += wt;
      if (gramSet.has(g)) gNum += wt;
    }
    const sStruct = gDen > 0 ? gNum / gDen : 0;

    const signal = sTheme * SL.W_THEME + sEmo * SL.W_EMO + sTone * SL.W_TONE + sStruct * SL.W_STRUCT;
    const catHit = !!(libCat && r.category === libCat);
    return { r, signal, total: signal + (catHit ? SL.CAT_BONUS : 0), catHit, tHits, eHits, sStruct };
  }).sort((a, b) => b.total - a.total || _cmpId(a.r.id, b.r.id));
  // ↑ 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): ห้ามใช้ localeCompare ตัดเสมอในโค้ดนี้
  //   localeCompare ขึ้นกับ locale/ICU ของเครื่อง ⇒ เครื่องพัฒนา (Windows) กับ Vercel (Linux)
  //   อาจตัดเสมอคนละทาง = คำโฆษณา "ผลนิ่ง 100% ข่าวเดิมได้โผเดิม" จริงแค่ภายในเครื่องเดียว
  //   → ใช้เทียบรหัสตัวอักษรตรงๆ ซึ่งให้ผลเดียวกันทุกเครื่องทุกรันไทม์
  //   (scoreMatchExamples บรรทัด ~270 ยังใช้ localeCompare เหมือนเดิม — เป็นโค้ดเก่า ห้ามแตะในแบตช์นี้)

  // 🛡️ เกราะ 2 (เส้นคะแนนขั้นต่ำ) — 🔴 ต้องวน max ของ "signal" ทุกใบ ห้ามดูใบที่คะแนนรวมสูงสุด
  //   เพราะใบคะแนนรวมสูงสุดอาจได้โบนัสชั้น 0.8 มาฟรีแล้วสัญญาณจริงต่ำ → ระบบจะถอยทั้งที่มีใบอื่นแมชดีอยู่
  //   และถ้าเอา total มาเทียบ ทุกใบในชั้นได้ 0.8 ฟรี = เกราะหลอกที่ไม่เคยทำงานเลย
  let maxSignal = 0;
  for (const s of scored) if (s.signal > maxSignal) maxSignal = s.signal;
  const note = noEss ? ` · ⚠️ ไม่มีบัตรลักษณะ ${noEss} ใบ` : '';
  if (maxSignal < SL.SIGNAL_FLOOR) {
    return { list: [], fell: true, note, reason: `สัญญาณสูงสุดทั้งคลัง ${maxSignal.toFixed(2)} < เส้น ${SL.SIGNAL_FLOOR}` };
  }
  // เส้น 0.8 มาจากไหน: วัด 300 ข่าวเนื้อเต็ม → ถอย 7/300 (2.3%) คือเฉพาะข่าวที่หอสมุดไม่มีอะไรใกล้เคียงจริงๆ
  //   ตั้ง 0.6 → ถอย 1/300 = เกราะแทบไม่เคยทำงานจริง (เกราะที่ไม่เคยถูกยิงคือเกราะที่ไม่มีใครรู้ว่าพัง
  //   — บทเรียนเดียวกับ 'ตำราว่างเงียบๆ' 2 ส.ค.) · ตั้ง 1.0 → ถอย 23/300 และครูหดจาก 144 เหลือ 137 ใบ
  //   เคสไม่มี newsBrief (สายขัดเงา) ถอย 237/300 · เคสว่างเปล่าทุกช่อง ถอย 300/300 — ปลอดภัยทั้งคู่

  const picked = [];
  const usedId = new Set(), usedTitle = new Set();
  const perShelf = {};
  // 🐛 คลังมีครูซ้ำจริง 1 คู่ ('เคนภูภูมิเข้าวงการมา 14 ปี…' มี 2 id) — วิธีเดิมเคยส่งครู "2 ใบเป็นใบเดียวกัน"
  //   ให้นักเขียน กันซ้ำด้วย title ด้วย ไม่ใช่แค่ id (แก้ที่ต้นทางฐานข้อมูลไม่ได้ในไฟล์นี้)
  const add = (s) => {
    const t = String(s.r.title || '').trim().slice(0, 60);
    if (usedId.has(s.r.id) || (t && usedTitle.has(t))) return false;
    usedId.add(s.r.id); if (t) usedTitle.add(t);
    perShelf[s.r.category] = (perShelf[s.r.category] || 0) + 1;
    picked.push(s);
    return true;
  };

  // 🛡️ เกราะ 1 (พื้นชั้นเดิม): หยิบใบคะแนนสูงสุดของชั้น libCat ใส่โผก่อน แล้วค่อยเติมจากอันดับรวม
  //   🔴 ต้องเป็น min(2, จำนวนใบในชั้น) ไม่ใช่ 2 ตายตัว — ชั้น 'ข่าวกีฬา' และ 'คนดังตกต่ำ' มีครูชั้นละ 1 ใบจริงๆ
  //   วัดแล้วเกราะนี้ไม่มีต้นทุน: ทั้งความหลากหลายและความตรงข่าวดีขึ้นทั้งคู่เมื่อเปิดเกราะ
  const own = scored.filter((s) => s.catHit);
  const quota = Math.min(SL.OWN_FLOOR, own.length, kUse);
  for (const s of own) { if (picked.length >= quota) break; add(s); }
  const forcedIds = new Set(picked.map((p) => p.r.id));

  // เติมที่เหลือจากอันดับรวม (ข้ามใบที่ใส่ไปแล้ว) — 🛡️ เกราะ 3: ใบที่อ่อนกว่า 45% ของใบหัวโผไม่รับ
  //   (โผหดต่ำกว่า K ได้ = ทำงานถูกต้อง · วัดจริงโผเฉลี่ย 7.66 ใบ · เต็ม 8 ใบ 254/293 ข่าว)
  const relFloor = scored[0].total * SL.REL_FLOOR;
  for (const s of scored) {
    if (picked.length >= kUse) break;
    if (usedId.has(s.r.id)) continue;
    if (s.total < relFloor) continue;
    add(s);
  }
  // 🛡️ เกราะ 6 (16 ส.ค. 69 — ผู้ตรวจอิสระจับได้): โผห้ามเหลือใบเดียว
  //   เกราะ 3 (เส้นสัมพัทธ์) + กันชื่อซ้ำ ทำให้โผหดเหลือ 1 ใบได้ → weightedSample(1 ใบ, 2) คืนแค่ 1 ใบ
  //   = นักเขียนได้ครูใบเดียว ทั้งที่ทุกโหมดในไฟล์นี้สัญญาว่าให้ 2 ใบ
  //   (วัดข่าวจริง 400 ใบยังไม่เจอ — เล็กสุด 2 ใบ · เป็นความเสี่ยงเชิงโครงสร้าง ปิดไว้ก่อนดีกว่ารอให้เกิด)
  //   วิธีเติม: หยิบใบอันดับถัดไปโดยไม่สนเกราะ 3 — ยังเป็นใบที่คะแนนดีที่สุดที่เหลืออยู่เสมอ
  if (picked.length === 1) {
    for (const s of scored) {
      if (picked.length >= 2) break;
      if (usedId.has(s.r.id)) continue;
      add(s);
    }
    if (picked.length >= 2) console.log(`[ViralFewshot] 🎚️ เกราะ6: โผหดเหลือ 1 ใบ → เติมใบอันดับถัดไปโดยข้ามเกราะ 3 (โผ ${picked.length} ใบ)`);
  }

  // 🛡️ กันสัญญาเพี้ยน: list ว่าง ต้องมาคู่กับ fell=true + เหตุผลอ่านออกเสมอ (ผู้เรียกอ่าน fell เป็นตัวตัดสิน)
  if (!picked.length) return { list: [], fell: true, note, reason: 'ไม่มีใบไหนผ่านเกราะ 3 (เส้นสัมพัทธ์)' };
  // โผ 1 ใบทั้งที่ทั้งคลังมีใบอื่นไม่ได้แล้ว (คลังเล็กมากจริงๆ) = ถอยวิธีเดิมปลอดภัยกว่าส่งครูใบเดียว
  if (picked.length === 1) return { list: [], fell: true, note, reason: 'คัดได้ครูใบเดียว (ทั้งคลังไม่มีใบอื่นให้เติม) — ถอยวิธีเดิม' };

  // 🔢 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้ — log เดิมบอกเลขที่ไม่ได้วัดสิ่งที่มันพูด):
  //   ของเดิมพิมพ์ "เกราะ1 ดันเข้า N ใบ" โดยใช้จำนวนใบที่เกราะ 1 หว่านลง = เท่ากับ quota ทุกข่าว (พิมพ์ 2 ตลอด)
  //   เจ้าของอ่านแล้วจะสรุปผิดว่าเกราะทับคะแนนทุกข่าว → เปลี่ยนไปนับ "ฝืนจริง" เท่านั้น คือใบที่เกราะ 1 ดันเข้ามา
  //   ทั้งที่ตัวมันเองไม่ติดโผด้วยคะแนนรวมของตัวเอง · วิธีวัด: จำลองการคัดแบบไม่มีเกราะ 1 แล้วเทียบรายชื่อ
  const natural = new Set();
  {
    const nId = new Set(), nTitle = new Set();
    for (const s of scored) {
      if (natural.size >= kUse) break;
      if (s.total < relFloor) continue;
      const t = String(s.r.title || '').trim().slice(0, 60);
      if (nId.has(s.r.id) || (t && nTitle.has(t))) continue;
      nId.add(s.r.id); if (t) nTitle.add(t);
      natural.add(s.r.id);
    }
  }
  const forcedReal = picked.filter((p) => forcedIds.has(p.r.id) && !natural.has(p.r.id)).length;

  const why = (s) => [
    s.tHits.length ? `ธีม:${s.tHits.slice(0, 3).join(',')}` : '',
    s.eHits.length ? `อารมณ์:${s.eHits.slice(0, 2).join(',')}` : '',
    s.sStruct > 0.05 ? `โครง ${Math.round(s.sStruct * 100)}%` : '',
    forcedIds.has(s.r.id) ? 'เกราะ1' : '',
  ].filter(Boolean).join(' ') || 'ชั้นเดิม';
  const shelfTxt = Object.entries(perShelf).sort((a, b) => b[1] - a[1]).map(([c, n]) => `${c}×${n}`).join(', ');
  return {
    list: picked.map((p) => p.r),
    fell: false,
    note,
    shelfTxt,
    // forced = ใบที่เกราะ 1 หว่านลงโผ (= quota เสมอ · เก็บไว้เพื่อความเข้ากันได้ของสคริปต์เก่า)
    // forcedReal = "ฝืนจริง" ใบที่ไม่ติดโผด้วยคะแนนของตัวเอง — ตัวนี้เท่านั้นที่ log ควรพิมพ์
    forced: picked.filter((p) => forcedIds.has(p.r.id)).length,
    forcedReal,
    quota,
    // เหตุผลรายใบเป็นภาษาคนอ่านออก — เจ้าของต้องเปิด log แล้วเถียงกับระบบได้ ไม่ใช่ตัวเลขลอยๆ
    // ★ 16 ส.ค.: เติม id ย่อ 8 ตัวหน้าทุกใบ (ผู้ตรวจท้วง "log ไม่เคยบอกว่าใบไหนแข่งบ้าง" — ย้อนสอบไม่ได้)
    reason: picked.map((p, i) => `${i + 1}.[${p.r.category}] ${String(p.r.id).slice(0, 8)} ${p.total.toFixed(2)} ${why(p)}`).join(' | '),
    head: picked[0]?.total ?? 0,
    tail: picked[picked.length - 1]?.total ?? 0,
  };
}

/**
 * วิธี 1 (บรรณารักษ์ AI): luna อ่าน "เนื้อดิบจริง + แก่นเรื่อง + อารมณ์" เทียบสารบัญทั้งคลัง 202 ใบ
 * เลือก 2 ใบที่เหมาะเป็นครูสอนวิธีเล่า พร้อมเหตุผล — ข้ามพรมแดนหมวดได้ (แก้เคสจับหมวดเพี้ยนไปในตัว)
 */
async function aiMatchExamples(brief, rows, essences) {
  if (!rows?.length) return null; // ★ ผู้ตรวจจับได้: คลังว่าง = ห้ามเสียเงินยิง AI กับสารบัญเปล่า
  const catalog = rows.map((r) => {
    const e = essences[r.id] || {};
    return `${r.id} | ${r.category} | ${String(r.title).slice(0, 55)} | อารมณ์:${(e.emotion || []).join(',')} | โทน:${e.tone || '?'} | ธีม:${(e.themes || []).slice(0, 4).join(',')}`;
  }).join('\n');
  const { callAI } = await import('../ai/openai.js');
  const res = await callAI({
    // ★ ผู้ตรวจจับได้ (บทเรียนเดียวกับสมองเลือกการ์ด 201 ใบ): luna เป็น reasoning model —
    //   สารบัญใหญ่ + เพดานต่ำ = ตอบว่างเปล่า ต้องให้เพดาน 8000 (2500 ยังเคยว่าง)
    model: 'gpt-5.6-luna', temperature: 0.1, maxTokens: 8000,
    prompt: 'คุณคือบรรณารักษ์คลังโพสต์ไวรัล เลือกตัวอย่าง 2 ใบที่ "เหมาะเป็นครูสอนวิธีเล่า" ให้ข่าวนี้ที่สุด\n' +
      'เกณฑ์เรียงสำคัญ: อารมณ์ตรง > โครงเรื่อง/สถานการณ์คล้าย > ธีมตรง — ห้ามเลือกแบบไม่มีเหตุผล\n' +
      `=== ข่าวที่จะเขียน ===\nหัว: ${brief.title}\nอารมณ์: ${(brief.emotionalTags || []).join(', ')} | แนว: ${brief.archetype || '-'}\n` +
      // 🔴 16 ส.ค. 69 — คืนเพดาน .slice ตามเดิม (ผู้ตรวจอิสระ 3 มุมจับตรงกันว่ารอบก่อนถอดออกโดยไม่ได้รายงาน)
      //   เหตุผลที่คืน: (1) ไม่ใช่งาน "ชั้นวางเฉพาะกิจ" (2) มันทำให้คำอ้าง "ปิดสวิตช์ = เดิมเป๊ะ" ไม่จริงสำหรับ
      //   VIRAL_MATCH_MODE=ai (3) พรอมต์นี้แบกสารบัญ 202 บรรทัด ~38,000 ตัวอักษรอยู่แล้ว ข่าวยาวสุดในคลัง ~8,000
      //   → ปลดเพดานที่นี่ = ค่าโทเคนโหมดบรรณารักษ์โตโดยไม่มีใครเคาะ · ถ้าเจ้าของอยากให้ luna เห็นเนื้อเต็ม
      //   ให้ทำผ่านสมุดเพดานกลาง newsForStage('VIRAL_MATCH') แล้วปลดตรงนี้เป็นงานแยกที่มีคนเทสโหมด ai จริง
      `แก่นเรื่อง: ${String(brief.coreStory || '').slice(0, 250)}\nเนื้อดิบ: ${String(brief.excerpt || '').slice(0, 900)}\n` +
      `=== สารบัญคลัง (id | หมวด | ชื่อ | อารมณ์ | โทน | ธีม) ===\n${catalog}\n=== จบ ===\n` +
      'ตอบ JSON เท่านั้น: {"picks":["id1","id2"],"reason":"เหตุผลสั้น 1-2 ประโยคว่าทำไมสองใบนี้เหมาะกับข่าวนี้"}',
  });
  const ids = [...new Set(Array.isArray(res?.picks) ? res.picks.map(String) : [])]; // dedupe — luna คืน id ซ้ำได้
  const chosen = ids.map((id) => rows.find((r) => String(r.id) === id)).filter(Boolean).slice(0, 2);
  if (!chosen.length) return null;
  return { picks: chosen, reason: String(res?.reason || '').slice(0, 220) };
}

/**
 * @returns {Promise<string>} บล็อกพร้อมแปะเข้าพรอมต์ writer (Style Pack + ตัวอย่างจริง 2 โพสต์)
 *
 * ⚠️ 16 ส.ค. 69 — ตัวเลือกครูถูกเรียกจาก **3 ที่** (ผู้ตรวจอิสระจับได้ว่าเอกสารรอบก่อนเขียนว่า 2 ที่):
 *   1) summarizeServiceText.js (สายหลัก TEXT) — ส่งเนื้อข่าวเต็มผ่านสมุดเพดานกลาง newsForStage('VIRAL_MATCH')
 *   2) summarizeService.js (สายแฝด URL — ปิดอยู่ด้วย TEXT_ONLY) — ตัดเนื้อเอง .slice(0, 900) ไม่ผ่านสมุดกลาง
 *      ⇒ ข่าวเดียวกันวิ่ง 2 สายจะได้โผไม่เท่ากัน (วัดแล้วซ้อนกัน ~95%) · แก้ต้องไปแก้ที่ไฟล์นั้น ไม่ใช่ที่นี่
 *   3) correction/viralPolishService.js (สายขัดเงา ถอดสายอยู่) — เรียกโดยไม่มี newsBrief/newsTitle เลย
 *      ⇒ โค้ดในไฟล์นี้ต้องทนกรณีไม่มี newsBrief เสมอ (เทสไว้แล้ว: ถอยวิธีเดิม ไม่พัง ไม่จดสมุด)
 */
export async function getViralFewshotBlock({ category = '', emotionalTags = [], archetype = '', newsTitle = '', newsBrief = null, noHistory = false } = {}) {
  const libCat = pickLibraryCategory({ category, emotionalTags, archetype });

  let examplesBlock = '';
  try {
    // ── ขั้น 1: เอารายการโพสต์ (แคช 10 นาที) — โหมดจับคู่ต้องเห็น "ทั้งคลัง" (ข้ามหมวดได้) · โหมดเดิมเห็นแค่หมวด ──
    const mode = _matchMode();
    // 🛡️ เกราะ 4 (ลำดับความสำคัญของสวิตช์): VIRAL_MATCH_MODE เป็นสวิตช์เก่าที่เจ้าของเคาะไปแล้ว 14 ส.ค. → ชนะเสมอ
    //   ห้ามให้ 2 โหมดทำงานทับกันเงียบๆ — ถ้าตั้งชนกันต้อง log ดังๆ ว่าตัวไหนชนะ (ทั้งคู่ยังเรียกใช้ได้ครบเหมือนเดิม)
    // 🛡️ เกราะ 5 (16 ส.ค. 69 — ผู้ตรวจอิสระ 2 มุมยิงตรงกัน · ปิดรูที่อันตรายที่สุดของฟีเจอร์นี้):
    //   VIRAL_ROTATE=0 คือสวิตช์ถอยเก่า แปลว่า "หยิบ 2 ใบบนสุดตายตัว" — ถ้าปล่อยให้ทำงานคู่กับชั้นเฉพาะกิจ
    //   จะกลายเป็น "หยิบ 2 ใบคะแนนสูงสุดของโผ" = ท่า "ครูหด สำนวนจืด" ที่เจ้าของสั่งย้อน a10a40f 14 ส.ค.
    //   (พิสูจน์แล้ว: ตั้ง 2 ตัวพร้อมกัน ยิงข่าวเดิม 3 เมล็ดสุ่ม ได้ครูคู่เดิมเป๊ะทั้ง 3 ครั้ง)
    //   → ใช้หลักเดียวกับเกราะ 4: สวิตช์เก่าชนะ · ชั้นเฉพาะกิจถอยทั้งดุ้น + ตะโกนบอกว่าตัวไหนชนะ
    //   ผลพลอยได้: VIRAL_ROTATE=0 กลับไปมีความหมายเดิมเป๊ะ (ดึงแค่ 6 ใบของชั้น ไม่ใช่ 300 ใบทั้งคลัง)
    const rotate = _rotateOn();
    // 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): matchModeName ไม่ถอดเครื่องหมายคำพูด (โค้ดเก่า ห้ามแตะในแบตช์นี้)
    //   ⇒ VIRAL_MATCH_MODE="ai" (ติดอัญประกาศจาก `vercel env add`) จะอ่านได้ '' = ปิดเงียบ
    //   แล้วเกราะ 4 จะมองไม่เห็นการชนกัน → ชั้นเฉพาะกิจทำงานแทน ทั้งที่เจ้าของเชื่อว่าบรรณารักษ์เปิดอยู่
    //   ยังไม่แก้ตัวอ่าน (จะเปลี่ยนความหมายสวิตช์เก่า = งานคนละแบตช์) แต่ต้องไม่เงียบ — ตะโกนบอก
    //   🔴 รอบ 2 (ผู้ตรวจยิงซ้ำ): ห้ามเตือนค่าปิดสามัญ — 'off' คือค่าที่เอกสาร/คอมมิต 14 ส.ค. บอกให้ใช้ปิดบรรณารักษ์
    //      ของเดิมตะโกนว่า "อ่านไม่ออก" ใส่คนที่ตั้งถูกตามคู่มือ = เตือนหมาป่า คนจะเลิกเชื่อ log
    //      เตือนเฉพาะค่าที่ "ตั้งใจเปิดแต่พิมพ์เพี้ยน" เท่านั้น (เช่น "ai" ติดอัญประกาศ / AI ตัวใหญ่ / score เว้นวรรค)
    const _mmRaw = String(process.env.VIRAL_MATCH_MODE ?? '').trim();
    const _mmTok = _mmRaw.replace(/^["']|["']$/g, '').trim().toLowerCase();
    const _mmIsOffish = ['', '0', 'off', 'false', 'no', 'none'].includes(_mmTok);
    if (_mmRaw && !mode && !_mmIsOffish) {
      console.log(`[ViralFewshot] ⚠️ VIRAL_MATCH_MODE="${_mmRaw}" อ่านไม่ออก (รับเฉพาะ ai / score แบบไม่มีอัญประกาศ ตัวพิมพ์เล็ก) → ถือว่าไม่ได้ตั้ง`);
    }
    const shortlistOn = _shortlistOn() && !mode && rotate;
    if (_shortlistOn() && mode) console.log(`[ViralFewshot] 🎚️ VIRAL_SHORTLIST=1 แต่ VIRAL_MATCH_MODE=${mode} ตั้งอยู่ด้วย → โหมดจับคู่เดิมชนะ ชั้นเฉพาะกิจไม่ทำงาน`);
    else if (_shortlistOn() && !rotate) console.log('[ViralFewshot] 🎚️ VIRAL_SHORTLIST=1 แต่ VIRAL_ROTATE=0 ตั้งอยู่ด้วย → สวิตช์ถอยเก่าชนะ ชั้นเฉพาะกิจไม่ทำงาน (ROTATE=0 = หยิบตายตัว ห้ามใช้คู่กัน)');
    // 🔴 จุดตายเงียบ: ถ้าไม่นับ shortlist เป็น "เอาทั้งคลัง" ทั้ง cacheKey และ limit ระบบจะดึงมาแค่ชั้น libCat
    //   แล้วให้คะแนนกันเองในชั้นเดิม = เปิดสวิตช์แล้วเหมือนไม่มีอะไรเกิดขึ้น แต่ log บอกว่าทำงานอยู่
    //   (แคชต้องคนละคีย์ด้วย ไม่งั้นเปิด/ปิดสวิตช์สลับกันจะกินแคชผิดก้อน)
    const wide = mode || shortlistOn;
    const cacheKey = wide ? '__all__' : libCat;
    let rows = null;
    const cached = _cache.get(cacheKey);
    if (cached && cached.rows && Date.now() - cached.at < CACHE_MS) {
      rows = cached.rows;
    } else {
      const sb = getSupabase();
      if (sb) {
        let q = sb.from('viral_examples')
          .select('id, title, content, writing_notes, category, engagement_likes')
          .order('engagement_likes', { ascending: false });
        const WIDE_LIMIT = 300;
        q = wide ? q.limit(WIDE_LIMIT) : q.eq('category', libCat).limit(rotate ? 100 : 6); // ★ 8 ส.ค. 69: โหมดจับคู่/ชั้นเฉพาะกิจ=ทั้งคลัง · โหมดเดิม=ทั้งหมวด (ใหญ่สุดจริง 64 ใบ)
        const { data } = await q;
        // ⚠️ 16 ส.ค. 69 (ผู้ตรวจอิสระ — ระเบิดเวลา ยังไม่ระเบิดวันนี้เพราะคลังมี 202 ใบ):
        //   engagement_likes ในตารางเป็น 0 ทั้งคลัง ⇒ .order() เรียงจากคีย์เท่ากันหมด = Postgres ไม่รับประกันลำดับ
        //   พอคลังโตเกิน 300 ใบ "โผกว้าง" จะกลายเป็น 300 ใบที่ไม่นิ่ง และคำสัญญา "ครูทุกใบมีสิทธิ์" จะไม่จริง
        //   ยังไม่แก้เองเพราะทางแก้จริงอยู่นอกไฟล์นี้ (เติมไลก์จริงลงตาราง หรือเพิ่มคีย์เรียงรอง) — ตะโกนไว้ก่อน
        if (wide && (data || []).length >= WIDE_LIMIT) {
          console.log(`[ViralFewshot] ⚠️ คลังชนเพดานดึง ${WIDE_LIMIT} แถว — ครูบางใบเข้าไม่ถึงตัวเลือกแล้ว (และลำดับไม่นิ่งเพราะ engagement_likes เป็น 0 ทั้งตาราง) → ต้องเคาะเพดาน/คีย์เรียงใหม่`);
        }
        rows = (data || []).filter(r => (r.content || '').length > 200);
        _cache.set(cacheKey, { rows, at: Date.now() });
        if (_cache.size > 30) _cache = new Map([..._cache].slice(-15));
      }
    }

    // ★ 14 ส.ค. 69: สวิตช์สูตรแสนไลก์เปิด → ทับไลก์จริงก่อนเข้าตัวเลือก (idempotent — ทับซ้ำได้ค่าเดิม)
    rows = _applyRealLikes(rows);

    // ── ขั้น 2: เลือก 2 ใบ ──
    //   โหมดจับคู่ (เจ้าของสั่ง 8 ส.ค. "ห้ามสุ่ม"): ai → บรรณารักษ์เลือกพร้อมเหตุผล · score → คะแนนแมชนิ่งๆ
    //   ล้มทุกชั้น → ถอยหมุนเวียนแบบเดิม (fail-safe — ดีกว่าไม่มีตัวอย่างเลย และถูกจดใน history ว่า fallback)
    let picks = [];
    let pickMode = rotate ? 'rotate' : 'top2';
    let pickReason = '';
    let samplePool = null; // โผที่ weightedSample เห็นจริง (ใช้จดสมุดประวัติเฉพาะโหมดชั้นเฉพาะกิจ)
    let crossCat = false;  // ตัวอย่างที่ได้มาข้ามหมวดหรือไม่ (ใช้ตัดสินหัวบล็อก ห้ามประกาศหมวดผิดให้นักเขียน)
    if (mode === 'ai' || mode === 'score') {
      // แคชต่อข่าวแบบ "แชร์สัญญา": ทุกเวอร์ชันของข่าวเดียวกัน (รวมที่วิ่งขนานพร้อมกัน) รอผลบรรณารักษ์ก้อนเดียว
      // = จ่าย AI ครั้งเดียว/ข่าว + ได้ครูคู่เดียวกันทุกเวอร์ชัน (จับตอนเทสจริง: 2 เวอร์ชันขนานเคยเบิก 2 รอบ)
      const mKey = newsTitle ? `${String(newsTitle).slice(0, 80)}|${libCat}|${mode}` : '';
      let mEntry = mKey ? _matchCache.get(mKey) : null;
      if (!(mEntry && Date.now() - mEntry.at < MATCH_CACHE_MS)) {
        const brief = { title: newsTitle, category, emotionalTags, archetype, libCat,
          coreStory: newsBrief?.coreStory || '', excerpt: newsBrief?.excerpt || '' };
        mEntry = {
          at: Date.now(),
          promise: (async () => {
            const ess = _loadEssences();
            if (mode === 'ai') {
              try {
                const m = await aiMatchExamples(brief, rows || [], ess);
                if (m) return { picks: m.picks, reason: m.reason, mode: 'ai' };
              } catch (e) { console.log('[ViralFewshot] 🎯 ai-match ล้ม (จะถอยชั้นถัดไป):', e.message?.slice(0, 40)); }
            }
            const m2 = scoreMatchExamples(brief, rows || [], ess);
            if (m2.length) {
              return {
                picks: m2.map((x) => x.row),
                reason: m2.map((x, i) => `ใบ${i + 1} ${x.reason}`).join(' | '),
                mode: mode === 'ai' ? 'score-fallback' : 'score',
              };
            }
            return null;
          })(),
        };
        if (mKey) {
          _matchCache.set(mKey, mEntry);
          if (_matchCache.size > 40) _matchCache = new Map([..._matchCache].slice(-20));
        }
      }
      const mRes = await mEntry.promise.catch(() => null);
      if (mRes) { picks = mRes.picks; pickReason = mRes.reason; pickMode = mRes.mode; }
    }
    if (!picks.length) {
      // ★ ผู้ตรวจจับได้: fallback ในโหมดจับคู่ต้องสุ่มเฉพาะ "หมวดเดียวกับข่าว" (โผ __all__ ข้ามหมวด —
      //   สุ่มทั้งคลังอาจได้ตัวอย่างบันเทิงให้ข่าวเศร้า) · หมวดว่างจริงค่อยยอมทั้งคลัง
      const pool = wide ? (rows || []).filter((r) => r.category === libCat) : (rows || []);
      let usable = pool.length ? pool : (rows || []);
      // 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): ชั้น libCat ว่าง → usable = ทั้งคลัง = ตัวอย่าง "ข้ามหมวด"
      //   แต่หัวบล็อกข้างล่างยังประกาศว่า `หมวด "${libCat}"` = โกหกนักเขียนว่าตัวอย่างหมวดเดียวกับข่าว
      //   🔴 รอบ 2 (ผู้ตรวจยิงซ้ำ): เดิมผูกกับ `wide` ซึ่ง **รวมโหมดจับคู่เก่า (VIRAL_MATCH_MODE) ด้วย**
      //      ⇒ ของใหม่รั่วไปเปลี่ยนหัวบล็อกของโหมดเก่า ทั้งที่แบตช์นี้สัญญาว่า "ปิดสวิตช์=ไม่แตะอะไรเลย"
      //      (ผู้ตรวจทำให้เกิดบน HEAD ได้จริงด้วยข่าวหมวด 'ข่าวบันเทิง' ที่ชั้นว่าง — ไม่ใช่เคสที่เกิดไม่ได้)
      //      → ผูกกับ shortlistOn อย่างเดียว: โหมดเก่าคงพฤติกรรมเดิมเป๊ะ ของใหม่รับผิดชอบเฉพาะของใหม่
      crossCat = shortlistOn && !pool.length;
      if (mode) pickMode = 'rotate-fallback';
      // ── 🎚️ ชั้นวางเฉพาะกิจ: เปลี่ยนแค่ว่า "usable คือใคร" — ตัวสุ่มบรรทัดล่างคือบรรทัดเดิมทั้งดุ้น ──
      //   ลำดับห้ามสลับ: ดึงแถว → _applyRealLikes(rows) → คัดโผ → weightedSample
      //   (ถ้าคัดก่อนทับไลก์ ตัวสุ่มจะได้ engagement_likes = 0 ทั้ง 202 ใบ เพราะในฐานข้อมูลจริงเป็น 0 หมด
      //    แล้ว weightedSample จะกลายเป็นสุ่มเท่ากันหมด = ทิ้งน้ำหนักไลก์จริงทั้งชุดโดยไม่มีใครสังเกต)
      if (shortlistOn) {
        const K = _shortlistK();
        const sl = shortlistExamples(
          { title: newsTitle, category, emotionalTags, archetype, libCat,
            coreStory: newsBrief?.coreStory || '', excerpt: newsBrief?.excerpt || '' },
          rows || [], _loadEssences(), K,
        );
        if (sl.list.length) {
          usable = sl.list;
          pickMode = 'shortlist';
          pickReason = sl.reason;
          console.log(`[ViralFewshot] 🎚️ ชั้นเฉพาะกิจ: คัดเข้ารอบ ${sl.list.length}/${(rows || []).length} ใบ (K=${K}) · ชั้น [${sl.shelfTxt}] · เกราะ1 พื้นชั้นเดิม ${sl.quota} ใบ (ฝืนคะแนนจริง ${sl.forcedReal} ใบ) · หัวโผ ${sl.head.toFixed(2)} ท้ายโผ ${sl.tail.toFixed(2)}${sl.note}`);
          // 📖 เหตุผลรายใบเต็มๆ อยู่บรรทัดของตัวเอง ไม่ถูกตัด — โจทย์เจ้าของบังคับ "ต้องตรวจย้อนได้"
          //   (บรรทัด ✅ ข้างล่างตัดที่ 90 ตัวอักษร ห้ามแตะ ไม่งั้น log ของโหมดเก่าเปลี่ยนตาม)
          console.log(`[ViralFewshot] 🎚️ โผเต็ม: ${sl.reason}`);
        } else {
          pickMode = 'shortlist-fallback';
          pickReason = `ถอยวิธีเดิม: ${sl.reason}`;
          // 🔴 16 ส.ค. 69 (ผู้ตรวจอิสระ 3 มุมท้วงคำว่า "ถอยวิธีเดิมทั้งดุ้น" ว่าไม่จริง — และถูก):
          //   ตอนเปิดสวิตช์ rows คือทั้งคลัง 202 ใบ ⇒ _applyRealLikes คิด "ค่ากลาง" จาก 202 ใบ (ใช้เติมใบที่ไม่มีไลก์จริง)
          //   ไม่ใช่ค่ากลางของชั้นแบบวิธีเดิม → น้ำหนักสุ่มเพี้ยนได้ถึง ~31% (ข่าวการเมือง 347→239 · ช่วยเหลือกัน 288→239)
          //   แก้ให้ตรงคำ: คิดน้ำหนักไลก์ใหม่บน "เฉพาะแถวของชั้นนี้" ก่อนส่งเข้าตัวสุ่ม = เท่าวิธีเดิมจริง
          //   (_applyRealLikes อ่านไลก์จริงจาก map ตาม id ไม่ได้อ่านจากค่าที่ทับไว้ในแถว → เรียกซ้ำได้ ไม่สะสมความเพี้ยน)
          usable = _applyRealLikes(usable);
          // 🔴 16 ส.ค. 69 รอบ 2 (ผู้ตรวจยิงซ้ำ): log เดิมพิมพ์ `ในชั้น "${libCat}"` เสมอ
          //   แต่ถ้าชั้นนั้นว่าง usable = ทั้งคลัง ⇒ log โกหกแทนหัวบล็อก (แก้หัวบล็อกแล้วแต่ลืม log)
          const _poolDesc = crossCat
            ? `ทั้งคลัง ${usable.length} ใบ (ชั้น "${libCat}" ไม่มีครูสักใบ)`
            : `ในชั้น "${libCat}" ${usable.length} ใบ`;
          console.log(`[ViralFewshot] 🎚️ ชั้นเฉพาะกิจถอย — ${sl.reason} → ใช้วิธีเดิม (สุ่มถ่วงไลก์ ${_poolDesc} · คิดค่ากลางไลก์ใหม่จากก้อนนี้เท่านั้น)${sl.note}`);
        }
        samplePool = usable; // จดขนาดโผ "ที่ตัวสุ่มเห็นจริง" ลงสมุดประวัติ (ของเดิมจด 202 = อ่านผิดความหมาย)
      }
      picks = rotate ? weightedSample(usable, 2) : usable.slice(0, 2);
    }
    if (picks.length > 0) {
      // ★ ผู้ตรวจจับได้: โหมดจับคู่เลือกข้ามหมวดได้ — หัวบล็อกห้ามประกาศหมวดผิดๆ ให้นักเขียน
      const blockTitle = (pickMode === 'ai' || pickMode === 'score' || pickMode === 'score-fallback' || pickMode === 'shortlist')
        ? 'โพสต์ไวรัลจริงที่จับคู่กับข่าวนี้ (โครงเรื่อง/อารมณ์ใกล้เคียง)'
        // 🔴 ถอยแล้วได้ตัวอย่างข้ามหมวด (ชั้น libCat ว่าง) → ห้ามบอกนักเขียนว่าเป็นหมวดเดียวกับข่าว
        : crossCat
          ? 'โพสต์ไวรัลจริงจากเพจ'
          : `โพสต์ไวรัลจริงหมวด "${libCat}" จากเพจ`;
      // 📏 เพดานตัวอย่างครู — อ่านสดทุกครั้ง (ดูเหตุผลเต็มที่ exampleChars() บนหัวไฟล์)
      const _exCap = exampleChars();
      let _cutCount = 0;
      examplesBlock =
        `=== 📚 ${blockTitle} (เลียนแบบ "จังหวะ-โครง-น้ำเสียง" เท่านั้น — ห้ามลอกเนื้อหา/ชื่อ/เหตุการณ์) ===\n` +
        picks.map((r, i) => {
          const full = String(r.content || '');
          if (full.length > _exCap) _cutCount++;
          return `--- ตัวอย่าง ${i + 1} ---\n${full.slice(0, _exCap)}\n` +
            (r.writing_notes ? `(จุดที่ทำให้ไวรัล: ${String(r.writing_notes).replace(/🔥 ทำไมถึง viral:\s*/, '').slice(0, 180)})\n` : '');
        }).join('\n') +
        `=== จบตัวอย่างไวรัลจริง ===\n\n`;
      // 🔴 ตะโกนบอกเมื่อครูยังถูกตัด — บทเรียน 16 ส.ค.: ของเดิมตัด 77% ของครูเงียบๆ ไม่มีใครรู้มาเป็นเดือน
      if (_cutCount) console.log(`[ViralFewshot] 📏 ตัวอย่างครูถูกตัดที่ ${_exCap} ตัวอักษร ${_cutCount}/${picks.length} ใบ (ครูอาจสอนไม่จบเรื่อง — ขยายด้วย VIRAL_EXAMPLE_CHARS)`);
      // 🔢 16 ส.ค. 69 (ผู้ตรวจอิสระจับได้): "จากโผ N ใบ" เคยพิมพ์ขนาดคลังทั้งก้อน (202) ขณะที่สมุดประวัติจด 8
      //   → อ่าน log กับอ่านสมุดได้เลขคนละตัวสำหรับข่าวเดียวกัน · ให้ log พูดเลขเดียวกับสมุดเสมอ
      //   🔴 โหมดเก่า (samplePool = null) พิมพ์เหมือนเดิมเป๊ะทุกตัวอักษร ห้ามเปลี่ยน
      const _poolTxt = samplePool
        ? `จากโผ ${samplePool.length} ใบ (คลัง ${(rows || []).length})`
        : `จากโผ ${(rows || []).length} ใบ`;
      console.log(`[ViralFewshot] ✅ ${picks.length} ตัวอย่าง [${pickMode}] ${_poolTxt}${pickReason ? ` | เหตุผล: ${pickReason.slice(0, 90)}` : ''} (ข่าว: ${String(newsTitle || category || '?').slice(0, 40)})`);
      // 📒 8 ส.ค. 69 เจ้าของสั่ง: จดสมุดประวัติถาวร — ข่าวไหนได้ตัวอย่างใบไหน + วิธีเลือก + เหตุผล
      // ★ 16 ส.ค. 69 (ผู้ตรวจอิสระ): โหมดชั้นเฉพาะกิจจด poolSize = โผที่ตัวสุ่มเห็นจริง (8 ใบ) + libSize = คลังทั้งก้อน
      //   ของเดิมจด 202 ทั้งที่ตัวสุ่มเห็น 8 → เจ้าของเปิด ?action=pick-stats แล้วอ่านผิด
      //   🔴 โหมดเก่าคงเป๊ะเหมือนเดิม (poolSize = ขนาดก้อนที่ดึงมา ไม่มีช่อง libSize)
      await _recordPickHistory(libCat, picks, {
        poolSize: samplePool ? samplePool.length : (rows || []).length,
        libSize: samplePool ? (rows || []).length : null,
        newsTitle, noHistory, pickMode, pickReason,
      });
    } else {
      console.log(`[ViralFewshot] ⚠️ หมวด "${libCat}" ไม่มีตัวอย่างพอ — ใช้ Style Pack อย่างเดียว`);
    }
  } catch (e) {
    console.log('[ViralFewshot] ⚠️ fetch failed (non-fatal):', e.message?.slice(0, 50));
  }

  if (process.env.VIRAL_STYLE_PACK === '0') {
    console.log('[ViralFewshot] 🎨 VIRAL_STYLE_PACK=0 — ไม่ส่งสูตรบังคับ ใช้เฉพาะตัวอย่างจากหอสมุดเป็นไกด์');
    return examplesBlock;
  }
  // 18 ส.ค. 69 เจ้าของสั่งถอดสูตรบังคับ v2 (721dbf8 14 ส.ค.) + สวิตช์ HOOK_STYLE_MODE (eb6ff50 16 ส.ค.) — คืนสภาพ 11 มิ.ย.
  // เหตุผล: ให้การ์ดกับตัวอย่างไวรัลเป็นแนวทาง ห้ามสั่งทับ · กู้ของเดิม: git show 721dbf8
  return VIRAL_STYLE_PACK + examplesBlock;
}
