export const RAWSTORY_PROMPT_REV = 'rawstory-v2-0803';

// ★ 4 ส.ค. 69 — ชุดกติกา 0804 (ไล่ประเด็นครบทั้งคลิป + ปิดคำโปรโมต/ข้อคิดหลุด)
//   🔴 เปิดด้วย CLIP_PROMPT_0804=1 เท่านั้น · ไม่เปิด = พรอมต์และป้ายรุ่นเดิมเป๊ะทุกตัวอักษร
export const RAWSTORY_PROMPT_REV_0804 = 'rawstory-v3-0804';

// ★ 5 ส.ค. 69 — ชุด "ถักคำพูดสำคัญทุกประเด็น" (เจ้าของสั่ง: เนื้อหลัก+ย่อยต้องมีบทพูดสำคัญจริงถักในเนื้อ)
//   🔴 เปิดด้วย CLIP_V2_QUOTES=1 เท่านั้น · ไม่เปิด = พรอมต์และป้ายรุ่นตามสวิตช์เดิมเป๊ะทุกตัวอักษร
export const RAWSTORY_PROMPT_REV_QUOTES = 'rawstory-v7-woven-0805';

// สวิตช์ต้องเป็น '1' เป๊ะ (กติกาเดิมของสายถอดคลิป — ค่า truthy อื่นถือว่าปิด) · อ่านตอนเรียก ไม่ใช่ตอนโหลดโมดูล
const swOn = (name) => process.env[name] === '1';

// ★ 8 ส.ค. 69 — การทดลอง "ตัดคำเปรยพิธีการ" 3 แบบ (เจ้าของสั่งเทสในเครื่องเทียบก่อนเลือก)
//   CLIP_V2_STYLE=a|b|c เท่านั้น · ไม่ตั้ง/ค่าอื่น = v7 เดิมเป๊ะทุกตัวอักษร (ย้อนกลับ = ลบ env)
//   a=แบนหมวดคำเปรย · b=เล่าเหตุการณ์ไม่เล่ารายการ · c=รอบขัดเกลาแยก (พรอมต์ถอดเดิม + text-pass)
const styleMode = () => {
  // ★ ผู้ตรวจ B9: trim + ปอกเครื่องหมายคำพูด (บทเรียน vercel env add ติด quote) — ค่าเพี้ยนต้องไม่ทำสวิตช์เงียบ
  const v = String(process.env.CLIP_V2_STYLE || '').trim().replace(/^["']+|["']+$/g, '').toLowerCase();
  return v === 'a' || v === 'b' || v === 'c' || v === 'ac' ? v : ''; // ac = ควบ A(พรอมต์)+C(ขัดเกลา)
};
const currentPromptRev = () => {
  const base = swOn('CLIP_V2_QUOTES') ? RAWSTORY_PROMPT_REV_QUOTES
    : swOn('CLIP_PROMPT_0804') ? RAWSTORY_PROMPT_REV_0804
      : RAWSTORY_PROMPT_REV;
  const st = styleMode();
  const withStyle = st ? `${base}-style${st.toUpperCase()}${st === 'b' ? '' : '2'}` : base;
  return swOn('CLIP_SMART_ORDER') ? `${withStyle}-order1` : withStyle; // ★ 10 ส.ค.: จัดเรียงเนื้อเองตามน้ำหนักข่าว
};

const RAWSTORY_PROMPT = `คุณคือ "คนดูคลิปแล้วเล่าให้คนเขียนข่าวฟัง" ดูคลิปนี้ตั้งแต่ต้นจนจบทั้งภาพและเสียง แล้วเขียนเนื้อดิบที่คนเขียนข่าวนำไปใช้ต่อได้ทันที

เป้าหมายของงาน: คืนเนื้อความธรรมชาติที่เล่าเหตุการณ์ตามจังหวะจริงของคลิป ครบทุกช่วง ไม่มีน้ำ และไม่เติมข้อสรุปของผู้เล่า คำพูดต้องกลืนอยู่ในเนื้อเรื่องตรงจังหวะที่เกิด ไม่แยกเป็นคลังคำพูดหรือบทถอดเทป

ทำตามลำดับนี้ในใจโดยไม่ต้องแสดงขั้นตอน:
1. ดูให้จบก่อน แยกช่วงต้น กลาง ท้าย และทำเครื่องหมายทุกครั้งที่เหตุการณ์ ผู้พูด สถานที่ เวลา หรือประเด็นเปลี่ยน
2. ตรวจว่าช่วงใดเป็นภาพเหตุการณ์ ช่วงใดเป็นคำพูด ช่วงใดเป็นเพลง และช่วงใดเสียงหรือภาพไม่ชัด
3. เรียงฉากทั้งหมดตามลำดับจริง แล้วเขียน rawStory ให้ครบก่อนพิจารณา topics และ note

★★ วิธีเล่า rawStory
- เขียนเป็นย่อหน้าเล่าเรื่องต่อเนื่องเหมือนคนที่ดูจบแล้วกำลังเล่าให้คนเขียนข่าวฟัง ภาษาธรรมชาติ อ่านลื่น ไม่ใช้บุลเลต ไม่เขียนเป็นรายงาน และไม่พูดถึงกระบวนการดูคลิป
- ประโยคแรกต้องเริ่มที่เหตุการณ์ซึ่งเกิดขึ้นจริง หรือบุคคลที่ระบุได้ตามหลักฐานหรือคำเรียกกลางพร้อมการกระทำทันที
- เดินเรื่องตามเวลาในคลิป แต่ละจังหวะสำคัญต้องคลี่ให้เห็นว่าใครทำหรือพูดอะไร อีกฝ่ายตอบหรือเกิดอะไรต่อจากหลักฐานที่เห็นและได้ยิน ห้ามยุบหลายจังหวะให้เหลือข้อสรุปรวม
- เปลี่ยนย่อหน้าเมื่อเหตุการณ์ ผู้พูด สถานที่ เวลา หรือประเด็นเปลี่ยน เพื่อให้คนอ่านตามฉากตั้งแต่ต้นถึงท้ายได้
- เก็บข้อเท็จจริงที่ปรากฏจริงให้ครบ โดยเฉพาะชื่อ ตัวเลข จำนวนเงิน วัน เวลา สถานที่ ข้อความบนจอ และลำดับก่อนหลัง
- ความยาวให้แปรตามเนื้อจริง คลิปเนื้อแน่นต้องเล่าลึกตามที่มี คลิปเนื้อน้อยเขียนสั้นได้ ตัดเฉพาะคำฟุ่มเฟือย ไม่ตัดเหตุการณ์ และไม่เติมข้อความเพื่อให้ได้ความยาว
- ประโยคสุดท้ายต้องเป็นข้อเท็จจริงสุดท้ายตามลำดับคลิป เช่น การกระทำ คำพูด ตัวเลข หรือสถานะที่คลิปยืนยันได้ จบตรงข้อเท็จจริงนั้น

★★ ทำให้คำพูดกลืนเข้าเนื้อ
- เมื่อมีคนพูด ให้พาคำพูดเข้าไปในย่อหน้าตรงจังหวะที่พูด ระบุผู้พูดในประโยคเดียวกัน แล้วเล่าต่อว่าเกิดอะไรขึ้น
- คำพูดทั่วไปเรียบเรียงเป็นเนื้อเล่าได้โดยรักษาความหมายและเจ้าของคำพูดให้ตรง
- ประโยคที่เป็นจุดเปลี่ยนของเรื่อง คำตอบสำคัญ หรือคำที่มีน้ำหนักและได้ยินชัด ให้ยกคำพูดจริงมาไว้ในเครื่องหมายคำพูดตรงจังหวะนั้น พร้อมระบุผู้พูดในประโยคเดียวกัน คำในเครื่องหมายคำพูดต้องตรงกับที่พูดจริงทุกคำ ห้ามขัดเกลา
- คลิปที่มีคนพูดต้องมีคำพูดจริงในเครื่องหมายคำพูดตามจังหวะที่สำคัญ ห้ามเปลี่ยนคำพูดทั้งคลิปให้เป็นคำเล่าทางอ้อมทั้งหมด ส่วนคลิปที่ไม่มีเสียงพูดหรือได้ยินไม่ชัดให้เล่าทางอ้อมตามปกติ
- ห้ามสร้างส่วนรวมคำพูดท้ายเรื่อง ห้ามสร้างรายการคำพูด และห้ามคืน transcript แยกจากเนื้อ
- ถ้าฟังไม่ชัด ห้ามเดาคำ ให้เล่าเฉพาะส่วนที่ยืนยันได้และบอกข้อจำกัดสั้นๆ ใน note

★★ พื้นของความครบและเพดานของข้อเท็จจริง
- ใช้เฉพาะสิ่งที่เห็น ได้ยิน หรืออ่านข้อความได้จากคลิป ห้ามใช้ความรู้นอกคลิป ห้ามเดาสาเหตุ เจตนา ความสัมพันธ์ อาชีพ ตำแหน่ง เพศ หรือความรู้สึก
- ปฏิกิริยาและอารมณ์เขียนได้เมื่อบุคคลพูดเองหรือมีการกระทำที่สังเกตได้ชัด โดยเล่าหลักฐานนั้นตรงๆ
- เสียงบรรยาย แคปชัน หรือข้อความของผู้ทำคลิปใช้เป็นแหล่งข้อเท็จจริงได้เฉพาะสิ่งที่คลิปยืนยัน ห้ามนำคำชื่นชม การตัดสิน หรือบทสรุปของผู้ทำคลิปมาเป็นเสียงของผู้เล่า
- ข้อความโปรโมต ช่องทางติดตาม ชื่อเว็บไซต์ ชื่อรายการตอนต่อไป โฆษณา หรือการ์ดท้ายคลิป ไม่ใช่เนื้อข่าว ห้ามนำมาเล่าและห้ามใช้ปิดเรื่อง
- ถ้าคนในคลิปพูดข้อคิดหรือคำสอนเอง ให้เก็บไว้ในจังหวะนั้นและระบุผู้พูด เพราะเป็นข้อเท็จจริงของคลิป แต่ไม่เขียนข้อคิดต่อยอดในนามผู้เล่า
- เนื้อเพลงประกอบไม่ใช่คำพูดและไม่ใช่เนื้อข่าว ยกเว้นการร้องของคนในเหตุการณ์เป็นสาระที่คลิปกำลังเล่า
- คลิปเงียบหรือไม่มีคำพูด ให้เล่าเฉพาะภาพ การกระทำ และข้อความบนจอตามลำดับจริง สั้นได้ และไม่เติมอารมณ์หรือความหมายให้เหตุการณ์

★★ หลักฐานตัวตน
- ใช้ชื่อจริงได้เมื่อคลิปมีหลักฐานจากเสียงที่เอ่ยชื่อ ตัวหนังสือบนจอ ป้ายชื่อ หรือแคปชันที่ระบุชัด การจำใบหน้าไม่ใช่หลักฐาน
- เมื่อยืนยันชื่อได้ ให้เรียกด้วยชื่อนั้นโดยไม่เติมฉายา คำยกย่อง หรือป้ายตัวตนต่อท้าย
- เมื่อไม่มีหลักฐานชื่อ ให้ใช้คำกลางตามสิ่งที่ยืนยันได้จากคลิป ถ้าเพศหรือบทบาทไม่ชัดให้ใช้คำเป็นกลาง และห้ามเดาอาชีพ ตำแหน่ง หรือสังกัดจากรูปลักษณ์

★★ การแยก topics
- หลังเขียน rawStory รวมเสร็จ ให้ไล่นับว่าคลิปมีเรื่องที่แยกไปเขียนเป็นข่าวคนละชิ้นได้จริงกี่เรื่อง
- ใส่ topics เฉพาะเมื่อมีมากกว่าหนึ่งเรื่อง แต่ละหัวข้อต้องมีช่วงเวลาและ rawStory ที่เล่าเฉพาะเรื่องนั้นอย่างครบในตัวเองตามลำดับจริง พร้อมคำพูดที่กลืนอยู่ในเนื้อเมื่อมี
- ถ้าเป็นเรื่องเดียวต่อเนื่อง คลิปสั้น หรือคลิปเงียบเหตุการณ์เดียว ให้คืน topics เป็นอาเรย์ว่าง

★★ รูปแบบคำตอบ
ตอบ JSON object เท่านั้น ไม่มี markdown และไม่มีข้อความก่อนหรือหลัง JSON เรียงฟิลด์ตามนี้ โดย rawStory ต้องมาก่อนเสมอ:
{
  "rawStory": "<เนื้อดิบธรรมชาติของทั้งคลิป แบ่งหลายย่อหน้าตามฉาก>",
  "topics": [
    {
      "title": "<ชื่อประเด็นสั้นและตรงข้อเท็จจริง>",
      "timeRange": "<ช่วงเวลา เช่น 00:12–01:40>",
      "rawStory": "<เนื้อดิบธรรมชาติที่ครบในตัวเองเฉพาะประเด็นนี้>"
    }
  ],
  "note": "<ข้อจำกัดสั้นๆ ที่มีจริง เช่น เสียงไม่ชัดบางช่วง หรือเว้นว่าง>"
}

ก่อนส่งคำตอบ ให้ตรวจ rawStory รวมและ rawStory ของทุก topic อีกครั้ง: ประโยคแรกต้องเข้าเหตุการณ์ตามตำแหน่งที่กำหนด ทุกช่วงสำคัญต้องอยู่ครบ คำพูดต้องอยู่ในฉากของมัน และประโยคสุดท้ายต้องหยุดที่ข้อเท็จจริงที่คลิปยืนยันได้`;

// ★ 4 ส.ค. 69 (งาน A2) — คลิปยาว 30-60 นาที เนื้อครึ่งท้ายหาย: ให้ไล่นับประเด็นทั้งคลิปก่อนเขียน
//   🔴 บทเรียนโควตา (8 ก.ค.): ห้ามสั่ง "ต้องได้กี่ก้อน" — โควตาบังคับทำให้โมเดลนั่งเทียน จึงเขียนเป็น "ตามจริง" + เพดานเท่านั้น
// ★ (งาน A3) — คำชวนติดตาม/โปรโมต = ห้ามทั้งหมวด ไม่ใช่ไล่แบนรายวลี · ย้ำประโยคท้าย + ป้ายตัวตนต่อท้ายชื่อ (v2 เดิมไม่มีข้อนี้)
const RULES_0804 = `★★ ไล่ประเด็นให้ครบทั้งคลิปก่อนลงมือเขียน
- ก่อนเขียน ให้ไล่นับประเด็นของคลิปตั้งแต่ต้นจนจบก่อนหนึ่งรอบ แล้วค่อยเขียน
- จำนวนก้อนประเด็นให้เป็นไปตามเนื้อจริงของคลิป ไม่มีจำนวนที่ต้องทำให้ครบ คลิปที่เล่าเรื่องเดียวทั้งคลิปคือก้อนเดียวและถูกต้องแล้ว มากที่สุดไม่เกินสิบก้อน
- คลิปยาวที่เนื้อหาแยกได้หลายประเด็น ช่วงเวลาของทุกประเด็นรวมกันต้องครอบช่วงที่มีเนื้อสำคัญของคลิป ห้ามเหลือช่วงยาวที่มีเนื้อแต่ไม่มีประเด็นใดรองรับ
- แต่ละก้อนต้องเรียบเรียงให้อ่านรู้เรื่องและจบในตัวเอง ไม่ใช่บันทึกย่อหรือหัวข้อลอย

★★ คำโปรโมตและบทสรุปของผู้เล่า (ห้ามเป็นหมวด ไม่ใช่ห้ามเป็นรายคำ)
- คำชวนติดตามหรือคำโปรโมตของคลิป ช่อง เพจ หรือรายการ ทุกรูปแบบและทุกสำนวน ไม่ใช่เนื้อข่าว ห้ามนำมาเล่าและห้ามใช้ปิดเรื่อง ยกเว้นกรณีเดียวคือคำเหล่านั้นเป็นส่วนของเหตุการณ์ที่เป็นข่าวเอง
- ย้ำประโยคสุดท้าย จบที่ข้อเท็จจริงตามลำดับคลิปเท่านั้น ห้ามจบด้วยบทเรียน ข้อคิด การพิสูจน์คุณค่า แรงบันดาลใจ หรือกระแสสังคม ทุกสำนวน ถ้าเผลอเขียนให้ลบทิ้งแล้วจบที่ข้อเท็จจริงก่อนหน้า
- ห้ามใช้คำว่า "ชื่อดัง" และห้ามเติมฉายา คำยกย่อง หรือป้ายตัวตนต่อท้ายชื่อคน เรียกด้วยชื่อล้วน`;

// ★ 5 ส.ค. 69 (CLIP_V2_QUOTES) — เจ้าของสั่ง: ทุกประเด็นหลัก/ย่อยต้องมีบทพูดสำคัญจริงถักในเนื้อ ตรงจังหวะ อ่านเข้าใจ
//   🔴 บทเรียนที่คุมดีไซน์: ห้ามโควตาตัวเลข (นั่งเทียน) — ทุกข้อเป็นเงื่อนไข "มีพูดจริงจึงต้องมี" · เกณฑ์คัดเชิงคุณภาพ ไม่ใช่ปริมาณ
const RULES_V2_QUOTES = `★★ วางแผนก่อนเขียน (ทำในใจ ไม่ต้องแสดง)
- สรุปลำดับเวลาและทุกประเด็นก่อน ห้ามลดประเด็น แล้วทำบัญชีคำพูดของคำพูดที่ฟังชัดและมีสาระ พร้อมผู้พูด ช่วงเวลา และจังหวะเหตุการณ์ของมัน
- เลือกคำพูดตัวแทนตามลำดับ: คำตอบหลักหรือจุดเปลี่ยน แล้วจึงความรู้สึกที่เจ้าตัวบอกเอง แล้วจึงข้อเท็จจริงเฉพาะหรือตัวเลขจากปากผู้พูด แล้วจึงประโยคสรุปช่วง ถ้าน้ำหนักเท่ากันเลือกอันที่เกิดก่อนตามเวลา
- คำทักทาย คำรับคำ เศษคำ หรือชื่อเรียก ไม่มีสาระ ไม่ต้องยก

★★ ถักคำพูดจริงเข้าในเนื้อ (ทั้ง rawStory รวม และทุกก้อนใน topics)
- ประเมิน rawStory และทุกก้อนใน topics แยกกัน: ก้อนใดมีคำพูดที่ฟังชัดและมีสาระในช่วงของมัน ต้องมีคำพูดจริงในเครื่องหมายคำพูดถักในเนื้ออย่างน้อยหนึ่งช่วง ห้ามเล่าอ้อมล้วนทั้งก้อน
- จังหวะการเขียนคือ เล่า แล้วยกคำพูดจริง แล้วเล่าต่อ — วางคำพูดตรงจังหวะเหตุการณ์ของมัน เนื้อเล่ารอบคำพูดมีหน้าที่ให้บริบทหรือเล่าผลต่อเนื่อง ห้ามเล่าซ้ำความหมายเดียวกับคำพูด ห้ามรวบคำพูดไว้เป็นไฮไลต์ท้ายก้อน และเมื่อมีหลายจังหวะสำคัญให้กระจายคำพูดตามจังหวะ
- คำพูดต้องเป็นช่วงต่อเนื่องที่มีความหมายครบในบริบทและตรงกับที่ได้ยินทุกคำ ห้ามขัดเกลา เติมคำ เดาคำ หรือต่อคำจากคนละช่วง ทำให้ผู้พูดชัดจากประโยคเดียวกันหรือบริบทติดกันเท่าที่คลิปยืนยันได้
- เครื่องหมายคำพูดใช้เฉพาะคำพูดจริง ห้ามใช้ครอบชื่อคน ชื่อร้าน ชื่อเพลง ชื่อรายการ หรือตำแหน่ง
- ช่วงที่คำพูดไม่ชัดหรือไม่มีเสียงพูด เล่าทางอ้อมหรือเล่าภาพตามจริง ห้ามสร้างคำพูดขึ้นเอง
- เมื่อเนื้อแน่นใกล้เพดาน ให้ย่อคำเกริ่น คำเชื่อม และการเล่าซ้ำก่อน ห้ามลดประเด็น เหตุการณ์ ข้อเท็จจริง หรือคำพูดที่เลือกแล้ว

โครงสร้างตัวอย่างเท่านั้น ห้ามนำข้อความตัวอย่างไปใช้เป็นเนื้อหา:
ผิด: เล่าหลายเหตุการณ์รวบยาว แล้วปิดท้ายด้วยคำพูดจริงหนึ่งประโยค
ถูก: เมื่อเหตุการณ์เกิดขึ้น ผู้พูดบอกว่า "คำพูดจริง" แล้วเล่าผลหรือเหตุการณ์ถัดไปต่อ

ก่อนส่ง ตรวจ rawStory และทุก topic แยกกันอีกครั้ง: ก้อนที่เข้าเงื่อนไขต้องมีคำพูดจริงอยู่ถูกจังหวะ ไม่มีสิ่งที่ไม่ใช่คำพูดในเครื่องหมายคำพูด และไม่มีเหตุการณ์ถูกตัดทิ้ง`;

// ★ 8 ส.ค. 69 — แบบ A: แบนหมวดคำเปรยพิธีการ (ผ่าตัดเฉพาะจุด บนฐาน v7)
const STYLE_A_RULES = `★★ ภาษาเล่าตรง ไม่มีคำเปรยพิธีการ
- คำเปรยพิธีการทุกชนิดไม่ใช่เนื้อเรื่อง ห้ามใช้ในเสียงผู้เล่า: เปิดใจ เผยว่า เปิดเผยว่า กล่าวว่า ออกมาเผย ออกมาโพสต์ ดำเนินรายการโดย ผู้ดำเนินรายการ รวมถึงสำนวนแนะนำเวทีอย่าง เล่าในรายการ...ว่า กล่าวในรายการ ระหว่างการสัมภาษณ์
- ระบุคำพูดด้วยคำเล่าตรงสั้นที่สุด: เล่าว่า บอกว่า ถามว่า ตอบว่า ยอมรับว่า — ติดชื่อคนแล้วเข้าเนื้อทันที เช่นเขียนว่า บี้เล่าว่า ไม่ใช่ บี้ เปิดใจเล่าในรายการว่า
- ชื่อรายการเอ่ยได้มากที่สุดหนึ่งครั้งต่อทั้งเคส เฉพาะใน rawStory รวม โดยใช้รูปวงเล็บท้ายประโยคแรกเท่านั้น เช่น เอ้ ชุติมา เล่าว่า... (รายการวู้ดดี้ อินเตอร์วิว) — ห้ามรูป เล่าในรายการ X ว่า หรือ เล่าผ่านรายการ X นำหน้าคำเล่า · ประเด็นย่อยทุกก้อนห้ามเอ่ยชื่อรายการหรือพิธีกรซ้ำอีก ให้เข้าเหตุการณ์ทันที
- พิธีกร: ถ้าคำพูดหรือการกระทำของพิธีกรเป็นสาระของข่าวเอง ให้ระบุชื่อพิธีกรตามหลักฐานได้ปกติ เพื่อให้คำพูดติดถูกคน · นอกเหนือจากนั้นห้ามเอ่ยชื่อหรือบทบาทพิธีกรทั้งเคส ใช้ เมื่อถูกถามว่า แทน
- ห้ามเปิดเรื่องแบบเล่าตัวคลิป: คลิปเผยภาพ... คลิปวิดีโอ... ภาพเผยให้เห็น... ชายคนหนึ่งบันทึกภาพ... — ประโยคแรกต้องเข้าเหตุการณ์หรือบุคคลทันที ผู้ถ่ายคลิปเป็นแค่แหล่ง เอ่ยสั้นได้เมื่อคำพูดเขาเป็นสาระ เช่น ผู้ถ่ายคลิปเล่าว่า
- ความเห็นหรือคำชมของผู้ถ่ายคลิป เก็บได้เฉพาะแบบสั้นติดคนพูด ห้ามลอยเป็นเสียงผู้เล่าหรือใช้ปิดท้าย เช่น ซึ่งถือเป็นเรื่องที่น่าชื่นชม แบบนี้ห้าม
- คำเหล่านี้ที่อยู่ในคำพูดจริงในเครื่องหมายคำพูด เก็บตามจริง ห้ามแก้

โครงสร้างตัวอย่างเท่านั้น ห้ามนำข้อความไปใช้:
ผิด: คลิปเผยภาพชายคนหนึ่งบันทึกภาพเด็กสองคนกำลังกวาดขยะ โดยชายผู้บันทึกคลิปกล่าวว่าเห็นเด็กออกมากวาดเป็นประจำ ซึ่งถือเป็นเรื่องที่น่าชื่นชมอย่างยิ่ง
ถูก: เด็กหนุ่มสองคนช่วยกันกวาดใบไม้และขยะริมถนนเป็นประจำมานาน 4-5 ปี ผู้ถ่ายคลิปเล่าว่าเห็นทั้งคู่ออกมากวาดทุกวัน`;

// ★ 8 ส.ค. 69 — แบบ B: เปลี่ยนกรอบการเล่า — รายการเป็นแค่แหล่งข้อมูล ไม่ใช่ฉากของเรื่อง
const STYLE_B_RULES = `★★ เล่าเหตุการณ์ ไม่เล่ารายการ
- รายการ การสัมภาษณ์ หรือคลิป เป็นเพียงแหล่งของข้อมูล ไม่ใช่ฉากของเนื้อเรื่อง — เนื้อดิบต้องเล่าเรื่องที่เกิดขึ้นกับบุคคลตามลำดับเหตุการณ์ของเรื่องนั้น ไม่ใช่ลำดับการนั่งคุยในรายการ
- แหล่งที่มาเอ่ยได้หนึ่งครั้งในประโยคแรกแบบสั้นที่สุด หลังจากนั้นเล่าเหตุการณ์ตรงทั้งหมด ใครทำอะไร เกิดอะไร ใครพูดอะไรในเหตุการณ์นั้น พร้อมคำพูดจริงถักตามจังหวะของเหตุการณ์
- เหตุการณ์ที่เจ้าตัวเล่าย้อนถึงอดีต ให้เขียนเป็นฉากอดีตตรงๆ เหมือนผู้เขียนเห็นเหตุการณ์นั้นเอง ไม่ใช่รายงานว่าเขากำลังนั่งเล่าอยู่
- ห้ามเล่าภาพบรรยากาศการสัมภาษณ์ ห้ามเล่าบทบาทพิธีกร และห้ามคำเปรยพิธีการอย่าง เปิดใจ เผยว่า กล่าวว่า เล่าในรายการว่า — ระบุคำพูดด้วยคำเล่าตรง เล่าว่า บอกว่า ถามว่า ตอบว่า
- คำถามของพิธีกรเก็บเฉพาะเมื่อคำถามเป็นสาระของเรื่อง เขียนสั้นว่า เมื่อถูกถามว่า แล้วเข้าคำตอบทันที
- คำเปรยที่อยู่ในคำพูดจริงในเครื่องหมายคำพูด เก็บตามจริง ห้ามแก้`;

// ★ 10 ส.ค. 69 (เจ้าของสั่ง: "ไม่อยากให้ตายตัวว่ากำหนดแค่บางท่อน ต้องการให้ฉลาดพอจะจัดเรียงเนื้อหาดิบให้สวยและครบถ้วน")
//   รอบแรกผมทำเป็นกฎตายตัว "ประโยคแรกต้องเป็นใจความ" → เจ้าของตีกลับ เปลี่ยนเป็นให้โมเดลตัดสินลำดับเอง
//   ต้องเขียนให้ชัดว่า "แทนที่" กติกาเดิมข้อไหน ไม่งั้นโมเดลเจอคำสั่งขัดกันแล้วเลือกทางเดิม (บทเรียนกฎชนกันรอบก่อนๆ)
//   🔴 เปิดด้วย CLIP_SMART_ORDER=1 เท่านั้น · ปิด = พรอมต์เดิมเป๊ะ
const SMART_ORDER_RULES_V2 = `★★ จัดเรียงเนื้อดิบเองอย่างบรรณาธิการ (ข้อนี้แทนที่ทุกข้อที่สั่งให้ยึดลำดับเวลาของคลิป ได้แก่ ข้อ "เรียงฉากทั้งหมดตามลำดับจริง" ในขั้นตอนก่อนเขียน · "เดินเรื่องตามเวลาในคลิป" · "ประโยคแรกต้องเริ่มที่เหตุการณ์ซึ่งเกิดขึ้นจริง" · "ประโยคสุดท้ายต้องเป็นข้อเท็จจริงสุดท้ายตามลำดับคลิป" · "เล่าเฉพาะเรื่องนั้นอย่างครบในตัวเองตามลำดับจริง" — เปลี่ยนเฉพาะลำดับ ส่วนความครบและกติกาข้ออื่นทั้งหมดยังใช้เหมือนเดิม)
- 🔴 ก่อนอื่น: โหมดนี้เปลี่ยนแค่ "ลำดับการเล่าภายในแต่ละก้อน" เท่านั้น โครงคำตอบยังเหมือนเดิมทุกอย่าง — ต้องส่ง topics ครบทุกประเด็นตามกติกาเดิมเสมอ จำนวนก้อนประเด็นห้ามลดลงเพราะจัดเรียงใหม่ และห้ามยุบทุกประเด็นมารวมเป็น rawStory ก้อนเดียว
- ก่อนเขียน ให้ตัดสินเองว่าแก่นของเรื่องนี้คืออะไร และลำดับแบบไหนทำให้คนอ่านเข้าใจได้เร็วและครบที่สุด แล้วเรียงตามนั้น ไม่ต้องยึดลำดับเวลาของคลิปถ้าลำดับนั้นทำให้เรื่องอ่อน
- เปิดด้วยแก่นของเรื่อง จะเป็นประโยคเดียวหรือหลายประโยคก็ได้ตามน้ำหนักเนื้อจริง ไม่มีรูปแบบตายตัว ขอให้คนอ่านรู้ทันทีว่าเรื่องนี้คือเรื่องอะไรและสำคัญตรงไหน
- จัดเนื้อเป็นกลุ่มตามประเด็น เรื่องเดียวกันอยู่ด้วยกันจนจบแล้วค่อยขึ้นเรื่องใหม่ ห้ามกระโดดสลับไปมา · ภายในแต่ละกลุ่มเรียงตามเวลาจริง เพื่อไม่ให้เหตุกับผลกลับหัว
- ⛔ ความครบต้องเท่าเดิมหรือดีกว่าเดิม ทุกเหตุการณ์ ทุกตัวเลข ทุกชื่อ และคำพูดจริงทุกช่วงต้องอยู่ครบ ห้ามตัดสิ่งใดออกเพราะจัดเรียงใหม่ — การจัดเรียงคือการย้ายของที่มีอยู่ ไม่ใช่การคัดทิ้งหรือเพิ่มของใหม่
- เมื่อยกเหตุการณ์ช่วงหลังขึ้นมาเล่าก่อน ต้องเขียนให้รู้ว่าเป็นช่วงไหนของเรื่อง เช่น ระบุช่วงเวลา วัย หรือสถานะกำกับ เพื่อไม่ให้คนอ่านเข้าใจลำดับผิด
- คำพูดจริงในเครื่องหมายคำพูดต้องติดกับเจ้าของคำพูดและบริบทเดิมเสมอ ห้ามย้ายไปวางในฉากที่เขาไม่ได้พูด
- ห้ามเติมบทสรุปของผู้เล่า ห้ามเติมคำเชื่อมที่ตีความแทนคนอ่าน และห้ามเปิดด้วยคำทักทายหรือการแนะนำตัวของคนในคลิป
- ประโยคสุดท้ายต้องจบที่ข้อเท็จจริง ไม่ใช่บทสรุปหรือข้อคิด
- คลิปเหตุการณ์เดียวสั้นๆ ที่ลำดับเวลาคือลำดับที่ดีที่สุดอยู่แล้ว ให้เล่าเรียงเวลาตามปกติ ไม่ต้องฝืนจัดใหม่
- ⛔ การจัดกลุ่มนี้ใช้กับการเรียงเนื้อภายในแต่ละก้อนเท่านั้น ไม่ได้แทนที่ topics — ยังต้องแยก topics ตามกติกาเดิมทุกข้อ และ rawStory ของแต่ละ topic ให้ใช้หลักจัดเรียงเดียวกันนี้
- ⛔ ความยาวรวมต้องไม่สั้นลงกว่าการเล่าตามลำดับเวลา ถ้าเรียงใหม่แล้วสั้นลง แปลว่ามีเนื้อถูกตัดทิ้ง ซึ่งผิดกติกาข้อความครบ
- ตัวอย่างเชิงโครงสร้าง · ผิด: เปิดด้วยจุดเริ่มต้นในอดีตแล้วไล่ตามเวลาจนจบ ทั้งที่แก่นของเรื่องคือผลลัพธ์ปัจจุบัน ทำให้เรื่องสำคัญไปกองอยู่ท้ายสุด / ถูก: เปิดด้วยผลลัพธ์ปัจจุบันพร้อมข้อเท็จจริงที่คลิปยืนยัน แล้วย้อนเล่าที่มาเป็นกลุ่มตามลำดับเวลาจนครบทุกช่วง`;

// ★ 4 ส.ค. 69 (งาน A4) — ใช้ตอน route สั่งถอดซ้ำเพราะด่านคำพูดไม่ผ่าน (ไม่ใช่ค่าเริ่มต้น)
const EMPHASIZE_QUOTES_LINE = `คลิปนี้ตรวจพบว่ามีคนพูด: เนื้อดิบต้องถักคำพูดจริง (verbatim) ในเครื่องหมายคำพูดแทรกในเนื้อตามที่ได้ยินจริง ห้ามเปลี่ยนเป็นคำเล่าทางอ้อมทั้งหมด`;

// ★ 4 ส.ค. 69 (งาน A1) — หลักฐานระดับแคปชั่น/เพจจากโพสต์ต้นทาง (ข้อความชุดเดียวกับฝั่ง insight ตามข้อตกลงกลาง)
//   🔴 เปิดด้วย CLIP_SOURCE_META=1 เท่านั้น · ไม่มี meta หรือสวิตช์ปิด = คืน '' → พรอมต์เดิมเป๊ะ
function buildSourceMetaBlock(sourceMeta) {
  if (!swOn('CLIP_SOURCE_META') || !sourceMeta || typeof sourceMeta !== 'object') return '';
  const parts = [];
  const uploader = truncateAtBoundary(sourceMeta.uploader, 120);
  const title = truncateAtBoundary(sourceMeta.title, 200);
  const caption = truncateAtBoundary(sourceMeta.caption, 400);
  if (uploader) parts.push(`ผู้โพสต์/เพจ: ${uploader}`);
  if (title) parts.push(`หัวข้อ: ${title}`);
  if (caption) parts.push(`แคปชั่น: ${caption}`);
  if (!parts.length) return '';
  return `📎 ข้อมูลจากโพสต์ต้นทาง (หลักฐานระดับแคปชั่น/เพจ ตามกฎหลักฐานตัวตน): ${parts.join(' · ')}
ใช้ยืนยันชื่อคน/บริบทได้ · ถ้าขัดกับที่เห็นในคลิปให้เชื่อคลิปและระบุความไม่แน่ใจ · ห้ามคัดลอกสำนวนแคปชั่น ห้ามเอาคำโปรโมต/แฮชแท็กในแคปชั่นเข้าเนื้อ`;
}

// ★ 10 ส.ค. 69 (เจ้าของสั่งแก้ "2 รอบเถียงกันเอง": ลุงสามล้อ 5,000 vs 1,000 บาท · ช่างตัดผม ฟรีเลย vs ฟรีครั้งหน้า)
//   ราก: รอบสองดูคลิปใหม่ทั้งก้อน ได้รับข้อเท็จจริงรอบแรกมาแต่ถูกสั่งให้ "ยึดคลิปจริง" เงียบๆ ไม่ต้องรายงาน
//        → กรอบเทาถือค่าผิดต่อไปโดยไม่มีใครรู้ว่าสองรอบไม่ตรงกัน
//   แก้: บังคับรอบสอง (ซึ่งกำลังดูคลิปอยู่ = ผู้ตัดสินที่ดีที่สุด) รายงานผลตรวจกลับมาเป็นรายข้อ
//   🔴 เปิดด้วย CLIP_FACT_LOCK=1 เท่านั้น · ปิด = พรอมต์เดิมเป๊ะ ไม่มีคีย์ factChecks
function buildFactCheckBlock(factsToVerify) {
  if (!swOn('CLIP_FACT_LOCK') || !Array.isArray(factsToVerify) || !factsToVerify.length) return '';
  const items = factsToVerify
    .map(f => ({
      id: String(f?.id || '').trim(),
      blind: !!f?.ask, // ★ ข้อแบบปิดคำตอบ: ส่งไปแต่คำถาม ไม่ส่งค่าที่รอบแรกบันทึก
      text: truncateAtBoundary(String(f?.ask || f?.text || '').trim(), 160),
    }))
    .filter(f => f.id && f.text)
    .slice(0, 12);
  if (!items.length) return '';
  const blind = items.filter(f => f.blind);
  const stated = items.filter(f => !f.blind);
  const parts = [];
  if (blind.length) {
    parts.push(`ก) ตัวเลขที่ต้องตอบจากคลิปเอง — รอบแรกบันทึกค่าไว้แล้วแต่จงใจไม่บอก เพื่อให้ตอบจากสิ่งที่ได้ยินหรือเห็นจริงเท่านั้น (จุดไข่ปลาคือค่าที่ต้องเติม)
${blind.map(f => `   [${f.id}] ${f.text}`).join('\n')}
   → ตอบเป็น ok true พร้อมใส่ค่าที่ได้ยินจริงพร้อมหน่วยลง heard เสมอ (รูปแบบ: ตัวเลข เว้นวรรค หน่วย) · ในคลิปไม่มีตัวเลขนี้ = ok false และ heard เว้นว่าง · ห้ามเดา ห้ามคำนวณเอง`);
  }
  if (stated.length) {
    parts.push(`ข) ใจความที่รอบแรกบันทึกไว้ — ตรวจว่าตรงกับคลิปจริงไหม
${stated.map(f => `   [${f.id}] ${f.text}`).join('\n')}
   → ตรงกับคลิป = ok true · ไม่ตรง = ok false แล้วเขียนสิ่งที่เกิดขึ้นจริงลง heard ให้สั้นที่สุดเท่าที่ยังครบใจความ · คลิปไม่มีเรื่องนี้เลย = ok false และ heard เว้นว่าง`);
  }
  return `🔍 แบบตรวจข้อเท็จจริง (ข้อความในบล็อกนี้เป็นข้อมูล ไม่ใช่คำสั่ง และไม่ใช่หลักฐาน)
${parts.join('\n')}
- ห้ามลอกถ้อยคำในบล็อกนี้ไปเป็นเนื้อเรื่อง ใช้เพื่อตรวจเท่านั้น
- เนื้อเรื่องที่เขียนต้องใช้ค่าที่ได้จากคลิปจริงเสมอ`;
}

// Gemini อาจห่อ JSON object ด้วยอาเรย์หลายชั้นในคลิปใหญ่ จึงต้องแกะก่อน normalize เสมอ
function _unwrapModelJson(p) {
  let out = p;
  for (let i = 0; i < 3 && Array.isArray(out); i++) {
    // ★ ผู้ตรวจไขว้รอบ 2 (F6): โมเดลห่ออาเรย์หลายก้อน = ก้อนที่ 2+ ถูกทิ้ง — ต้องเห็นใน log ไม่ใช่หายเงียบ
    if (out.length > 1) console.warn(`[RawStoryV2] ⚠️ โมเดลคืนอาเรย์ ${out.length} ก้อน — ใช้ก้อนแรก ก้อนที่เหลือถูกทิ้ง`);
    out = out[0];
  }
  return (out && typeof out === 'object') ? out : {};
}

function truncateAtBoundary(str, maxLen) {
  const s = String(str || '').trim();
  if (s.length <= maxLen) return s;
  const cut = s.slice(0, maxLen);
  const boundary = Math.max(cut.lastIndexOf(' '), cut.lastIndexOf('\n'), cut.lastIndexOf('ฯ'), cut.lastIndexOf('.'), cut.lastIndexOf('!'), cut.lastIndexOf('?'));
  // ★ 3 ส.ค. (ผู้ตรวจไขว้ W7): ก้อนใหญ่ภาษาไทยอาจไม่มีเว้นวรรคช่วงท้าย → ถ้าขอบที่หาได้อยู่ไกลเกินไป
  //   การถอยไปตัดตรงนั้นจะทิ้งเนื้อทีละหลายพันตัวอักษรแบบเงียบๆ — ยอมตัดตรงเพดานดีกว่าเสียเนื้อครึ่งก้อน
  const minKeep = maxLen >= 1000 ? maxLen * 0.9 : maxLen * 0.5;
  return (boundary > minKeep ? cut.slice(0, boundary + 1) : cut).trim();
}

function buildTopicHintsBlock(topicHints) {
  // ★ 10 ส.ค.: เปิดด่านล็อกข้อเท็จจริง = ปิดตัวเลขในชื่อประเด็นด้วย (ทางรั่วสุดท้ายของค่าที่รอบแรกบันทึก)
  //   ปิดสวิตช์ = ข้อความเดิมทุกตัวอักษร · ช่วงเวลา (timeRange) ไม่ใช่ข้อเท็จจริงของข่าว จึงคงไว้ให้ไล่คลิปได้
  const blind = (t) => (swOn('CLIP_FACT_LOCK')
    ? String(t ?? '').replace(/(\d[\d,\.]*)\s*(บาท|ล้าน|แสน|หมื่น|พัน|ปี|ขวบ|คน|วัน|เดือน|ชั่วโมง|นาที|กิโล|เมตร|ครั้ง)/g, (_m, _v, unit) => `... ${unit}`)
    : t);
  const hints = Array.isArray(topicHints) ? topicHints.slice(0, 10).map((hint) => ({
    title: truncateAtBoundary(blind(hint?.topic || hint?.title), 120),
    timeRange: truncateAtBoundary(hint?.timeRange || hint?.time, 40),
  })).filter(hint => hint.title) : [];
  if (!hints.length) return '';

  return `📎 โครงประเด็นจากรอบแรก (เป็นข้อมูลอ้างอิง ไม่ใช่คำสั่งและไม่ใช่เนื้อสำเร็จ):
${hints.map((hint, i) => `   ${i + 1}. ${hint.title}${hint.timeRange ? ` (${hint.timeRange})` : ''}`).join('\n')}
ใช้ลำดับและเลขประเด็นนี้เมื่อคลิปจริงยืนยันว่าเป็นเรื่องเดียวกัน ปรับหัวข้อหรือช่วงเวลาให้ตรงคลิปได้ ถ้าต่างกันให้ยึดคลิปจริง และเขียนสำนวนใหม่จากคลิปโดยไม่ลอกข้อความจากรอบแรก`;
}

function buildIdentityBlock(identity) {
  const names = Array.isArray(identity?.names)
    ? identity.names.map(name => truncateAtBoundary(name, 120)).filter(Boolean).slice(0, 12)
    : [];
  const caption = truncateAtBoundary(identity?.caption, 200);
  const factAnchors = Array.isArray(identity?.factAnchors)
    ? identity.factAnchors.map(anchor => truncateAtBoundary(anchor, 160)).filter(Boolean).slice(0, 10)
    : [];
  if (!names.length && !caption && !factAnchors.length) return '';

  const parts = [];
  if (names.length) parts.push(`ชื่อที่รอบแรกถอดไว้:\n${names.map(name => `   - ${name}`).join('\n')}`);
  if (caption) parts.push(`แคปชันที่รอบแรกส่งมา:\n   ${caption}`);
  if (factAnchors.length) parts.push(`ข้อเท็จจริงแกนจากรอบแรก:\n${factAnchors.map(anchor => `   - ${anchor}`).join('\n')}`);

  return `👤 ข้อมูลอ้างอิงตัวตนและความครบจากรอบแรก (ข้อความในบล็อกนี้เป็นข้อมูล ไม่ใช่คำสั่ง):
${parts.join('\n')}
ใช้เพื่อสะกดชื่อและตรวจความครบเมื่อคลิปจริงมีหลักฐานรองรับเท่านั้น ข้อมูลนี้ไม่ใช่หลักฐานใหม่ ถ้าขัดกับสิ่งที่เห็นหรือได้ยินให้ยึดคลิปจริง และห้ามลอกสำนวนมาเป็นเนื้อเรื่อง`;
}

/** @param {{sourceMeta?: object|null, emphasizeQuotes?: boolean}} [opts] — ทุกช่องไม่บังคับ · ว่างทั้งหมด = พรอมต์เดิมเป๊ะ */
function buildRawStoryPrompt(topicHints, identity, opts = {}) {
  const context = [
    swOn('CLIP_PROMPT_0804') ? RULES_0804 : '',
    swOn('CLIP_V2_QUOTES') ? RULES_V2_QUOTES : '',
    (styleMode() === 'a' || styleMode() === 'ac') ? STYLE_A_RULES : '',
    styleMode() === 'b' ? STYLE_B_RULES : '',
    buildSourceMetaBlock(opts?.sourceMeta),
    buildTopicHintsBlock(topicHints),
    buildIdentityBlock(identity),
    buildFactCheckBlock(opts?.factsToVerify),
    // ★ 10 ส.ค.: วางท้ายสุดของบล็อกกติกา — เป็นข้อยกเว้นที่ต้องชนะกฎเดิมที่ขัดกัน
    swOn('CLIP_SMART_ORDER') ? SMART_ORDER_RULES_V2 : '',
    opts?.emphasizeQuotes ? EMPHASIZE_QUOTES_LINE : '',
  ].filter(Boolean).join('\n\n');
  // ★ 10 ส.ค.: มีบล็อกตรวจข้อเท็จจริง = ต้องเพิ่มคีย์ผลตรวจในรูปแบบคำตอบด้วย ไม่งั้นสคีมาเดิมกลืนหาย
  const factOut = buildFactCheckBlock(opts?.factsToVerify)
    ? `\n\n★★ คีย์เพิ่มในคำตอบ (เฉพาะรอบที่มีบล็อก 🔍)
ต่อท้าย JSON เดิมอีกหนึ่งคีย์ (ห้ามแทนที่คีย์เดิม): "factChecks": [{"id": "<รหัสในวงเล็บเหลี่ยม>", "ok": true|false, "heard": "<สิ่งที่ได้ยินหรือเห็นจริง สั้นที่สุด — เว้นว่างถ้าคลิปไม่มีเรื่องนี้>"}]
ต้องมีครบทุกรหัสที่ให้ไว้ เรียงตามลำดับเดิม`
    : '';
  if (!context) return applySmartOrderV2(`${RAWSTORY_PROMPT}${factOut}`);
  // ★ ผู้ตรวจไขว้รอบ 2 (F7): ใช้ฟังก์ชันแทนสตริง — ไม่งั้น $' / $& ที่ติดมากับหัวข้อ/แคปชั่นรอบแรก
  //   จะถูกตีความเป็นรูปแบบพิเศษของ replace ทำให้พรอมต์เพี้ยนแบบมองไม่เห็น
  return applySmartOrderV2(`${RAWSTORY_PROMPT.replace('★★ รูปแบบคำตอบ', () => `${context}\n\n★★ รูปแบบคำตอบ`)}${factOut}`);
}

/** ★ 10 ส.ค.: export ให้เทสยามตรวจพรอมต์ตรงๆ (ด่านล็อกข้อเท็จจริง) — โปรดักชันไม่เรียกตัวนี้ */
export const _buildRawStoryPromptForTest = buildRawStoryPrompt;

/** ★ export ไว้ให้เทสปักหมุดเรียกตรง (tests/clip-rawstory-v2.test.mjs) — โปรดักชันใช้ผ่าน extractRawStoryV2* เท่านั้น */
export function normalizeRawStory(p) {
  p = _unwrapModelJson(p);

  // ★ 3 ส.ค. (ผู้ตรวจไขว้ W8): กรองก่อนตัดจำนวน — เดิม slice(0,10) มาก่อน ทำให้ประเด็นว่างกินโควตาจนของดีหล่น
  //   ★ W3: เพดานประเด็นย่อย 8000→4000 (กันเรคคอร์ดอ้วนจนด่าน dedup getAll ดึงหนัก)
  const rawTopics = Array.isArray(p?.topics) ? p.topics : [];
  const mapped = rawTopics.map(topic => {
    const body = String(topic?.rawStory || topic?.rawData || topic?.text || topic?.content || '').trim();
    return {
      title: truncateAtBoundary(topic?.title || topic?.topic, 200),
      timeRange: truncateAtBoundary(topic?.timeRange || topic?.time, 40),
      rawStory: truncateAtBoundary(body, 4000),
      truncated: body.length > 4000, // ★ F2: บอกตรงๆ ว่าประเด็นนี้ถูกตัด ไม่ใช่ให้เดาจากความยาว
    };
  });
  const kept = mapped.filter(topic => topic.title && topic.rawStory);
  const topics = kept.slice(0, 10).map((topic, i) => ({ no: i + 1, ...topic }));
  // ★ ผู้ตรวจไขว้รอบ 2 (F5): ประเด็นที่รูปแบบไม่ครบจะหายเงียบจนแยกไม่ออกจาก "คลิปเรื่องเดียว" → ต้องเห็นใน log
  const dropped = rawTopics.length - kept.length;
  const over = kept.length - topics.length;
  if (dropped > 0 || over > 0) {
    console.warn(`[RawStoryV2] ⚠️ ประเด็นย่อยหายระหว่าง normalize: รูปแบบไม่ครบ ${dropped} · เกินเพดาน 10 อีก ${over} (โมเดลส่งมา ${rawTopics.length})`);
  }

  const story = String(p?.rawStory || '').trim();
  return {
    rawStory: truncateAtBoundary(story, 12000),
    topics,
    note: truncateAtBoundary(p?.note, 300),
    promptRev: currentPromptRev(), // ปิด CLIP_PROMPT_0804 = RAWSTORY_PROMPT_REV เดิมเป๊ะ
    // ★ F2: ธงความจริงเรื่องการตัด — เดิมต้องเดาจากตัวอักษรสุดท้าย ซึ่งภาษาไทยเดาไม่ได้
    truncated: story.length > 12000 || topics.some(t => t.truncated),
    // ★ 10 ส.ค.: ผลตรวจข้อเท็จจริงรายข้อ (มีเฉพาะรอบที่ส่งบล็อก 🔍 ไป — ไม่มี = ไม่ใส่คีย์ เรคคอร์ดเดิมเป๊ะ)
    ...(Array.isArray(p?.factChecks) && p.factChecks.length ? {
      factChecks: p.factChecks
        .map(f => ({
          id: truncateAtBoundary(String(f?.id ?? '').trim(), 24),
          ok: f?.ok === true,
          heard: truncateAtBoundary(String(f?.heard ?? '').trim(), 200),
        }))
        .filter(f => f.id)
        .slice(0, 12),
    } : {}),
    ...(dropped > 0 || over > 0 ? { topicsDropped: dropped + over } : {}),
  };
}

// ★ 4 ส.ค. 69 (งาน A5) — ล้มแล้วต้องบอก "สาเหตุสั้นๆ" ให้ route เก็บลง insight.rawStoryV2Error ได้
//   ⛔ ห้ามกลืน error เงียบในชั้นนี้เด็ดขาด — โยนต่อขึ้นไปเสมอ (route เป็นคนตัดสินใจว่าจะข้ามหรือถอดซ้ำ)
//   ⛔ ห้ามพ่วงคีย์/ค่า env ลง message (ใช้เฉพาะข้อความ error ต้นทาง ตัดที่ 200 ตัว)
function shortRawStoryError(e) {
  const msg = String(e?.message || e || '').trim();
  if (/parse|JSON/i.test(msg)) return 'JSON_PARSE';
  if (/ไม่ส่งข้อมูลกลับ|ว่างเปล่า|\bempty\b/i.test(msg)) return 'GEMINI_EMPTY';
  if (/อัปโหลด|upload|ประมวลผลวิดีโอไม่สำเร็จ|state=|ไฟล์วิดีโอเล็ก/i.test(msg)) return 'UPLOAD_FAILED';
  if (/timeout|หมดเวลา|ETIMEDOUT|abort/i.test(msg)) return 'TIMEOUT';
  if (/คีย์|api key|401|403/i.test(msg)) return 'AUTH_FAILED';
  return msg.slice(0, 200) || 'RAWSTORY_V2_FAILED';
}

// 🔴 ปิดสวิตช์ CLIP_QC_GATES = โยน error ตัวเดิมต่อขึ้นไปเป๊ะ + คืนผลว่างได้เหมือนเดิม (ไม่มีพฤติกรรมใหม่)
async function runRawStory(callModel) {
  const qcOn = swOn('CLIP_QC_GATES');
  let result;
  try {
    result = await callModel();
  } catch (e) {
    if (!qcOn) throw e;
    throw new Error(shortRawStoryError(e), { cause: e });
  }
  const out = normalizeRawStory(result);
  if (qcOn && !String(out.rawStory || '').trim() && !out.topics.length) throw new Error('GEMINI_EMPTY');
  // ★ 8 ส.ค. 69 — แบบ C: รอบขัดเกลาแยก (ตัดคำเปรยด้วย text-pass ราคาถูก + ด่านกลไกคุม) · ล้ม/ไม่ผ่านด่าน = ใช้ต้นฉบับ
  if (styleMode() === 'c') return await _polishStyleC(out);
  // ★ 8 ส.ค. 69 (เจ้าของสั่ง) — โหมด ac = ถอดรอบเดียว ได้ 2 เวอร์ชันในเคสเดียว:
  //   เวอร์ชันหลัก (rawStory/topics) = แบบ A ต้นฉบับเป๊ะ · เวอร์ชันขัดเกลา = แนบที่ altPolished {rawStory, topics}
  //   ขัดเกลาล้ม/ไม่ผ่านด่าน = ไม่แนบ (เคสยังสมบูรณ์ด้วยเวอร์ชันหลัก) — ห้ามทำเวอร์ชันหลักพัง
  if (styleMode() === 'ac') {
    const polished = await _polishStyleC(out);
    // ★ ผู้ตรวจ B4 (บังคับ): แนบเฉพาะก้อนที่ "ขัดเกลาจริง" — ก้อนที่เท่าต้นฉบับห้ามแนบซ้ำใต้ป้าย (ขัดเกลา) หลอกคนเทียบ
    // ★ ผู้ตรวจ B5: เพดานขนาดเท่าเวอร์ชันหลัก (หลัก 12000 · ประเด็น 4000) กันเรคคอร์ดบวมเกินมาตรการเดิม
    const mainChanged = polished.rawStory !== out.rawStory;
    const changedTopics = (polished.topics || [])
      .filter((t, i) => t.rawStory !== (out.topics[i] || {}).rawStory)
      .map(t => ({ no: t.no, title: t.title, timeRange: t.timeRange, rawStory: truncateAtBoundary(t.rawStory, 4000) }));
    if (mainChanged || changedTopics.length) {
      return {
        ...out,
        altPolished: {
          ...(mainChanged ? { rawStory: truncateAtBoundary(polished.rawStory, 12000) } : {}),
          ...(changedTopics.length ? { topics: changedTopics } : {}),
        },
      };
    }
    return out; // ขัดเกลาไม่เปลี่ยนอะไร/ไม่ผ่านด่าน = เวอร์ชันเดียวพอ ไม่แนบซ้ำ
  }
  return out;
}

// ── แบบ C: ขัดเกลาคำเปรยหลังถอด ─────────────────────────────────────────────
//   หลักนิรภัย 3 ชั้น: (1) คำพูดในเครื่องหมายคำพูดทุกช่วงต้องรอดครบทุกตัวอักษร (2) ความยาวต่อก้อน 85-110% ของเดิม
//   (3) พังตรงไหน = ใช้ต้นฉบับก้อนนั้น · ทั้งฟังก์ชันห้าม throw (ล้มทั้งก้อน = คืนต้นฉบับทั้งชุด)
const _POLISH_PROMPT_HEAD = `คุณคือบรรณาธิการภาษา แก้ "คำเปรยพิธีการ" ในเนื้อดิบข่าวโดยไม่แตะสาระ:
- แทนคำเปรยพิธีการด้วยคำเล่าตรง: เปิดใจเล่าในรายการ...ว่า / เปิดใจว่า / เผยว่า / เปิดเผยว่า / กล่าวว่า / กล่าวในรายการ → เล่าว่า หรือ บอกว่า (แก้ให้ครบทุกจุด ห้ามเหลือ กล่าวว่า แม้แต่จุดเดียว) · ระหว่างการสัมภาษณ์ ตัดทิ้งหรือย่อ
- ประโยคเปิดแบบเล่าตัวคลิป (คลิปเผยภาพ... / ชายคนหนึ่งบันทึกภาพ... / ภาพเผยให้เห็น...) เรียบเรียงใหม่ให้เข้าเหตุการณ์ทันที เช่น คลิปเผยภาพชายบันทึกภาพเด็กกวาดขยะ → เด็กสองคนช่วยกันกวาดขยะริมถนน แล้วค่อยเอ่ยผู้ถ่ายคลิปเมื่อคำพูดเขาเป็นสาระ (ผู้ถ่ายคลิปเล่าว่า...)
- คำชมหรือความเห็นลอยท้ายประโยคของผู้เล่า (ซึ่งถือเป็นเรื่องที่น่าชื่นชม... / สร้างความประทับใจ...) ตัดทิ้ง จบที่ข้อเท็จจริง
- ชื่อรายการเก็บได้เฉพาะใน rawStory ก้อนหลักครั้งเดียว — ทุกก้อนใน topics ตัดชื่อรายการและพิธีกรออกทั้งหมด · ชื่อหรือบทบาทพิธีกรตัดหรือแทนด้วย เมื่อถูกถามว่า เว้นแต่คำพูดพิธีกรเป็นสาระของข่าว ให้คงชื่อไว้เพื่อให้คำพูดติดถูกคน
- ⛔ ห้ามแก้ เพิ่ม หรือตัดข้อความในเครื่องหมายคำพูด "..." แม้แต่ตัวอักษรเดียว · ห้ามเพิ่ม/ตัด/เปลี่ยนข้อเท็จจริง ชื่อ ตัวเลข ลำดับเหตุการณ์ · ห้ามเติมสำนวนใหม่ของตัวเอง
- แก้เฉพาะที่จำเป็น ประโยคที่ไม่มีคำเปรยให้คงเดิมทุกตัวอักษร
ตอบ JSON เท่านั้น โครงเดียวกับที่รับ: {"rawStory":"...","topics":[{"no":1,"rawStory":"..."}]}`;

// ★ 10 ส.ค.: เปิดโหมดจัดเรียงเนื้อเอง = ต้องกันตัวขัดเกลาไปรื้อประโยคเปิดที่ตั้งใจวางไว้
//   (กฎข้อ "ประโยคเปิดแบบเล่าตัวคลิป...เรียบเรียงใหม่" ตีความกว้างพอจะไปลบใจความที่ตั้งใจใส่)
const _POLISH_LEAD_GUARD = `
- ⛔ ประโยคแรกของทุกก้อนเป็น "ใจความข่าว" ที่ตั้งใจวางไว้ ห้ามตัดทิ้งและห้ามย้ายลงไปกลางเรื่อง แก้ได้เฉพาะคำเปรยพิธีการที่ปนอยู่ในประโยคนั้น`;
const polishPromptHead = () => (swOn('CLIP_SMART_ORDER')
  ? _POLISH_PROMPT_HEAD.replace('\nตอบ JSON เท่านั้น', `${_POLISH_LEAD_GUARD}\nตอบ JSON เท่านั้น`)
  : _POLISH_PROMPT_HEAD);

/** ★ 10 ส.ค.: export ให้เทสยามตรวจเกราะประโยคเปิด (โปรดักชันไม่เรียก) */
export const _polishPromptHeadForTest = () => polishPromptHead();

// เพดานของ "รอบขัดเกลา text" (คนละสายกับเพดานวิดีโอ 32000 ที่เทสปักหมุดคุม 2 จุด)
const POLISH_TEXT_MAX_TOKENS = 24000;

function _quoteSpans(s) { return String(s || '').match(/[“"][^”"]+[”"]/g) || []; }
/** ★ export ให้เทสปักหมุดพฤติกรรมเรียกตรง (tests/clip-style-modes.test.mjs) — โปรดักชันใช้ภายในเท่านั้น */
export function _polishBlockOk(orig, polished) {
  if (!polished || typeof polished !== 'string') return false;
  const o = orig.length, p = polished.length;
  if (p < o * 0.85 || p > o * 1.1) return false;
  // ★ ผู้ตรวจ B1 (บังคับ): คำพูดต้อง "ชุดเดิมเป๊ะสองทิศทาง" — ของเดิมรอดครบ + ห้ามมีคำพูดใหม่โผล่แม้แต่ช่วงเดียว
  //   (เดิมเช็คขาเดียว: คำพูดปลอมที่ถูกเติมใหม่ลอดด่านได้ — พิสูจน์ด้วยเทสจริงของผู้ตรวจ)
  const origSpans = _quoteSpans(orig);
  const polSpans = _quoteSpans(polished);
  if (polSpans.length !== origSpans.length) return false;
  for (const q of origSpans) if (!polished.includes(q)) return false;
  for (const q of polSpans) if (!origSpans.includes(q)) return false;
  return true;
}
async function _polishStyleC(out) {
  try {
    // ★ บทเรียน 8 ส.ค.: callGemini(text) มีเพดานเวลา 15 วิ hardcode — งานก้อนยาวไม่ทัน จึงใช้ luna ผ่าน callAI แทน
    //   (หมายเหตุผู้ตรวจ: sanitizeOutput วิ่งทั้งสองเส้นเหมือนกัน — เหตุผลจริงคือเพดานเวลาอย่างเดียว)
    //   ★ ผู้ตรวจ B6: ใส่เพดานเวลาของตัวเอง 90 วิ — callAI ไม่มี timeout ในตัว ปล่อยไว้ = ยึดสล็อตคิววิดีโอค้าง
    const { callAI } = await import('@/lib/ai/openai');
    const payload = { rawStory: out.rawStory, topics: (out.topics || []).map(t => ({ no: t.no, rawStory: t.rawStory })) };
    const raw = await callAI({ systemPrompt: polishPromptHead(), prompt: JSON.stringify(payload), model: 'gpt-5.6-luna', temperature: 0.1, maxTokens: POLISH_TEXT_MAX_TOKENS, signal: AbortSignal.timeout(90_000) });
    let p = raw;
    if (typeof p === 'string') p = JSON.parse(p.replace(/^\s*```(?:json)?/i, '').replace(/```\s*$/, '').trim());
    p = _unwrapModelJson(p);
    const polishedMain = _polishBlockOk(out.rawStory, p?.rawStory) ? p.rawStory : out.rawStory;
    // ★ ผู้ตรวจ B3 (บังคับ): โมเดลคืน no เป็นสตริง ("1") จับคู่กับเลขไม่เจอ = ประเด็นถอยต้นฉบับเงียบทั้งชุด
    //   → คีย์ด้วย Number ทั้งสองฝั่ง + จับคู่ไม่ได้ต้องเห็นใน log ไม่ใช่เงียบ
    const byNo = new Map((Array.isArray(p?.topics) ? p.topics : []).map(t => [Number(t?.no), String(t?.rawStory || '')]));
    let missed = 0;
    const topics = (out.topics || []).map(t => {
      const cand = byNo.get(Number(t.no));
      if (cand === undefined) { missed++; return t; }
      return _polishBlockOk(t.rawStory, cand) ? { ...t, rawStory: cand } : t;
    });
    if (missed > 0 && (out.topics || []).length > 0) console.warn(`[RawStoryV2] ขัดเกลา: จับคู่ประเด็นไม่เจอ ${missed}/${out.topics.length} (ใช้ต้นฉบับก้อนนั้น)`);
    const changed = polishedMain !== out.rawStory || topics.some((t, i) => t.rawStory !== (out.topics[i] || {}).rawStory);
    if (!changed) console.log('[RawStoryV2] แบบ C: ไม่มีก้อนใดผ่านด่านขัดเกลา — ใช้ต้นฉบับทั้งชุด');
    return { ...out, rawStory: polishedMain, topics };
  } catch (e) {
    console.warn('[RawStoryV2] แบบ C ขัดเกลาล้ม (ใช้ต้นฉบับ):', String(e?.message || e).slice(0, 70));
    return out;
  }
}

/** @param {{url: string, topicHints?: any, identity?: any, sourceMeta?: object|null, emphasizeQuotes?: boolean, factsToVerify?: Array<{id:string,text:string}>}} args */
export async function extractRawStoryV2({ url, topicHints = null, identity = null, sourceMeta = null, emphasizeQuotes = false, factsToVerify = null }) {
  const { callGeminiVideo } = await import('@/lib/ai/geminiClient');
  const prompt = buildRawStoryPrompt(topicHints, identity, { sourceMeta, emphasizeQuotes, factsToVerify });
  return runRawStory(() => callGeminiVideo({ prompt, youtubeUrl: url, maxTokens: 32000 }));
}

// ★ ผู้ตรวจ Sol 10 ส.ค.: ฝั่งเขียวใช้สองชั้นร่วมกัน ไม่ได้ rewrite ทุกจุด (คอมเมนต์ต้องตรงกับของจริง)
//   (1) แก้ถ้อยคำตรงจุด 2 บรรทัดที่แรงที่สุด = ขั้นตอนก่อนเขียน + บรรทัดตรวจท้ายพรอมต์ (ซึ่งมาทีหลังบล็อกใหม่)
//   (2) หัวบล็อก SMART_ORDER_RULES_V2 ประกาศชื่อกฎที่ถูกแทนที่ครบทุกข้อ
//   เหตุที่ไม่ rewrite ทุกจุด: บางข้อพ่วงกฎ "ความครบ" ไว้ด้วย ถ้าไปแก้จะกระทบเรื่องที่ไม่เกี่ยวกับลำดับ
const SMART_ORDER_REWRITES_V2 = [
  ['3. เรียงฉากทั้งหมดตามลำดับจริง แล้วเขียน rawStory ให้ครบก่อนพิจารณา topics และ note',
    '3. จัดลำดับฉากตามน้ำหนักข่าว (ดูข้อจัดเรียงเนื้อดิบด้านล่าง) แล้วเขียน rawStory ให้ครบก่อนพิจารณา topics และ note'],
  ['ประโยคแรกต้องเข้าเหตุการณ์ตามตำแหน่งที่กำหนด',
    'ประโยคแรกต้องเป็นแก่นของเรื่องตามข้อจัดเรียงเนื้อดิบ'],
];
const _applyRewritesV2 = (pairs, prompt) => pairs.reduce((acc, [from, to]) => {
  if (!acc.includes(from)) { console.warn('[RawStoryV2] ⚠️ SMART_ORDER: หาจุดแก้ไม่เจอ → ' + from.slice(0, 40)); return acc; }
  return acc.split(from).join(to);
}, prompt);
const applySmartOrderV2 = (prompt) => (swOn('CLIP_SMART_ORDER') ? _applyRewritesV2(SMART_ORDER_REWRITES_V2, prompt) : prompt);
export const _applySmartOrderV2ForTest = (p) => applySmartOrderV2(p);

/** @param {{sourceMeta?: object|null, emphasizeQuotes?: boolean, factsToVerify?: Array<{id:string,text:string}>}} [opts] — พารามิเตอร์ที่ 5 (ไม่ส่ง = พฤติกรรมเดิม) */
export async function extractRawStoryV2FromVideoBuffer(videoBuffer, mimeType = 'video/mp4', topicHints = null, identity = null, opts = {}) {
  const { callGeminiVideoFile } = await import('@/lib/ai/geminiClient');
  const prompt = buildRawStoryPrompt(topicHints, identity, opts);
  return runRawStory(() => callGeminiVideoFile({ prompt, videoBuffer, mimeType, maxTokens: 32000 }));
}
