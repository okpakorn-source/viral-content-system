# 🎛️ แผงสวิตช์ยุคระบบเลือกครูไวรัล — เปิด/ปิด/ย้อนยุคได้ด้วย env ล้วนๆ ไม่ต้องแก้โค้ด

> อัปเดต 14 ส.ค. 69 · ทุกยุคอยู่ในโค้ดตัวเดียวกัน สลับด้วยตัวแปร Environment บน Vercel (ตั้งแล้วต้อง **Redeploy 1 ครั้ง** เสมอ)
> คีย์เวิร์ดสั่งงาน AI: พูดชื่อยุคได้เลย เช่น *"ย้อนยุค 2 เดือน"*, *"เปิดบรรณารักษ์"*, *"ปิดสูตรแสนไลก์"*

## ตารางยุคทั้งหมด

| ยุค (คีย์เวิร์ด) | ช่วงที่เคยใช้จริง | พฤติกรรม | วิธีเปิด | เช็คว่าทำงาน (สมุด pick-history) |
|---|---|---|---|---|
| **ยุค 2 เดือน** (ครูตายตัว) | 11 มิ.ย. – 8 ส.ค. | หยิบ 2 ใบแรกของหมวดตายตัว ใบเดิมซ้ำทุกข่าว | `VIRAL_ROTATE=0` | `mode=top2` + โผ=6 |
| **สุ่มทั้งหมวด สูตรเดิม** | 8–10 ส.ค. + 14 ส.ค. กลางวัน | สุ่มถ่วงไลก์ทั้งหมวด 2 เวอร์ชันคนละคู่ + แพ็ค v1 + 4-5 ย่อหน้า | `VIRAL_HITS_FORMULA=0` | `mode=rotate` + เปิดหัวหลากท่า 4-5 ย่อหน้า |
| **บรรณารักษ์** (AI จับคู่) | 10–14 ส.ค. (ถูกถอด — ครูหด สำนวนจืด) | luna อ่านเนื้อข่าว+สารบัญ 202 เลือก 2 ใบพร้อมเหตุผล | `VIRAL_MATCH_MODE=ai` (หรือ `score`=คะแนนโค้ด) | `mode=ai` + โผ~202 |
| **สูตรแสนไลก์** ⭐ตัวจริงปัจจุบัน (เจ้าของเคาะ 14 ส.ค. ค่ำ) | 14 ส.ค. ค่ำ → ตอนนี้ | สุ่มทั้งหมวด **+** แพ็ค v2 (เปิดตัวเลขนำ/ห้ามเปิดคำพูด/แบนบอกความรู้สึก/จบประโยคนิยาม) **+** ล็อก 3 ย่อหน้า 250-350 คำ **+** ไลก์จริงถ่วงการหยิบ (median+sqrt ทุกใบมีสิทธิ์) | **ไม่ตั้งอะไรเลย** (ค่าเริ่มต้น) | เนื้อ 3 ย่อหน้า + เปิดตัวเลข/ภาพ + log `💗 ไลก์จริง` |

## หลักการรวมสวิตช์ (ตอบ: "ตัวใหม่ไม่เวิร์คย้อนยุค 2 เดือนได้ไหม" → ได้ ทุกทาง)

- สวิตช์ทุกตัว **อิสระต่อกัน** และ **ไม่ตั้ง = ตัวจริงปัจจุบันเสมอ**
- ตัวอย่างการย้อน: สูตรแสนไลก์ไม่เวิร์ค → ลบ `VIRAL_HITS_FORMULA` = กลับตัวจริงทันที · อยากไปไกลกว่านั้น → ตั้ง `VIRAL_ROTATE=0` เพิ่ม = กลับยุค 2 เดือน
- ⚠️ ข้อจำกัด combo (ผู้ตรวจอิสระ S4): `VIRAL_HITS_FORMULA=1` + `VIRAL_ROTATE=0` = ได้แพ็ค v2 + 3 ย่อหน้า แต่**ไลก์จริงไม่มีผลกับการหยิบ** (สาย top2 ไม่ re-sort หลังทับ) — แนะนำเปลี่ยนทีละตัว

## สวิตช์เสริมที่เกี่ยวข้อง
| สวิตช์ | ทำอะไร | ค่าปัจจุบัน |
|---|---|---|
| `MODEL_BREAKDOWN=gpt-5.6-terra` | ขั้น 2 (แตกประเด็น) กลับไปใช้ terra แบบยุค 16 ก.ค.–14 ส.ค. (เร็วกว่า 2 เท่า ถูกกว่าครึ่ง แต่ประเด็นย่อยน้อยกว่า 8→5) | ไม่ตั้ง = **gpt-5.5** (เจ้าของเคาะ 15 ส.ค. "เล่าดีกว่าทุกมุม") |
| `MODEL_BLUEPRINT=gpt-5.6-luna` | ขั้น 3 (วางโครงอารมณ์) กลับไปใช้ luna ตัวเดิม — ถูกกว่า 5.2 เท่า (0.43 vs 2.26 บาท/ข่าว) เร็วกว่า 2 เท่า แต่สะพาน/ข้อห้ามอ่อนกว่า | ไม่ตั้ง = **gpt-5.6-sol** (เจ้าของเคาะ 15 ส.ค. หลังแข่ง 9 โมเดล · กรรมการ 7 เสียงยกที่ 1 ห้าเสียง) |
| `CARD_PICKER_MODEL=claude-sonnet-5` | ขั้น 4 (เลือกการ์ด) ใช้ sonnet-5: จุด A คิดเบา+แคชสารบัญ · จุด B คิดกลาง (แล็บ 100+ นัด: แม่นเท่าคิดหนัก นิ่งกว่า ผ่านเคสกับดักตาย/เลิกรา) | ไม่ตั้ง = **claude-sonnet-5 (ตัวจริง — เจ้าของเคาะเปิด 15 ส.ค. ดึก ba63139)** · ถอยกลับ: `CARD_PICKER_MODEL=gpt-5.6-luna` |
| `CLAUDE_WRITE_MODEL=claude-opus-4-8` | ขั้น 5 (นักเขียน) ถอยกลับแชมป์เดิมทั้งระบบ — ค่าเดียวได้ระบบเดิมเป๊ะ (มีข้อสอบคุม) · 🔴ห้ามตั้ง =claude-fable-5 (ปล่อยว่าง=fable อยู่แล้ว ตั้งแล้วลามด่านอื่น) | ไม่ตั้ง = **claude-fable-5 หลัก → opus-4.8 สำรอง** (เจ้าของเคาะ 15 ส.ค. หลังดวล 2 รอบ · 3ae6508) |
| `VIRAL_TRUISM_ENDING=1` | จบโพสต์ด้วยสัจธรรม (กฎยุคแรก — ถูกปิด 1 ส.ค. เพราะชนกฎย่อหน้า) | ปิด (แพ็ค v2 ใช้ "ประโยคนิยามภาพ" แทน ซึ่งคนละตัวกัน) |
| `VIRAL_MATCH_MODE=score` | จับคู่ด้วยคะแนนโค้ดล้วน (ไม่จ่าย AI) | ปิด |

## จุดกู้ใหญ่ (ถ้าสวิตช์เอาไม่อยู่)
- ป้าย GitHub `viral-formula-best-14aug69` = สูตรตัวจริงทั้งระบบ ณ 14 ส.ค. (ก้อนสำรอง OneDrive + ดิสก์ 2 จุด — วิธีกู้อยู่ในไฟล์ "วิธีกู้สูตรไวรัล-อ่านก่อน.md" ในก้อน)

## จุดในโค้ด (สำหรับ AI/ช่างที่มาแก้ต่อ)
- ตัวเลือกครู + สไตล์แพ็คทั้ง 2 เวอร์ชัน + ตัวทับไลก์จริง: `src/lib/services/viralFewshot.js`
- ล็อกย่อหน้า/ความยาว: `summarizeServiceText.js` + `summarizeService.js` (บรรทัด lengthConfig — ตัวจริงคือสาย Text เพราะ TEXT_ONLY)
- ไฟล์ไลก์จริง: `data/viral-likes-real.json` (สร้างโดย `scripts/match-real-likes.mjs` จาก CSV เพจ · ครอบ 130/202 ใบ) — ไม่มีไฟล์ = ระบบข้ามเงียบๆ ไม่พัง
  · ใบที่ไม่มีไลก์จริงได้ "ค่ากลางของโผ" + ทั้งโผบีบสเกลด้วย sqrt → **ทุกใบยังมีสิทธิ์ถูกหยิบจริง** (กันครูหดตามบทเรียน 14 ส.ค.)
- ข้อสอบ: `node tests/viral-fewshot-rotation.test.mjs` (32 ข้อ — 24-26 สวิตช์/แพ็ค v2 · 29-32 ตัวถ่วงไลก์จริง)
