# 📋 แผนแก้และปรับปรุงระบบข่าวให้สมบูรณ์ — เสนอเจ้าของ 1 ส.ค. 2569

**ที่มา:** รายงานออดิต `AUDIT-NEWS-PIPELINE-20260801.md` + คำสั่งเจ้าของวันนี้ (แก้ตามออดิต · ใช้โมเดลเก่งตามถนัด · โล๊ะโมเดลต่ำกว่า 5.6 ทั้งสาย · คลัง 201 ใบต้องหยิบได้ทุกใบ · เสนอแผนก่อนลงมือ)
**สถานะ:** ⏸️ รอเจ้าของเคาะ — ยังไม่แตะโค้ดใดๆ นอกจากข้อ 0
**ผู้เขียนแผน:** Fable 5 (ultracode) · ทุกจุดในแผนเปิดโค้ดจริงยืนยันแล้ววันนี้

---

## 0) ทำไปแล้ววันนี้ (ตามคำสั่งแรก — ไม่ต้องเคาะซ้ำ)
| งาน | ผล |
|---|---|
| กู้ด่านกันลิงก์ TEXT_ONLY 4 ไฟล์ (auto / auto/process / auto/stream / queue/add) | ✅ ตรวจ diff ก่อนกู้: เป็นรอยลบล้วน 50 บรรทัด ไม่มีงานอื่นปน — กู้จาก HEAD สำเร็จ |
| พรีเช็คสุขภาพโมเดล (verify-arms) | ✅ 7/7 เขียว: claude haiku/sonnet/opus/fable + codex luna/terra/sol |
| เปิดโค้ดยืนยันทุกจุดที่จะแก้ (ตามคำเตือนในออดิต) | ✅ ตรงรายงาน 100% + เจอเพิ่ม: ป้ายหลอกมี 2 ชั้น (บันทึก+โชว์ผล) · ปุ่ม "ลบทั้งหมด" มีใช้จริงในหน้าเว็บ · โค้ดมี id `fallback_builtin` อยู่แล้ว แก้ง่ายกว่าที่คิด |
| ไขปริศนาจำนวนการ์ด 3 แหล่งไม่ตรง (73/69/201) | ✅ **ของจริงบนเว็บ = 201 ใบ** (นำเข้าชุดใหญ่ 126 ใบ 25 ก.ค. ทำฝั่ง cloud) · ไฟล์ในเครื่อง 73 = สภาพก่อนนำเข้า ค้างเพราะบั๊ก sync (แผนเฟส 1 ข้อ 10 + เฟส 2) |

**สภาพเครื่อง:** คิวว่าง (ปลอดภัยต่อ build/เทส) · โค้ดเครื่องนี้ตามหลังเว็บจริง 6 คอมมิต → deploy ต้องไปทาง worktree `C:\tmp` เท่านั้น (สูตรเดิมของโปรเจกต์)

---

## เฟส 1 — ผ่าตัดเฉพาะจุด 10 รายการ (9 ไฟล์ รวม ~35 บรรทัด) — เสี่ยงต่ำ ผลสูง ⭐ ทำก่อน

### 1A — ปิดคดี "ย่อหน้า 3 ชอบจบด้วยข้อคิดแปลกๆ" (แก้ที่ต้นเหตุ: คำสั่งขัดกันเอง)
| # | ไฟล์:บรรทัด | แก้อะไร | กี่บรรทัด |
|---|---|---|---|
| 1 | `src/lib/services/viralFewshot.js:20` | ข้อ 5 "จบด้วยประโยคสัจธรรม" → ใส่สวิตช์ `VIRAL_TRUISM_ENDING` **default ปิด** แทนด้วย "จบด้วยประโยคบรรยายข้อเท็จจริงเรียบๆ" (คงกติกา "ห้ามจบด้วยคำถาม" ไว้เสมอ) — เปิดคืนได้ด้วย env `=1` | ~3 |
| 2 | `src/lib/services/summarizeServiceText.js:1123-1124` | กฎเหล็ก "ทุกข่าวต้องมีบทเรียน" → คงไว้ แต่บังคับ **ให้บทเรียนแทรกในเนื้อเรื่อง (ย่อหน้าเปิด/กลาง) เท่านั้น ห้ามยกไปเป็นข้อคิด/บทสรุป/คำอวยพรในย่อหน้าสุดท้าย** + "อิ่มใจจากเรื่องราวเอง ไม่ใช่จากประโยคสรุปแง่คิด" | 2 |
| 3 | `src/lib/ai/promptStoreText.js:186` | เติมวลี "— ไม่เกิน 2 ประโยค ไม่สรุปข้อคิดชีวิต ไม่วิเคราะห์เพิ่ม" ให้เท่าสาย URL (`promptStore.js:186`) ที่แพตช์ไปแล้วแต่ตกสายที่ใช้จริง | 1 |

> ⚠️ ตามออดิต: **ห้ามแก้ด้วยการไล่แบนคำ** (โมเดลแต่งวลีเอง แบนแล้วจะแต่งใหม่) — 3 ข้อนี้แก้ที่คำสั่งขัดกันตรงๆ

### 1B — ปิดป้ายหลอก "ใช้หอสมุด" (ไฟล์เดียวกับข้อ 2)
| # | ไฟล์:บรรทัด | แก้อะไร |
|---|---|---|
| 4 | `summarizeServiceText.js:737` | เส้นคิวที่หลุดไปใช้พร้อมท์ Built-in → บันทึก `promptSource='fallback_builtin'` ตามจริง (เดิมติดป้าย `library`) |
| 5 | `summarizeServiceText.js:748` | `matchType` ของเคส Built-in → `'FALLBACK'` (เดิม `PRE_SELECTED` ปนกับการ์ดจริง) |
| 6 | `summarizeServiceText.js:1560` | ผลลัพธ์โชว์ชื่อ "📦 Built-in Fallback V12" ตามจริง (เดิมโชว์ "📦 Library" ทั้งที่ไม่ใช่) |

ผล: วันไหนระบบหลุดไปใช้พร้อมท์สำรอง **จะเห็นใน log ทันที** — ปิดความเสี่ยง "หลุดแล้วดูไม่ออก"

### 1C — บั๊กร้ายแรงแก้ง่าย 3 ข้อ + งานตามเก็บ 1 ข้อ
| # | ไฟล์:บรรทัด | ปัญหา → แก้ |
|---|---|---|
| 7 | `src/lib/ai/openai.js:187-189` | ไม้สอง gpt-4o ถูกส่งเพดานคำตอบ 24,000 แต่รับได้ 16,384 → **ตายทุกครั้งที่ถูกเรียก** (= ไม่มีตัวสำรองจริงทั้งระบบ) → ใส่ตัวหนีบเพดาน 16,384 เฉพาะรุ่น legacy (~3 บรรทัด) — ไฟล์คุ้มครอง แก้เฉพาะจุดนี้ตามที่ออดิตอนุมัติ |
| 8 | `src/app/api/queue/clear/route.js:18` | ล้างคิวทั้งระบบได้โดยไม่มีรหัส → โหมด `all` ต้องส่ง header `x-admin-key` ตรงกับ env `ADMIN_API_KEY` (**fail-closed**: ไม่ตั้ง env = ล็อกตาย) · โหมด `stale` ปกติใช้ได้เหมือนเดิม |
| 9 | `src/app/api/prompt-library/route.js:153` + `src/app/prompt-library/page.js` | `DELETE ?id=all` ลบคลัง **201 ใบ** เกลี้ยงในคลิกเดียวไม่มีรหัส → ใส่ด่านเดียวกับข้อ 8 เฉพาะ `id=all` + หน้าเว็บถามรหัสก่อนส่ง (ปุ่มยังใช้ได้ ไม่หัก) · ลบรายใบปกติไม่กระทบ |
| 10 | `src/lib/persistStore.js:251` | `update()` เขียนแค่ Supabase ไม่ sync ไฟล์สำรอง (ผิดกฎ AGENTS.md ข้อ 10 — ตัวเดียวที่ตกหล่น) → เติม sync ตามแพทเทิร์นเดียวกับ `add()` (~6 บรรทัด) — ต้นเหตุไฟล์ค้างตั้งแต่ 1 ก.ค. |

---

## เฟส 2 — คลังพร้อมท์ 201 ใบ ต้องหยิบใช้ได้ทุกใบ 🆕 (คำสั่งวันนี้)

**ตรวจแล้ว:** ตัวให้คะแนน (`promptMatcher.js`) **ไม่มีเพดานจำนวนใบ** — สแกนทุกใบที่โหลดเข้ามา · ตัวโหลดฝั่งเว็บอ่านครบ (แบ่งหน้า 1000 แถว เพดาน 20,000) → โครงหลักรองรับ 201 ใบอยู่แล้ว ความเสี่ยงจริงอยู่ 3 จุดนี้:

| # | งาน | ทำไม | วิธี |
|---|---|---|---|
| 2.1 | **อัปเดตไฟล์สำรองในเครื่อง 73 → 201** | ตอน Supabase สะดุด ระบบ fallback มาไฟล์นี้ → การ์ดใหม่ 128 ใบ "หายจากสนาม" ชั่วคราว และไฟล์นี้ถูก deploy ขึ้นเว็บเป็น fallback ด้วย | สคริปต์ node ดึงครบจาก Supabase → เขียน `data/prompt-library.json` → commit (หลังแก้เฟส 1 ข้อ 10 ไฟล์จะเดินเองต่อ) |
| 2.2 | **ตรวจช่องข้อมูลจับคู่ครบทุกใบ** | คะแนนจับคู่คิดจาก category/emotionalTags/conflictTags/archetype/hookStyle — ใบไหนช่องพวกนี้ว่าง = คะแนนต่ำตลอดกาล **ไม่มีวันถูกหยิบ** (การ์ดนำเข้าใหม่ 128 ใบยังไม่เคยถูกตรวจ) | สคริปต์ตรวจ 201 ใบ รายงานใบที่ขาด/ช่องที่ขาด → เติมข้อมูลด้วยเครื่องมือเดิมของระบบ (ไม่เจนการ์ดใหม่) |
| 2.3 | **รายงานการกระจายการใช้** | สถิติเดิม: แชมป์ใบเดียวถูกใช้ 210 ครั้ง ขณะที่การ์ดใหม่หลายใบ "ใช้ 0 ครั้ง" — ต้องรู้ว่ากระจายจริงแค่ไหน | สรุปใบใช้ 0 ครั้งต่อหมวด · ถ้ากระจุก เสนอจูน `PROMPT_VARIETY_BAND` (env บนเว็บ ไม่แตะโค้ด) |
| 2.4 | (พ่วง) วิเคราะห์ 201 ใบ: กี่ใบสั่ง "ย่อหน้า 3 = บทเรียน" | ตัวเลขเดิม 59/73 นับจากคลังเก่า — คลังจริงใหญ่ขึ้น ต้องรู้ขอบเขตจริงก่อนตัดสินใจเฟส 5 | วิเคราะห์จากไฟล์ที่ refresh แล้ว (งานอ่านอย่างเดียว ไม่มีค่า AI) |

---

## เฟส 3 — โล๊ะโมเดลต่ำกว่า 5.6 ออกทั้งสาย 🆕 (คำสั่งวันนี้)

**หลักเลือก (ตามสั่ง "เหมาะกับงาน ไวประหยัด"):** `gpt-5.6-sol` = งานหนัก/เขียน · `gpt-5.6-terra` = มาตรฐาน + **ไม้สองทุกตัว** · `gpt-5.6-luna` = งานเร็วประหยัด
**ข้อได้เปรียบ:** ทั้งตระกูลวิ่งผ่านท่อ OpenAI เดิม — terra พิสูจน์บนเว็บจริงแล้วตั้งแต่ 16 ก.ค. (ขั้น breakdown: เร็วกว่า 3 เท่า ถูกกว่าครึ่ง คุณภาพเท่า) → ไม่ต้องเดินท่อใหม่

### 3.1 แกนกลาง `src/lib/ai/modelConfig.js` (จุดเดียวคุมทั้งระบบ)
| ค่า | เดิม | ใหม่ | เหตุผล |
|---|---|---|---|
| MODEL_CONTENT_WRITE (ตัวเขียนข่าว) | gpt-5.5 | **gpt-5.6-sol** | งานหัวใจ ต้องตัวเก่งสุด — ราคาเท่าเดิมเป๊ะ ($5/$30) |
| MODEL_MAIN_REASONING / NEWS_ANALYSIS / FINAL_QA | gpt-5.5 | **gpt-5.6-sol** | งานคิดหนัก ยกทั้งชุด |
| MODEL_COVER_JUDGE / MODEL_VISION | gpt-5.5 | **gpt-5.6-sol** | ระบบปกปิดอยู่ — เปลี่ยนให้ครบสายแบบไม่มีผลข้างเคียงตอนนี้ |
| MODEL_BREAKDOWN | gpt-5.6-terra | คงเดิม ✅ | ตัวอย่างความสำเร็จที่พิสูจน์แล้ว |
| MODEL_FAST_CHEAP + keyword/json/caption/spell | gpt-5.4-mini | **gpt-5.6-luna** | งานเร็ว — luna $1/$6 (จาก $0.75/$4.5 — แพงขึ้นจิ๋วมากแต่รุ่นใหม่กว่า ฉลาดกว่า) |
| MODEL_HEAVY_FALLBACK (ไม้สอง) | gpt-4o (**ตายอยู่**) | **gpt-5.6-terra** | ไม้สองที่ "รอดจริง" — วัดจริงงานข่าว 42 วิ + ถูกกว่า sol ครึ่งหนึ่ง |

### 3.2 โซ่ไม้สองใน `openai.js` (จุดเดียวที่ hardcode โซ่)
`sol→terra` · `terra→sol` · `luna→terra` · (เผื่อจุดตกค้าง: `5.5→terra`, `mini→luna`) · คงตัวหนีบ legacy จากเฟส 1 ไว้

### 3.3 จุดฝังรายไฟล์ — สายที่วิ่งจริง (live)
| ไฟล์ | เดิม → ใหม่ |
|---|---|
| `correction/viralPolishService.js:15` (ขัดเงาภาษา) | gpt-5.5 → **sol** |
| `correction/flagFixerService.js:15,87` (แก้จุดผิด) | gpt-4o → **terra** · gpt-4o-mini → **luna** |
| `services/clipInsightService.js:215,538` (ถอดคลิป — ไม้สองของ Gemini) | gpt-5.5 → **sol** |
| `services/contentScreen.js:39` (ด่านคัดเนื้อหา) | gpt-5.5 → **sol** |
| `services/newsDesk/aiRaw.js:20-21` (โซ่ไม้สองฝั่งโต๊ะข่าว) | 4o/4o-mini → **terra/luna** |
| `lib/aiClient.js:17` | gpt-4o → **sol** |
| จูนเวลา: `summarizeServiceText.js` + `summarizeService.js` ไม้สอง breakdown | 60 วิ → **90 วิ** (terra วัดจริง ~42 วิ + เผื่อ) — คอมเมนต์งบเวลารวมอัปเดตตาม |

### 3.4 ระบบที่ปิดอยู่ (ปก/โต๊ะข่าว/ตาหาหน้า) — เปลี่ยนให้ครบสายแบบ dormant
`coverDirectorService` (gpt-4o ×3) · `coverShotPlanner` · `coverPlannerService` · `candidateHeroVision` · `candidateIdentityVerifier` · `evidenceConfidenceService` · `captionAnalyzerService` · `metaFrameExtractor` · `auto-cover` (×3) · `faceDetector` (มี env `FACE_MODEL` รองรับ luna แล้ว — สลับ default) · `deskModelConfig` — ทั้งหมด → sol/terra/luna ตามน้ำหนักงาน + ป้ายบนหน้าเว็บ (`ScanSummary`/`EditorPanel`/`HuntSetup` ที่โชว์ชื่อ gpt-5.5) + ค่า default หน้า settings + อัปเดต AGENTS.md ให้ตรงความจริงใหม่

### 3.5 เงิน + ความเสี่ยง
- **ค่าใช้จ่ายรวม ≈ เท่าเดิม**: ตัวเขียน sol ราคาเท่า 5.5 เป๊ะ · luna แพงกว่า mini เล็กน้อยในงานจิ๋ว · **ได้ไม้สองที่ใช้ได้จริงคืนมา** (เดิมจ่ายให้ 4o ที่ตายฟรีทุกครั้ง)
- **เสี่ยงหลัก: สำนวนตัวเขียนเปลี่ยน** → คุมด้วยเทสเจนจริง 3-5 ชิ้นเทียบก่อน/หลัง (ใช้ข่าวจริงชุดเดิมจากคิว) — ถ้าสำนวนแย่ลง revert ได้ใน 1 คอมมิต
- **ทางเลือก opus5 เป็นตัวเขียน**: ท่อ Claude มีอยู่แล้วในระบบ (`claudeClient.js` + SmartAI — เคยใช้ A/B Sonnet 5) → เสนอทำเป็น **เฟสถัดไป**: A/B opus5 vs sol หลังตระกูล 5.6 นิ่ง 2-3 วัน (ต้องตั้งกุญแจ Anthropic บนเว็บ + แก้จุดตรวจชื่อโมเดล 1 บรรทัด) — ไม่ควรเปลี่ยน 2 อย่างพร้อมกันในวันเดียว จะแยกไม่ออกว่าผลมาจากอะไร

---

## เฟส 4 — เก็บกวาดเสถียรภาพ (คัดจากบั๊กที่เหลือใน 61 ข้อ) — ทำเป็นแบตช์ แบตช์ละ ≤3 ไฟล์

| ลำดับ | เรื่อง | ไฟล์ | อาการ |
|---|---|---|---|
| 4A-1 | งบเวลา worker เกินเพดานตัวเอง (960 วิ ใน 800 วิ) | `queue/worker/route.js:78` | งานหนักถูกตัดกลางคันแบบไร้ร่องรอย |
| 4A-2 | ผลลัพธ์ว่าง/สั้นผิดปกตินับเป็น "สำเร็จ" | `summarizeServiceText.js:1540` | ข่าวเปล่าหลุดออกไปได้ |
| 4A-3 | DNA วิเคราะห์ล้ม → เสียบ "ดราม่าสังคม" ปลอมเดินต่อเงียบ | `summarizeService.js:836,2071` | ได้การ์ดผิดแนวทั้งใบ ไม่มีป้ายเตือน |
| 4A-4 | จ่ายเงินเกลาคำแล้วโยนผลทิ้ง 100% (เช็คชนิดข้อมูลผิด) | `safeCorrectionService.js:141` | เผาเงินฟรีทุกงาน |
| 4A-5 | Gemini timeout ตั้งผิดชั้น (15 วิ ไม่เคยทำงานจริง) | `ai/geminiClient.js:91` | สลับไม้สองไม่ทัน |
| 4B-1 | Tavily ไม่มี timeout + ไม้สองวิจัยไปไม่ถึง | `tavilyService.js:37` + `research-search/route.js:25` | ค้างยาว |
| 4B-2 | เซฟคลังข่าวยิงแล้วไม่รอ แต่ตอบว่าเซฟแล้ว | `auto/process/route.js:219` | บนเว็บอาจไม่ได้เซฟจริง |
| 4B-3 | กุญแจคิว default `test-key` + ตรวจเฉพาะเมื่อส่ง header | `queue/add/route.js:22` | ต้องประสาน env ฝั่ง Discord ก่อนแก้ |
| 4B-4 | Auto-Pilot โต๊ะข่าว default = เปิด (ขัดนโยบาย) | `newsDesk/harvester.js:985` | ตอนนี้ dormant เพราะปิดโต๊ะข่าวอยู่ — แก้กันอนาคต |
| 4B-5 | เขียนไฟล์ data ทับตรง ไม่ atomic + การ์ดข้อมูลรั่ว 1 ใบ | `persistStore.js:96` + `prompt_1ebbcf93` | ไฟล์เสี่ยงพังตอน crash |
| 4C | ชุดตาเห็น (ต่อยอดช่องว่าง KPI): บันทึกโมเดลจริงต่อเวอร์ชัน · เก็บ promptId ต่อเวอร์ชัน · ขยายประวัติคิว >12 งาน | `generationLogger.js:101` ฯลฯ | ข่าวพังแล้วชี้ตัวการได้ |
| 4D (ทีหลัง) | SSRF allow-list (สาย URL ปิดอยู่) · ล็อก `/api/auto/stream` ที่เลิกใช้ | — | ความเสี่ยงต่ำตอนนี้ |

> ⚠️ กลุ่มนี้ยังไม่ผ่านฝ่ายค้านตรวจซ้ำ — ทุกข้อจะ**เปิดโค้ดยืนยันก่อนแก้**เหมือนที่ทำกับเฟส 1 วันนี้

---

## เฟส 5 — ล้างการ์ดสอนผิด (ทำ**เฉพาะ**ถ้าเทสเฟส 1 แล้วยังมีอาการ)
- ล้างคำสั่ง "ย่อหน้า 3 ปิดด้วยบทเรียน/คำอวยพร" ในการ์ด (ตัวเลขจริงรอผลเฟส 2.4) → เหลือ "ปิดด้วยข้อเท็จจริงที่มีน้ำหนัก"
- ใส่ตัวกรองโรงงานสกัดการ์ด ไม่ดูดแพทเทิร์น "จบด้วยข้อคิด" เข้าคลังอีก
- ตาข่ายท้ายทาง: กฎ correction จับสำนวนข้อคิดที่ย่อหน้าปิด (จับแพทเทิร์นประโยค ไม่ใช่แบนคำเดี่ยว)

---

## 🔒 ขั้นตอนคุมคุณภาพ (บังคับทุกเฟส)
```
เปิดโค้ดยืนยัน → สำเนา .bak → แก้เฉพาะจุด → validate-workflow ผ่าน 100%
→ เช็คคิวว่างก่อน build → next build ไม่มี error
→ เจนข่าวจริง 3-5 ชิ้นบนเครื่องเทส เทียบก่อน/หลัง (ผู้ตัดสินอิสระ ไม่ใช่คนเขียน)
→ Sol 5.6 ultra ตรวจ diff ทั้งหมด (กติกา: คนเขียนห้ามตรวจงานตัวเอง)
→ commit แยกเฟส (ถอยทีละเฟสได้) → deploy ผ่าน worktree C:\tmp → เฝ้างานจริงชิ้นแรกบนเว็บ
```

## 🤖 ตารางมอบงานโมเดล (Model Decision Log)
| งาน | โมเดล | เหตุผล |
|---|---|---|
| เฟส 1A+1B (ชั้นพร้อมท์ไทย 3 ไฟล์) | **Fable 5** (effort สูงสุด) | งานภาษาไทยละเอียด แก้คำสั่งขัดกัน — พลาดนิดเดียวกระทบข่าวทุกชิ้น |
| เฟส 1C (route/clamp/sync 5 ไฟล์) | **Opus 5** (high) | ตรรกะโค้ดชัดเจน ต้อง correctness สูง |
| เฟส 2 (สคริปต์ตรวจ+refresh คลัง) | **Opus 5** (high) | งานสคริปต์+ข้อมูล แม่นยำ |
| เฟส 2.4 (วิเคราะห์การ์ด 201 ใบ) | **Fable 5** | งานอ่านภาษาไทยเชิงลึก |
| เฟส 3 (โล๊ะโมเดล — แก้ตามแผนที่ที่สำรวจครบแล้ว) | **Fable 5** ทำเอง | ถือแผนที่ครบทุกจุดในมือ งาน mechanical ที่ห้ามตกหล่น |
| เฟส 4 (แบตช์เสถียรภาพ) | **Opus 5** นำ | แก้ระบบ async/timeout ตรรกะหนัก |
| ตรวจรับก่อนขึ้นเว็บ (ทุกเฟส) | **Sol 5.6 ultra** | ผู้ตรวจอิสระ — ไม่ได้เขียนสักบรรทัด |
| ผู้ตัดสินเทสเจน (คุณภาพข่าว) | **Opus 5** | อิสระจากคนแก้พร้อมท์ (Fable) |

## ✋ สิ่งที่เจ้าของต้องเคาะ/ตั้งค่า
1. **เคาะเฟสที่ให้ลงมือ** — แนะนำ: เฟส 1+2+3 รอบเดียว (เทสรวมชุดเดียวคุ้มสุด) → เฟส 4 ทยอยเป็นแบตช์ → เฟส 5 ตามผลเทส
2. **ตั้ง `ADMIN_API_KEY`** ใน Vercel + `.env.local` (รหัสปุ่มลบล้าง — ไม่ตั้ง = ปุ่มลบทั้งหมดล็อกตาย ซึ่งปลอดภัยกว่าเดิม)
3. (เฟสถัดไป ถ้าสนใจ opus5 เป็นตัวเขียน) ตั้งกุญแจ Anthropic บนเว็บ → ผมจัด A/B ให้

*ประเมินเวลา: เฟส 1+2+3 + เทส + ตรวจ + deploy = จบในวันเดียว · เฟส 4 อีก 2-3 แบตช์ตามสะดวก*
